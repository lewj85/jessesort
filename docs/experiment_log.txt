JESSESORT EXPERIMENT LOG
Last updated: 2026-09-08
Status: compact source of truth for current production, active research roadmap, and decision-level experiment history.
experiment decisions, propagation status, and decision-relevant historical evidence.

LOG COMPACTION POLICY
- This log intentionally summarizes old experiments rather than preserving every procedural detail.

EXPERIMENT DECISION POLICY
- Use REJECT only when a mechanism is broadly inferior, causally unsound, or no practically useful regime survives validation.
- If a candidate has a substantial/repeatable win in a meaningful regime but loses elsewhere, mark it PROMISING / MORE ANALYSIS NEEDED rather than rejecting it. Test simple general routing variables already available to the algorithm (especially n, sizeof(T), run/pile count, or an existing structural signal) before closing it.
- A size threshold is legitimate when a crossover is stable across intermediate/OOD sizes; exact canonical-size checks are not. A non-monotone crossover means size alone is insufficient, not that the underlying mechanism should be discarded.
- Preserve promising experimental implementations/results even when they are not yet production-retained, and record the next discriminating experiment explicitly.
- Preserve what affects future decisions: mechanism tested, variation scope, retain/reject status, meaningful speedups/regressions, crossover thresholds, and why a result mattered.
- Remove stale next-step prose, documentation-edit narration, obsolete artifact paths, repeated benchmark tables, shell/build minutiae, and old absolute timings after later algorithm changes make them non-comparable.
- Old percentage/ratio results are historical causal evidence, not current performance claims.
- Default history size is 1-3 lines per experiment. Keep expanded entries only for architectural pivots or the currently active research chain; banked baseline files hold procedural/timing detail.
- Section 1 defines CURRENT code/policy. Section 2 is the ONLY active roadmap. Section 3 is the single complete chronological experiment history.
- Do not recreate long-form experiment diaries. Add concise evidence and update current sections in place.

===============================================================================
1. CURRENT REPOSITORY STATE
===============================================================================

1.1 BENCHMARK AND AUDIT STANDARD
- E077 introduced the whole-sort benchmark methodology; E100 centralizes its deterministic seed policy.
- E319 reduced the routine benchmark roster to seven Jessesort variants plus `std::sort`; architecturally interesting non-routine headers remain preserved under `include/jessesort/experimental/`.
- E320 retired multi-variation timing inside one benchmark process after proving that related variants could warm a shared backend for one another and move a bimodal median.
- E323 removed the pre-timing reference-sort bias and defined warm-state steady-throughput. E337 closes the infrastructure audit by isolating every **algorithm × canonical-input cell** in its own process, randomizing/logging all cell order, keeping reference `std::sort` after timing, and making fixed CPU affinity canonical when `taskset` is available. E337 was necessary because algorithm-only isolation still allowed fixed workload order and unpinned scheduler/frequency bimodality to move short-input medians.
- Canonical `make bench` profile (E466 runner cleanup):
    * C++20, -O3, -march=native, -DNDEBUG
    * routine set = 8 Jessesort descriptors + independently timed `std::sort`
    * default n = {10k, 100k} only; 1k/1m require explicit direct invocation and are never selected by the routine runner
    * 2 untimed warmups per size/algorithm/input process
    * 500 measured independent trials per cell in one uninterrupted process after warmups
    * every size × algorithm × input is a separate process; one deterministic unique seed per trial
    * the suite records and enforces exact CPU model plus benchmark/source fingerprints on resume
    * active-cell rows stay quarantined under `in_progress/`; interrupted cells are discarded and rerun from trial 0
    * each fully validated cell is atomically committed; 100k refreshes the cumulative ZIP after every cell, while 10k refreshes once after all 14 inputs for the current algorithm to avoid checkpoint overhead dominating short cells
- CACHE/PROCESS POLICY:
    * Do **not** use Linux `drop_caches` as a CPU-cache flush; it targets filesystem page cache/dentries/inodes, not L1/L2/L3/BTB/TLB state.
    * Do **not** add a giant userspace eviction buffer to the canonical benchmark. That changes the workload into an artificial cold-cache/memory-pressure test and still cannot normalize instruction cache, branch predictor, TLB, or heterogeneous shared-cache topology.
    * Residual hardware state from a previous child may survive process exit, but it is consumed by the next cell's untimed algorithm/input-specific warmups before measured trials. The benchmark intentionally measures warmed steady behavior.
    * The source vector is copied immediately before the timer, so input data is intentionally hot in a consistent way across algorithms.
    * Every size × algorithm × input cell is a separate process. Cell order is randomized and recorded to prevent permanent algorithm or workload identity correlation with long-timescale host/thermal/frequency drift. The default one-second cooldown is a thermal/frequency settling aid, not a claimed cache flush.
    * Canonical runs auto-pin children to the first CPU in the runner's allowed affinity mask when `taskset` is available; override with `BENCH_CPU=<cpu>` or disable with `BENCH_CPU=none`. E337 promoted pinning from optional to canonical after it collapsed a strong unpinned Random%25 bimodality.
    * CPU frequency, thermal state, interrupts, VM contention, and host migration remain external noise sources. Close decisions still require same-session controls and repeated confirmation.
- VALIDATION POLICY:
    * Correctness validation remains outside timed regions.
    * The reference `std::sort` used for exact-output validation runs after the timed algorithm. This removes the E323-discovered bias where the `std::sort` control was always timed as a second back-to-back `std::sort`, and every Jessesort timing immediately followed a full reference sort.
- CPU REPORTING IS MANDATORY FOR EVERY TIMED TEST: copy the exact CPU model reported by `/proc/cpuinfo` / benchmark metadata into the experiment-log entry alongside timing evidence. Do this for localized harnesses as well as full canonical sweeps. Hosted sessions may migrate between CPU models, so an experiment without its CPU recorded is not a safe longitudinal timing reference.
- E204 established that unchanged code can move materially across CPU models; historical absolute microseconds are therefore not longitudinal baselines. Record CPU/compiler with every timed result and use matched-session controls for causal comparisons.
- E205's rotating same-process order policy is **superseded** by E320/E323 for cross-variation timing. E205 remains useful evidence that benchmark context itself can move timings, but it is no longer the canonical process model.
- PRE-E323 TIMING PROVENANCE:
    * Pre-E320 timing comparisons that placed related algorithms in one process are susceptible to shared-backend cache/predictor/order contamination. Structural/correctness evidence remains valid; small timing-derived decisions are provisional until audited.
    * E320-E322 removed same-process cross-variation contamination, but still performed an untimed reference `std::sort` before each timed call and ran child algorithms in a fixed shell order. Large architectural effects remain useful; close ratios versus `std::sort` and near-noise-floor variation differences remain provisional.
    * Do not rewrite old experiment history. Mark retained decisions as revalidated, structurally safe, superseded, or still provisional as the audit proceeds.
- MANDATORY POST-CHANGE ACCEPTANCE RULE: test only the maintained variation(s) whose executable sorting behavior is affected by the change. A one-variation change requires canonical acceptance only for that variation. If that variation imports/consumes changed logic from another maintained variation, test the dependent/source variation too; this normally means at most two variations, not the full routine bank.
- Every performance-change evaluation must preserve the canonical timing/distribution evidence for the affected variation(s). Localized/phase harnesses remain the primary causal test; the affected-variation canonical run is the repository-level acceptance check. A full routine-bank sweep is optional infrastructure/periodic validation, not a retention prerequisite for a narrowly scoped algorithm change.
- AFFECTED-VARIATION REGRESSION AUDIT: acceptance is not complete until every canonical timing cell for each affected variation at every tested size is compared against an immediately rerun same-session control using independent-process medians and distributions. Any apparent regression >1.0% must be investigated before retention. Repeat flagged cells under matched conditions and inspect p25/p75, IQR, MAD, and trial distributions rather than deciding from one median.
- For close A/Bs, candidate and control must not be timed sequentially in the same process on the same trial input. Prefer separate candidate/control processes or separately built binaries with identical seed schedules. Same-binary/phase harnesses may still be used for structural diagnosis or untimed counters, but not as sole evidence for a small whole-sort retain decision.
- E405 HOST-BIMODALITY GUARDRAIL: on hosted CPUs, identical code can occupy distinct execution-speed modes for long contiguous trial bursts even under fixed CPU affinity. If an inactive/null path moves >1% or a fast cell is visibly multimodal, a one-shot process median is not decision-grade. Repeat independently launched candidate/control cohorts with identical seed schedules and alternating process order; make the decision from the distribution/median of process-level ratios, and use inactive rows to estimate common-mode movement. Do not attribute null-path movement to the code change.
- When a canonical affected-variation run conflicts with a smaller causal harness, treat the causal harness as mechanism evidence and the isolated canonical run as repository-level acceptance evidence; investigate rather than allowing either one to silently overrule the other.
- Targeted numeric-size invocations use the requested trial count directly; they do not automatically apply the 1m 10:1 reduction.
- Canonical seed policy for normal benchmarks and one-off A/B tests:
    * base seed: `jessesort::bench::kCanonicalBaseSeed` (0x8A5CD789)
    * mixer: `jessesort::bench::trial_seed()` in `benchmarks/benchmark_seed.h`
    * do not invent a new seed population for an ordinary one-off test; explicitly label intentional held-out/confirmation cohorts instead.
- Preserve raw per-algorithm timings. Report median, p25/p75, IQR, MAD, and ratio of independent medians as appropriate; do not silently trim outliers. Paired cross-algorithm trial ratios are not part of the canonical benchmark.
- Full rationale and remaining limitations: `docs/benchmark_methodology.md`.
- Baseline workload families (frozen in E180C): Random, Sorted, Reverse, Sorted+Noise(5%),
  Sorted+Noise(10%), Random%25, Alternating, Sawtooth, MixedDirectionRuns, BlockSorted, OrganPipe,
  and Rotated. E228 adds two permanent router-safety phase-change families without modifying any of
  those 12 generators: MixedPhase3 = Sorted+Noise(5%) -> MixedDirectionRuns -> Random in three
  contiguous near-equal phases; MixedPhase12 = the 12 frozen baseline families once each in baseline
  order in 12 contiguous near-equal phases. Current acceptance therefore covers 12 frozen baseline
  families + 2 router-safety families. Synthetic diagnostics outside these 14 may explain mechanisms
  but may not tune production policy unless first promoted to the permanent benchmark suite.
- HISTORICAL PERFORMANCE REFERENCES:
  * E189 preserves the older Intel Xeon Platinum 8573C canonical tables used in the README for historical comparison.
  * E203 is a same-session AMD EPYC 9V74 snapshot of the then-maintained pre-phase-map bank; it is historical, not a current baseline.
  * Detailed historical timing artifacts were pruned from the maintained repository; the compact historical summary here is authoritative.
  * Current optimization decisions must use an immediately rerun same-session baseline for the affected tagged variation(s), with CPU/compiler recorded.
  * E204 demonstrated why: unchanged code moved materially across CPUs, so absolute historical microseconds are not longitudinal performance claims.
- Structured-generator scaling (E180C): use one consistent target scale ~= n^(2/3) everywhere
  E180B introduced sqrt(n). Sawtooth uses that target as its regular period; BlockSorted uses seeded
  variable ascending block lengths around it before block-order shuffling; MixedDirectionRuns uses seeded
  variable monotone run lengths around it with randomized direction/range/step. This keeps both run count
  and run length growing with n without the overly short-run sqrt(n) regime discovered in E180B.
- `Rotated` intentionally keeps a randomized split. E100 audited 500 canonical 100k
  trials: mean split/discontinuity position 49.322%, median 49.954%, decile counts close
  to uniform, and exactly 250/500 positions before versus at/after the nominal 50%
  freeze decision point. Keep the randomized generator; control it through canonical
  seeds rather than deterministic stratification.
- `make bench` / `./benchmark.sh` is the canonical multi-algorithm runner. Resume occurs only at committed measurement units: an interrupted 500-trial cell is discarded and rerun from trial 0 with fresh warmups, while completed cells are never rerun. The runner locks CPU model and benchmark/source/seed/runner fingerprints so evidence from different binaries or hosts cannot be silently mixed.
- DURABLE CHUNK POLICY (E209): long experiments must be split into small resumable checkpoints rather
  than relying on one execution environment to finish a multi-hour sweep. Preferred sequence is compile/smoke,
  localized 10k, localized 100k, retain/reject, then affected-variation acceptance one size (or small pattern group) at a time.
  Flush every completed trial immediately; after each chunk, regenerate/save its summary and snapshot any retained
  source/documentation state before beginning the next chunk. Environment/tool timeout must cost at most the current
  incomplete trial/cell, never the whole experiment.
- Validate workload generators rather than trusting labels. Test non-integer types and
  custom comparators separately when genericity/correctness/performance conclusions
  depend on value representation.
- Do not assume an optimization transfers between variations; propagation gets its own
  causal evidence unless the change is purely mechanical/documentary.

1.2 CURRENT MAINTAINED VARIATIONS - E229 LAYER TAGS

AUTHORITATIVE NAMING
- Grammar: `jessesort_<decomposition>_<router>_<reconstruction-and-merge>[_<other-policy>...]`.
- Underscores separate major layers; dashes combine tags inside a layer.
- Full definitions/provenance: `docs/E229_variation_tag_migration.md`.
- Historical E001-E228 records keep their original numbered names; do not rewrite them retroactively.

DEFAULT BENCHMARK DESCRIPTORS (eight Jessesort + std::sort; E458/E466 current roster)
- `physical` -> `jessesort_physical_probe-route_adj-adaptive-buffer` (pre-E225 `physical` control)
- `simulated` -> `jessesort_simulated_probe-route_adj-adaptive-buffer` (pre-E225 `simulated` control)
- `frozen-single` -> `jessesort_simulated-frozen_probe-route_adj-powersort-adaptive-buffer_single-overflow` (pre-E225 `frozen-single` control)
- `indexed` -> `jessesort_indexed_probe-route_adj-adaptive-buffer_move-only` (pre-E225 `indexed` control)
- `noalloc-direct` -> `jessesort_noalloc-direct_probe-route_inplace-adaptive_run-reclaim64_move-only` (E369 current Rust-contract/no-heap contender)
- `noalloc-low-run` -> `jessesort_noalloc-low-run_probe-route_inplace-adaptive_run-reclaim64_move-only` (restored as the eighth routine specialist after E430/E436 roster reconciliation)
- `simulated-direct` -> `jessesort_simulated_direct-merge-probe-route_adj-adaptive-buffer` (E225 direct-merge champion)
- `phase-map` -> `jessesort_simulated-direct_phase-map-mature_probe-route_adj-adaptive-buffer` (E283/E314 mature mixed-input phase-map champion)

NON-ROUTINE / EXPERIMENTAL DESCRIPTORS
- `simulated-inplace`, `frozen-deferred`, `frozen-live`, `avx2`, `linked`, `noalloc`
- `physical-direct`, `simulated-probe-direct`, `simulated-inplace-direct`, `frozen-single-direct`, `frozen-deferred-direct`, `frozen-live-direct`, `avx2-direct`, `linked-direct`, `indexed-direct`
- `simulated-phase-map` - E250 gated galloping phase-map plant; copy-based per-region execution.
- `simulated-phase-map-range` - E251 range-aware phase-map plant; zero-copy mapped-region execution.
- `simulated-phase-map-range-transition-partition` - E255 descendant; same map/range execution, with a conservative contaminated-entropy partition override.
- `simulated-phase-map-range-transition-partition-body-entropy` - E256 descendant; broadens E255 only to allow a non-STRUCTURED tail while keeping a STRUCTURED lead and ENTROPY midpoint.

CURRENT BANK SIZE
- Eight routine JesseSort descriptors are benchmarked by default: `physical`, `simulated`, `frozen-single`, `indexed`, `noalloc-direct`, `noalloc-low-run`, `simulated-direct`, and `phase-map`, plus independently timed `std::sort`. E458 restored `noalloc-low-run` as the eighth routine specialist after the E430/E436 roster audit showed its capability had survived and remained best-in-class on some regimes.

1.2A MAINTAINED ARCHITECTURE REFERENCE

CANONICAL IDENTITY QUICK REFERENCE
- `physical`-`simulated-inplace` are NON-FREEZING designs. `frozen-single`, `frozen-deferred`, and `frozen-live` are the EARLY-FREEZE/OVERFLOW designs.
  `avx2` is a `simulated`-derived SIMD/capped-probe research path and is not part of the freeze family.
  `linked` and `indexed` are maintained research families for linked-blueprint locality and index-tail genericity.
  `noalloc` and `noalloc-low-run` are the allocation-free branch: `noalloc` is the narrow bounded/fallback baseline;
  `noalloc-low-run` adds a shared physical tail pool and selective low-run general in-place merging.

  `physical`  Actual physical piles                  no freeze      no overflow
  `simulated`  Simulated blueprint                    no freeze      no overflow
  `simulated-inplace`  `simulated`-style simulation + in-place flatten no freeze      no overflow
  `frozen-single`  Early freeze + single deferred run     freeze family  one large overflow run
  `frozen-deferred`  Early freeze + deferred bands          freeze family  32-element deferred bands
  `frozen-live`  Early freeze + adaptive live overflow  freeze family  fixed32 / natural asc / bidir natural
  `avx2`  `simulated`-derived AVX2 capped-probe path      no general freeze; temporary probe spill only
  `linked`  Linked-blueprint locality family       no freeze      linked element/span reconstruction
  `indexed`  Index-tail genericity family           no freeze      move-only-capable generic Patience path
  `noalloc` Allocation-free bounded family         fixed caps      no-heap Jesse front end + in-place fallback
  `noalloc-low-run` Allocation-free shared-tail merge       fixed caps      shared 127-tail pool + gated live run-stack reclamation
  `simulated-phase-map` Gated galloping phase map       mapped regions   copy-based fresh `simulated` per region
  `simulated-phase-map-range` Range-aware phase map       mapped regions   zero-copy range-capable fresh `simulated` per region
  `simulated-phase-map-range-transition-partition` Range-aware phase map + conservative transition backend mapped regions   E251 execution + contaminated-entropy partition override
  `simulated-phase-map-range-transition-partition-body-entropy` Range-aware phase map + body-entropy transition backend mapped regions   E255 mechanism with non-STRUCTURED-tail broadening

- E050 is the canonical numbering reset. Detailed historical records have been translated
  to these implementation identities where practical; old artifact FILENAMES may still
  contain pre-E050 labels and must be interpreted by namespace/implementation identity.

CANONICAL DIFFERENCE BREAKDOWN - `physical` THROUGH `noalloc-low-run`
- `physical`: literal vector-backed physical Patience piles. No freezing. This is the physical-pile representation baseline.
- `simulated`: simulated Patience using value-tail metadata + packed 32-bit blueprint tags, followed by streaming auxiliary reconstruction. No freezing. This is the primary speed-oriented simulated baseline.
- `simulated-inplace`: controlled `simulated` reconstruction/memory variation. It intentionally shares `simulated` insertion/topology but resolves the blueprint by in-place permutation-cycle flattening. No freezing; do not route away the in-place flatten merely because E157 shows it is often slower.
- `frozen-single`: early-freeze family with one large deferred overflow run. Ordinary pile creation freezes around the retained 50% decision policy, then overflow is gathered contiguously and sorted as one run.
- `frozen-deferred`: early-freeze family with deferred 32-element overflow bands. Bands remain unsorted during insertion and are sorted during reconstruction; exact full bands use the retained specialized 32-element network.
- `frozen-live`: early-freeze family with live/adaptive overflow handling. Weak structure uses live 32-element bands; clearly ascending low-pile structure uses ascending natural runs; other strong directional cases may use bidirectional natural runs.
- `avx2`: `simulated`-derived SIMD research path. It caps the initial probe to a small pile count (default eight piles/game over the first 128 values), uses temporary probe spill, and specializes exact-eight int32 tail lookup with AVX2. It is not a general freezing variation.
- `linked`: linked-blueprint locality family. Whenever Patience reconstruction is reached, reconstruction remains linked: E201 selects compact span links for low-fragmentation blueprints or in-place element links otherwise, with first-merge fusion. Specialized direct backends/monotone exits may bypass Patience normally, but `linked` does not use ordinary `simulated` tag reconstruction merely to hide linked costs.
- `indexed`: index-tail/genericity family. Pile-tail metadata stores source indices and compares through the untouched source instead of storing copies of T. This enables the move-only/non-copyable path. Copyable `indexed` may reuse `simulated`'s optimized reconstruction/merge after blueprint creation; the defining index-tail mechanism must remain intact.
- `noalloc`: allocation-free bounded baseline. Fixed 63-asc + 64-desc source-index tails, <=64 fixed run descriptors, no O(n) blueprint/T scratch, and no heap allocation inside sort. Only cheap bounded run geometries are handled by Jesse reconstruction; unsupported shapes fall back to allocation-free partition/heapsort. E210 reduces peak explicit fixed metadata to 2,560 B plus scalar locals/call stack on the current 64-bit build.
- `noalloc-low-run`: allocation-free shared-tail + selective in-place merging. Uses one physical 127-index tail pool but **retains the current logical 63/64 split**; E213's adaptive split is not retained. E216 added geometry-gated general in-place merging for suitable <=64-run layouts. E217 extends that path past 64 natural runs by live bounded run-stack reclamation: when the fixed 64 descriptors fill, the smallest adjacent completed pair is normalized/merged in place to reclaim one descriptor. A first-65 preflight requires average run length >=1024 and rejects repeated-overlap/Sawtooth-like geometry, preserving fallback for short/high-entropy or movement-heavy cases.
- `simulated-phase-map`: E250 large-N phase-map plant. A cheap endpoint route-diversity gate decides whether to pay for x2 galloping 32-value dual-Patience observations capped at 8k; confirmed changes use 75%-late no-lookback boundaries. Each mapped region is then sorted by a fresh ordinary `simulated` decomposition and the runs are merged. The reference implementation copies mapped chunks.
- `simulated-phase-map-range`: E251 zero-copy execution plant. Mapping/gating/boundaries match `simulated-phase-map`, but mapped regions use a phase-map-local range-capable `simulated` engine on `std::span<T>` and avoid the copy-in/copy-back layer. Fresh `simulated` remains its default mapped-region backend.
- `simulated-phase-map-range-transition-partition`: E255 conservative backend-specialized descendant. It keeps E251 mapping/boundaries/execution intact, but when a stored ENTROPY region independently observes `(STRUCTURED, ENTROPY, ENTROPY)` at lead/mid/tail it bypasses fresh `simulated` and uses the existing high-entropy partition backend. The override is specialized-integral-only and otherwise falls back to E251 behavior.
- `simulated-phase-map-range-transition-partition-body-entropy`: E256 broader transition descendant. It preserves E255's stored-ENTROPY + STRUCTURED-lead + ENTROPY-midpoint mechanism but accepts any non-STRUCTURED tail, adding the independently validated `(STRUCTURED, ENTROPY, OTHER)` cohort. E255 remains the conservative control.

IDENTITY GUARDRAIL
- E229 replaced the former opaque numbered names with the descriptive short tags used below.
- These tags name architectural experiments, not merely whichever policy is fastest today. Preserve each variation's defining mechanism when benchmarking it: `simulated-inplace` keeps in-place flattening, `linked` stays linked after Patience reconstruction, `indexed` keeps index tails, `noalloc` remains the narrow bounded/fallback no-heap control, and `noalloc-low-run` remains the shared-tail + selective low-run-merge extension. A faster mechanism that removes one of those identities should be tested as propagation or a new variation rather than silently replacing it.

`physical`. ACTUAL PHYSICAL PILES
- Header: `include/jessesort/detail/pipelines/reference/physical.h`
- Representation: vector-backed physical patience piles with separate tail mirrors.
- Early routing: equality preserves the current game. A >=32-element monotone prefix is
  materialized directly. Otherwise the true first-64 probe routes Random-like inputs to
  direct bit-walk tail search, high-locality inputs to exact previous-pile hint + binary
  fallback, and ambiguous inputs to ordinary binary search (E046/E047/E056).
- E099 natural-run route: after an initial monotone prefix covering >=12.5% of n, later
  strict runs of length >=8 whose endpoints resolve to one physical pile are appended in
  bulk. This is retained and especially effective for OrganPipe/Rotated.
- E109 convergence baseline: `physical` now uses the same non-freezing Random merge predicate as `simulated` / `simulated-inplace`:
  the first-64 `earlyRandomLike` signal + common 3.5/4.25/5.0 pile-density thresholds + the
  <=96-byte trivially-copyable type gate. The older `physical`-only minority-game balance predicate is
  no longer part of the common starting policy.
- Merge kernel: E083 raw pointers + E085 bidirectional low/high-end branchless merge are retained.
- E111 removed selective PowerSort from the common `physical` / `simulated` / `simulated-inplace` policy after cross-variation revalidation.
- E115 consolidates `physical` / `simulated` / `simulated-inplace` onto one shared ends-based adj-pair post-flatten merge driver with
  adaptive ordered-boundary probing and the retained E045/E085 routing/kernel choices.

`simulated`. SIMULATED BLUEPRINT
- Header: `include/jessesort/detail/pipelines/reference/simulated.h`
- Representation: sentinel-free value tails plus packed 32-bit blueprint tags.
- Early insertion routing: >=32 monotone-prefix recognition; first-64 structure signal;
  one-time no-hint continuation for sustained Random-like structure; split exact-hint
  path for >=75% hint locality; ordinary search otherwise (E043/E065).
- E402 propagates E354's small-N three-pile exception to ordinary `simulated`: below 32k, a perfectly nondecreasing first-64 probe producing exactly three piles skips E183 valley rescue.
- E098 natural-run route: if the initial strict monotone prefix covers >=12.5% of n,
  later strict runs of length >=8 are endpoint-tested and blueprint-tagged in bulk when
  both endpoints map to the same pile. Exact blueprint/count/tail equality was required
  in A/B validation.
- Reconstruction: descending-game runs first, ascending-game runs second. E081 normalizes
  packed local tags to global run IDs before scatter. E086 keeps an explicit `step[]`
  table below 500k and derives direction from normalized run ID at >=500k, where the
  metadata-stream reduction wins.
- E109 convergence baseline replaces `simulated`'s old pre-E085 E053 density coefficients with the shared
  non-freezing 3.5/4.25/5.0 thresholds. `simulated` keeps its `earlyRandomLike` prerequisite, E045 gallop
  selector, and adaptive ordered-boundary probing. E101 previously removed the obsolete E063 per-pair
  imbalance fallback. E111 removed selective PowerSort; E115 moves `simulated` onto the same shared ends-based
  adj-pair post-flatten merge driver used by `physical`/`simulated-inplace`.
- High-entropy merge: E082 raw pointers + E085 bidirectional branchless merging retained.
  E092 widened the merge-only trivially-copyable type cutoff through 96 bytes; 128 bytes
  crossed to neutral/slower. Do not infer the same cutoff for insertion copies.
- E225 adds a `simulated`-champion-only bounded natural-run direct route before Patience insertion for n>=10k.
  It uses only comparator-defined structure: first 64 values nondecreasing, at least one inversion in
  a 9-point coarse sample, and a mutation-free preflight of <=128 natural runs. Accepted inputs normalize
  descending runs and merge the natural runs directly with `simulated`'s existing O(n) auxiliary merge storage;
  >128-run inputs fall back to the unchanged Patience path before mutation. This is a generic structural
  route, not a value-domain specialization. E227 later propagates this same public-wrapper route to the other
  allocating variations `physical` and `simulated-inplace` through `indexed`; `noalloc` / `noalloc-low-run` intentionally remain separate because E225 uses O(n) scratch.

`simulated-inplace`. SIMULATED BLUEPRINT + IN-PLACE PERMUTATION FLATTENING (NO FREEZE)
- Header: `include/jessesort/jessesort_simulated_probe-route_inplace-flatten-adj-adaptive-buffer.h`
- This is a NON-FREEZING controlled variation of `simulated`. E156 audited/reconciled the pair:
  `simulated-inplace` now shares `simulated`'s pre-Patience router, simulated insertion engine, random-merge selector,
  and post-flatten merge driver. Its intentional distinguishing mechanism is in-place
  permutation flattening instead of `simulated`'s streaming reconstruction. No large-N flatten cutoff
  is retained; poor `simulated-inplace` flatten scaling is allowed to remain visible as experimental evidence.
- This is a NON-FREEZING variation. Insertion shares `simulated` semantics, including E065 routing
  and retained E099 natural-run batching. Flattening converts blueprint tags to destinations
  and permutes the array in place with cycle swaps.
- E109 convergence baseline aligns `simulated-inplace` with `physical`/`simulated` on descending-game-first materialization and
  common 3.5/4.25/5.0 density thresholds. `simulated-inplace` still differs architecturally by performing the
  blueprint flattening as an in-place cycle permutation. E111 removes selective PowerSort; E115
  moves `simulated-inplace` onto the same shared ends-based adj-pair post-flatten merge driver as `physical`/`simulated` while
  retaining adaptive ordered-boundary probing, E045 gallop policy, E085 kernel, and <=96-byte type gate.
- E104 revisited E045 under the current E085/E093/E102 cost model with high trial counts.
  `simulated-inplace` now retains the same final-layout 5/7 gallop-trigger selector used by `simulated`: trigger 5
  only for non-branchless, non-PowerSort layouts with 12-128 runs and largest run <=1.25x
  the mean; trigger 7 remains the default.
- Retained as an experimental reconstruction/memory comparison, not the default `simulated` path.

`physical` / `simulated` / `simulated-inplace` NON-FREEZING CONSOLIDATION - RESOLVED THROUGH E115
- `physical`, `simulated`, and `simulated-inplace` represent the same intended non-freezing patience-pile decomposition; their primary
  architectural differences are physical versus simulated insertion storage and flattening mechanics.
- E109-E114 systematically re-tested historical policy drift across all three variations rather than
  preserving variation-specific assumptions. The retained common policy uses descending-first
  materialization, `earlyRandomLike`, common 3.5/4.25/5.0 density thresholds, adaptive ordered-boundary
  probing, and the retained E045/E085 merge routing/kernels.
- E111 removed selective PowerSort from `physical` / `simulated` / `simulated-inplace`.
- E115 consolidated all three onto the same shared ends-based adj-pair post-flatten merge driver.
  After flattening, do not assume `physical` / `simulated` / `simulated-inplace` need separate merge-driver logic unless a genuinely new
  architecture-specific mechanism provides canonical affected-variation evidence.
- The E109-E115 reconciliation series is CLOSED. Do not reopen insertion-shape parity, old PowerSort
  availability, historical boundary-probing/classifier/density exceptions, merge-driver parity, or tag
  numbering merely because older historical records mention them. A new attempt requires a materially
  new mechanism plus canonical acceptance for the variation(s) actually affected by that mechanism.

`frozen-single`. EARLY FREEZE + ONE LARGE DEFERRED OVERFLOW RUN
- Header: `include/jessesort/detail/pipelines/reference/frozen_single.h`
- E049 one-time first-64 hinted/no-hint routing remains outside the long insertion loop.
- Freeze policy: 50% is the decision point. `frozen-single` may extend ordinary pile creation to a
  strict next power-of-two cap only under its conservative selected policy; therefore
  50% is not always the exact physical freeze boundary.
- E099 natural-run batching is retained in a freeze-aware form that preserves the freeze
  transition and keeps the power-of-two extension interval scalar.
- Overflow: one contiguous deferred region sorted once with std::sort.
- Merge routing retains the post-insertion Random signal, E085 bidirectional branchless
  kernel, and E093 <=96-byte trivially-copyable merge cutoff. Selected PowerSort-style
  scheduling remains limited to its retained run-structure conditions.

`frozen-deferred`. EARLY FREEZE + DEFERRED-SORT 32-ELEMENT OVERFLOW BANDS
- Header: `include/jessesort/jessesort_simulated-frozen_probe-route_adj-adaptive-buffer_deferred-bands32.h`
- E049 one-time first-64 hinted/no-hint routing; 50% nominal freeze decision point with
  the retained broad selected power-of-two extension policy.
- E099 natural-run batching is retained with the STRONGER `frozen-deferred` / `frozen-live` gate: after the >=12.5%
  initial monotone prefix, the first continuation run must itself be >=8 before the route
  is enabled. This preserves OrganPipe gains while avoiding the broad-gate Rotated loss.
- Overflow: deferred 32-element bands. E090 uses a fixed specialized network for exact
  full 32-element bands; partial/conservative cases retain std::sort. The network was
  ~28.4% faster locally over 4,096,000 int-band sorts, with diluted whole-sort impact.
- Merge routing retains E085 bidirectional branchless merging and E093 <=96-byte
  trivially-copyable cutoff. `frozen-deferred` keeps its variation-specific dominant-endpoint merge
  deferral; global PowerSort and simple adaptive band shaping were rejected.

`frozen-live`. EARLY FREEZE + LIVE-SORTED 32-ELEMENT OVERFLOW BANDS
- Header: `include/jessesort/jessesort_simulated-frozen_probe-route_adj-timsort-adaptive-buffer_live-bands32.h`
- Shares `frozen-deferred`'s early-freeze base structure and nominal 50%/selected power-of-two extension.
- Uses the same stricter E099 first-continuation-run >=8 natural-run gate as `frozen-deferred`.
- E179 adds the retained STRICT post-insertion overflow router. Weak/fragmented structure keeps
  live-sorted 32-element bands; clearly ascending low-pile structure uses E175 ascending natural
  runs; clearly descending low-pile structure and high-pile long monotone structure use E176
  bidirectional natural runs with 32 as a minimum weak-structure granularity, not a cap.
- The router combines overflow fraction, final frozen pile density, and the first 32 overflow value
  transitions collected inline. Sampling is suppressed when freeze-time pile geometry already makes
  both natural tiers impossible (piles>64 and density>0.006).
- Merge routing retains E085 bidirectional branchless merging and E093 <=96-byte cutoff.
  Random-like inputs use branchless adjacent merging; non-random inputs preserve the retained
  TimSort-style/adjacent selector.

`avx2`. AVX2 EXPERIMENTAL SIMULATED VARIATION (`simulated`-DERIVED; NO GENERAL FREEZE)
- Header: `include/jessesort/jessesort_simulated-avx2_probe-route_adj-adaptive-buffer_capped-probe8x128.h`
- `simulated`-derived pipeline with a SIMD-oriented capped-probe front end. Default E076 behavior
  caps each game at eight piles during the first 128 values; excess would-be piles spill
  to a temporary probe-overflow run that is sorted and married into an existing pile.
- Exact-eight int32 pile-tail lookup uses one AVX2 compare. E105 adds a narrow repeated
  hint-centered one-register AVX2 continuation for 5k-20k, low-pile layouts (<=12 ordinary
  post-probe piles); broader/global and large-n windowing were rejected. Longer one-shot
  probes and sequential multi-register AVX2 scans remain closed.
- E099 natural-run batching is retained without changing capped-probe behavior on Random.
- E095 fixed the old public-path discrepancy: normalized reconstruction is now the default
  capped-probe reconstruction. E096 then removed its separate `step[]` stream entirely;
  reconstruction improved modestly while whole-sort effect was near neutral.
- E189 restores E162's retained raw non-overflow bulk-span reconstruction to the public `avx2` call after
  a later call-site omission left the flag at its default false. The original 75% adjacency gate is
  tightened to 95% for the E180C 12-row suite so Sorted+Noise(10%) remains neutral while long-run
  structured rows keep the large raw-span reconstruction win.
- E089 retained bidirectional branchless merging and E093 retained the <=96-byte
  trivially-copyable merge cutoff.
- `avx2`+ numbering remains reserved for SIMD-oriented experiments.

1.2B CURRENT IDENTITY GUARDRAILS
- Use layer-tag descriptors in all current documentation and benchmark selectors.
- Historical E001-E228 records may retain the names used at the time; `docs/E229_variation_tag_migration.md` is the provenance map.
- Preserve defining mechanisms when evolving a variation: do not silently turn `simulated-inplace`, `linked`, `indexed`, `noalloc`, or either phase-map plant into a different architecture merely because another path is faster.
- Shared historical policies such as E109 convergence remain documented in the experiment history; this section records current identities, not old convergence chronology.

1.3 CURRENT SHARED ENGINEERING DECISIONS
- Numeric sentinels are removed from maintained implementations.
- `physical`, `simulated`, `simulated-inplace`, the frozen families, `avx2`, `linked`, and the phase-map families require copyable + movable values in their current implementations because some
  metadata paths store values by copy. `indexed` uses index tails and a non-copyable reconstruction path, so `indexed` supports
  move-only, non-default-constructible values while still requiring move construction/assignment. `noalloc` and `noalloc-low-run` also
  support movable non-copyable values and, by identity, perform no heap allocation inside the sort.
- Comparators must provide a strict weak ordering. Default floating-point `<` is not supported for NaN-containing
  inputs; use an explicit NaN-aware strict-weak-order comparator, such as a total-order/NaN-last comparator.
- Equality preserves the current game in maintained pile-routing logic.
- Branchless high-entropy two-run merging is bidirectional across `physical` through `avx2` (E085-E089).
- The merge-only fast-path size gate is <=96 bytes for trivially copyable values across
  `physical` through `avx2` (E092/E093); this is not a universal element-size policy.
- Natural-run batching is retained across `physical` through `avx2` (E098/E099), with stricter activation in
  `frozen-deferred` / `frozen-live` as described above.
- `frozen-single`, `frozen-deferred`, and `frozen-live` use 50% as the nominal freeze decision point, with retained power-of-two
  extension policies able to move the actual freeze boundary later.
- Conventional even-sample median retained.
- Random%100 samples uniformly from [0,99]. Sorted+Noise uses 5% independent random
  replacements. Misleading jitter workload removed. BlockSorted handles a partial final
  block safely.

1.4 CURRENT BENCHMARK ARTIFACT POLICY
- `benchmarks/canonical/benchmark.cpp` is the canonical whole-sort benchmark and `make bench` is the routine 10k+100k entrypoint.
- Canonical suite runs write ephemeral raw state under gitignored `results/`; committed repository evidence is limited to compact representative summaries under `benchmarks/baselines/`. Historical raw timing trees are pruned after their decision-relevant conclusions are consolidated here.
- A 500-trial cell is atomic: partial active-cell output is never merged with a later process. If interrupted, discard that cell and rerun it from trial 0; completed cells remain committed. At 10k the ZIP checkpoint is refreshed after all 14 inputs for an algorithm; at 100k it is refreshed after every cell.
- Performance decisions use one algorithm/input cell per process under the warm-isolated methodology. `std::sort` is an independently timed control; correctness reference sorting occurs after the timed call. Close audit decisions use repeated independent process medians rather than same-process paired observations.
- Test only variations whose executable behavior changes; include an imported dependency/source variation only when its behavior is affected.
- Every retained performance change requires canonical affected-variation acceptance, CPU/compiler context, >1% regression investigation, propagation disposition, and a ZIP checkpoint.
- **Evidence-integrity hard gate (E364):** never combine raw timings, medians, or ratios from different benchmark cohorts in one result row. Every displayed ratio must be machine-recomputed from the raw values in that same cohort/artifact; if a second cohort is useful, show it separately and label it separately.
- A decision-eligible experiment artifact must preserve raw trial timings, deterministic seed/trial IDs, benchmark method/version, CPU/compiler context, source + executable hashes, and the exact derivation script. Summary-only or ratio-only CSVs are supporting evidence only and cannot by themselves justify RETAIN/REJECT.
- Experiment-specific harnesses must include reachability/negative-control checks when the candidate is conditional. If a candidate-inactive cell moves >1%, treat that cohort as contaminated for causal inference until repeated independent pinned-process rechecks resolve it; do not rationalize the delta as layout/noise while retaining from the same cohort.
- For small/medium effects or noisy hosts, use repeated independent pinned processes with alternating control/candidate process order and base decisions on the median across process medians. Same-process paired timing may diagnose causality but is not the sole retention evidence when cache/predictor carryover is possible.
- For deterministic varying-seed candidate/control workloads, join isolated-process raw trials by `(input,n,trial,seed)` and report **both** ratio-of-medians and median seed-paired ratio. If they disagree in direction around 1.0, the result is distribution-sensitive and is automatically audit-flagged; do not choose whichever estimator favors the candidate. `benchmarks/compare_seed_aligned.py` enforces seed identity and flags direction conflicts.
- **Layout/codegen guardrail (E371 correction):** compiler/layout/codegen differences are diagnostic triggers, not standalone rejection criteria. If a candidate changes unrelated emitted code, expand causal acceptance and recheck suspicious cells under the evidence-integrity rules; retain the candidate when validated performance/correctness/contract evidence shows a material win and no unacceptable regression. Withhold/reject only for measured adverse behavior, not byte-identity loss by itself.

1.5 E364 EVIDENCE-INTEGRITY INCIDENT / AUDIT HOLD
- A consolidated E363 table incorrectly placed raw E359/E362/std::sort timings from one cohort beside an E362/E359 ratio from another cohort. The row was internally inconsistent and is invalid. The underlying files were not fabricated; the synthesis was wrong. E363 decision claims are on audit hold until rebuilt from raw same-cohort artifacts.
- The first E364 rebuild also demonstrated that separate candidate/control binaries can move candidate-inactive rows materially through code-layout/process effects. A later identical-executable cohort still showed large inactive-row process drift, so a single process median is insufficient on the present host for this micro-change. E364 therefore requires repeated independent-process replication plus inactive-row sanity checks.
- Scope response: do not assume all historical experiments are invalid. Audit recent decision-bearing experiments by provenance class first; rerun only experiments whose retained/rejected conclusion depends on unverifiable summaries, mixed cohorts, the formerly unreliable benchmark script, or unresolved inactive-row movement.

===============================================================================
2. ACTIVE TODO / ROADMAP
===============================================================================

Priority order (2026-09-05)
1. **E691 speed convergence without losing generalization.** E691 is the forward maintained backbone; E661 remains the public/default speed reference. Recover performance through generic observable structure, never benchmark-family fingerprints or donor-name routing.
2. **E702 ghost-run integration / realization audit.** Do not add a new Sawtooth/BlockSorted route. E701 outer monotone regions are already realized in-place and sent directly to the final merge pool; audit the older dense-ghost path used inside the mature Patience engine, where detached ghost spans are still represented by blueprint tags and copied/reverse-copied into the reconstruction buffer before merge. Measure whether ghost spans can remain physically realized or otherwise bypass redundant reconstruction/movement while preserving mixed-input/phase-map behavior and generic comparator semantics. Focus on post-detachment handling, not new ghost admission thresholds.
3. **E703 generic phase-local sparse-disorder repair.** Revisit the Sorted+Noise5 signal without fixed 128-element blocks or named-noise routing. Let phase-map boundaries define arbitrary-sized regions/islands; test whether locally repairing only unresolved sparse-disorder regions and then merging realized runs can beat full Patience representation while preserving mixed/random-island behavior.
4. **E704 low realized-run-density direct merge.** When decomposition produces a small number of physically realized contiguous runs, test whether direct merge should replace further Patience/reconstruction work. Sweep generic run-density economics (candidate region roughly 0.5%-3% of n or an equivalent cost model), with no Rotated-specific assumptions. Require that runs are already realized; do not pay scatter/flatten cost merely to qualify.
5. **Run/phase geometry cluster.** Recover generic Sawtooth/BlockSorted/Rotated/MixedDirectionRuns speed only through transferable run/phase mechanisms; do not pursue the rejected Alternating two-lane specialization.
6. **Duplicate/cardinality structure.** Keep this only as a generic equivalence-density/cardinality research question; do not recover Random%25 by turning the route into a counting sort or by assuming a small fixed key universe.
7. **Boundary/block geometry.** Study Rotated, BlockSorted, RunMosaic/UnevenRunMosaic, SparseInversionPatches, OffsetRotationRamp, and OverlappingSortedBlocks for shared monotone-boundary/run-layout signals rather than independent routes.
8. **Noisy-sorted crossover.** E691 is poor on very low noise, approaches parity around the middle, and becomes stronger at higher noise. Find the causal local-disorder transition and make admission continuous rather than thresholding named Noise families.
9. **Reduce generic E691 overhead.** Random is only modestly slower than E661; after the large structural cliffs, reduce redundant probes, pressure bookkeeping, and eagerly built metadata.
10. **Cheaper pressured-game overflow.** Preserve E691's per-game T=9 safety semantics while deferring/reducing overflow normalization and merge work. Priority hypothesis: keep overflow virtual through the first merge level or consume it through simulated merge ownership rather than flattening first.
11. **Hard OOD anti-regression envelope.** Preserve E691's strong MonotoneBurstNoise, affine/bitonic, ChunkEntropyMixture, LocalizedAlternatingBursts, and BlockShuffle>=32 behavior while importing E661 speed mechanisms.
12. **Fallback failures versus std::sort.** RandomWalk, StickyRandomWalk, PeriodicPerturbed, BlockShuffle16/32, and low-noise SortedNoise remain important counterexamples. Determine whether the problem is classifier ownership, backend choice, or execution overhead.
13. **Production replacement bar.** Do not replace E661 until E691 eliminates current multi-x cliffs, avoids broad >10% established regressions (prefer <=5% on common paths), preserves its OOD win envelope, and remains independently benchmarkable against E661.
14. **Transferable retained donors only.** E607 narrow blueprints, compact/hierarchical scheduling, morph admission, E656 fusion evidence, and within-pile/cross-game value-density ideas may be revisited only by rebasing the mechanism onto E691 or current E661. Do not compare obsolete whole headers directly.
15. **Noalloc/Rust track.** `jessesort::sort_unstable_noalloc` is the strict bounded public contender; `jessesort::noalloc::sort_adaptive` is the performance-oriented noalloc path. Near-term work is tooling/comparison cleanup and eventual direct Rust/ipnsort validation, not more scratch-threshold fishing.

Deferred/high-level research backlog (not priority ordered)
- **Ghost-run continuation and direct ingestion.** E575 proved that detached dense monotone runs can be useful, but simple span/min-length threshold sweeps and relative admission guards are closed. Future work should change what happens *after* a run is detached: keep ghost ownership virtual, ingest a proven run directly into later merge ownership, or use ghost descriptors as structure sensors. Do not retry E571/E585 anchored placement + immediate `std::inplace_merge` as-is.
- **Deeper simulated blueprint merging / delayed materialization.** Continue the E649-E656 line beyond one local fusion: consume blueprint, ghost, or overflow ownership through one or more merge levels before physically flattening; exploit reconstructed pair/triple geometry and natural dependency closures inside an already-required reconstruction/merge pass. Avoid a separate ownership representation whose construction erases the movement savings (E651/E652).
- **Patience decomposition as a recurring structure sensor.** When cheap monotone/run evidence becomes ambiguous, consider sparse/galloped dual-Patience observations of pile count, game balance, concentration, and pressure rather than a one-shot classifier. Keep phase-boundary evidence separate from backend-choice evidence; E252-E257 showed that broad RouteState labels alone are too coarse for backend routing.
- **Freeze by structural economics rather than fixed input percentage.** Revisit early freezing using pile count, per-game pressure, power-of-two/balanced-merge geometry, or an estimated remaining-work model. Preserve the old idea of allowing overflow to help balance later merge levels. Do not reopen blind freeze-percentage or cap sweeps without a structural model.
- **Single-game / reverse-traversal / two-ended Patience research.** E467-E479 established this as a distinct experimental family. Future attempts need a stronger general discriminator or representation mechanism: exact/reliably mapped descending regions, reverse visitation without physical reversal, or two-ended/rainbow-style pile ownership. Do not promote simple single-game parity work merely for symmetry.
- **Streaming outer loop and proved natural-run consumption.** Explore consuming long comparator-proved monotone segments as units once pile/run ownership is known, then re-entering the outer classifier after each committed segment. Connect this with the active run/phase-geometry work instead of adding benchmark-family routes.
- **Layout-stable specialist composition.** Strong specialist mechanisms such as repeated-ascending materialization and exact low-run/rotation handling should be revisited only through stable wrappers/module boundaries or clearly isolated ownership. Avoid monolithic hot-header edits where unrelated codegen movement dominates the measurement.
- **Better in-place long-run merge primitive.** The noalloc/Rust line still has a primitive-level movement bottleneck on overlapping long runs; merge-order tuning alone did not solve it (E345). Reopen when there is a materially different in-place merge/rearrangement primitive, not another scheduler permutation.
- **High-entropy backend parity remains parked.** Random-path profiling historically put most remaining cost in partition + smallsort rather than Patience routing. Reopen only for a genuinely new partition/smallsort primitive, compiler/codegen opportunity, or direct Rust/ipnsort parity experiment; do not resume broad threshold micro-tuning.
- **Historical regression/provenance diagnostics remain available, not active optimization.** If a coherent same-method regression reappears, first build a retained-gain cancellation/ownership map and then use logarithmic checkpoint bisection under the current isolated runner. Do not replay old experiments linearly or treat old absolute timings as current baselines.

Execution discipline
- One mechanism at a time; use clean separate binaries for causal performance decisions.
- Canonical retain/reject default: 10k and 100k, 500 trials/cell, 2 warmups, deterministic seeds, pinned CPU when available; use 1000+ trials for close/high-value adjudication.
- Investigate >1% regressions with reversed process order and correctness validation before retention.
- Keep production/backbone code free of benchmark-family fingerprints; route only on generally observable structure/state.
- `results/` is ephemeral and gitignored. Preserve only compact summaries/provenance needed for current decisions; detailed tables belong in the chat/artifact that made the decision, not the clean repository.

===============================================================================
3. COMPACT EXPERIMENT HISTORY
===============================================================================

Experiments are intentionally kept at decision/mechanism level. Detailed raw timing cohorts and throwaway harnesses are not permanent repository artifacts unless an active line still depends on them.

- **E001 - Benchmark harness corrections** - RETAINED
  - Random%100 fixed to uniform [0,99]; signed modulo had produced 199 values.
- **E002 - Sentinel removal / index-tail prototype** - MIXED: sentinel removal retained; index tails rejected
  - Value tails remained faster. The index-tail attempt caused roughly a 60% random-input regression in the relevant prototype.
- **E003 - Static early-freeze policy** - PARTIAL: 50% baseline retained; production extension superseded
  - 50% improved routine-size random performance relative to 36.8% and became the static baseline used for subsequent early-freeze audits.
- **E004 - V1 actual-piles audit** - RETAINED
  - Sorted: about 86% faster.
- **E005 - V2 simulated-blueprint audit** - RETAINED
  - Random: 2.1% faster at 1k, 3.8% at 10k, 1.0% at 100k; 100k treated as neutral.
- **E007 - V5/shared early-freeze code cleanup** - RETAINED
  - Correctness equivalent to pre-cleanup audited code; non-monotone timing mostly within 2-3%, reverse materially faster.
- **E008 - Generic/API audit** - RETAINED
  - 1k: about 7.5% faster.
- **E009 - V6 live-overflow variation** - RETAINED
  - Random: about 1.0% slower.
- **E010 - V6 live band-size audit** - SUPERSEDED: size 32 restored later
  - Size 8: repeated 20-24% organ-pipe improvement versus size 32.
- **E011 - V4 single large overflow variation** - RETAINED
  - Initial 100k ratios versus std::sort:
- **E012 - V5 dominant-endpoint merge policy** - RETAINED only for V5
  - Repeatedly regressed 100k alternating by about 5-8%.
- **E013 - V6 smallest-adjacent greedy merge** - REJECTED
  - Random: +9.3%.
- **E014 - V1 two-of-three merge** - REJECTED
  - Random: +1.3%.
- **E015 - V2 two-of-three merge** - REJECTED
  - No repeatable material gain; block-sorted regressed 4.34-5.31% every run.
- **E016 - V3 in-place cycle flattening** - RETAINED
  - Random was ~13.8% slower while OrganPipe improved ~5.5-8.9%; V3 was retained as an architectural in-place-flatten experiment, not a universal speed win.
- **E017 - Routing/game collation** - REJECTED
  - Still slower at 1M, including random +6.2% and alternating +8.4% to +13.1%.
- **E018 - Equality-to-ascending one-comparison routing** - REJECTED
  - Random: -0.16%.
- **E019 - V2 pile-tail search policy** - RETAINED
  - std::lower_bound miss path: 46-76% slower on random/random%100 at 100k.
- **E020 - V6 TimSort-style merge scheduler** - RETAINED selectively
  - Random: +3.14%.
- **E021 - V2 PowerSort-style merge scheduler** - RETAINED selectively
  - At 100k, pure PowerSort was about 4.09% slower on Random but 7.04% faster on Sorted+Noise(5%), 4.58% faster on Sawtooth, and 4.57% faster on BlockSorted.
- **E022 - V4 PowerSort-style merge scheduler** - RETAINED selectively
  - At 100k, Random was +1.72%, Sorted+Noise(5%) -10.31%, Random%100 -1.57%, BlockSorted +2.36%, and the remaining routine workloads were within about +/-2%.
- **E023 - V3 PowerSort-style merge scheduler** - RETAINED selectively
  - Random -0.06%, Sorted+Noise(5%) -5.69%, Random%100 +0.02%.
- **E024 - V1 PowerSort** - REJECTED
  - Random regressed +4.56% to +5.01% across all three runs.
- **E025 - V5 PowerSort** - REJECTED
  - Random regressed +3.22% to +3.33% in every run.
- **E026 - V6 PowerSort** - REJECTED
  - Noise(5%) regressed +5.30% to +6.78% in every run.
- **E027 - Power-of-two freeze targeting** - RETAINED
  - At 10k, the retained policies preserved the structured gains; V5/V6 Random%100 did not regress materially in that run.
- **E028 - Random-input lineage audit** - RETAINED; restored live band size 32
  - Final random lineage medians (current / original; negative is faster): 10k 100k 1M V1 actual -0.319% -0.038% +0.467% V2 simulated -7.890% -10.167% -12.117% V6 live32 -0.969% -5.682% -4.968%
- **E029 - V2 descending-game-first reconstruction** - RETAINED
  - Outside Rotated, all regressions are below the routine 2-3% significance band; Alternating and BlockSorted retain clear gains.
- **E030 - Other-variation reconstruction-order audit** - MIXED
  - Retain descending-game-first in V1. It gives large, highly repeatable gains on Sorted+Noise and BlockSorted with no large regression; the only negative near the normal noise threshold is Alternating (~2.3%).
- **E031 - V1 adj-merge fast paths** - RETAINED
  - Random: 7201.613 -> 7205.510 us (0.05% slower)
- **E032 - V1 exponential galloping** - RETAINED
  - Random: 7698.847 -> 7714.423 us (0.20% slower)
- **E033 - V1 ordered-boundary elision** - RETAINED
  - Random: 7707.490 -> 7715.527 us (0.10% slower)
- **E034 - V1 asymmetric smaller-side merge** - REJECTED
  - Random: 7698.730 -> 8433.615 us (9.55% slower)
- **E035 - V2 exponential galloping** - RETAINED
  - Random: 5290.070 -> 5341.730 us (0.98% slower)
- **E036 - V2 Random router + branchless merge** - RETAINED
  - Force PowerSort on random: ~2.6% slower at 100k.
- **E037 - Cross-variation 64-element probe** - ARCHITECTURE EVIDENCE / selectively propagated
  - At 100k the early >=6/6 rule activated on 98% of full-range Random and 98% of Random%100 in the initial 50-seed sample; later pile density separated them (98% vs 0%). A separate 200-seed held-out check reproduced this: 198/200 Random prefixes qualified early and passed the late density rule, while 197/200 Random%100 prefixes qualified early but 0/200 passed the late density rule.
- **E038 - V3 Random router** - RETAINED
  - 300-trial process: Random 5993.120 -> 5168.022 us (13.77% faster).
- **E039 - V5 blueprint-derived Random merge router** - RETAINED
  - Seed 12000001: Random 5393.147 -> 4713.476 us (12.60% faster). Sorted+Noise 1142.946 -> 1146.441 us (0.31% slower).
- **E040 - V4/V6 blueprint-derived Random merge router** - RETAINED
  - V4 seed 43000033: Random 5783.413 -> 5044.724 us (12.77% faster). Sorted+Noise 1161.189 -> 1156.887 us (0.37% faster); other routine inputs remained near neutral at decision scale.
- **E041 - V1 final-density/game-balance Random merge router** - RETAINED
  - 300-trial seed 9100000: Random 7734.697 -> 6903.124 us (10.75% faster).
- **E043 - V2 two-stage early insertion routing** - RETAINED
  - 100k, 300 trials, seed set 1: Random: B -4.18%, C -4.62% vs A. Random%100: B +75.99% slower, C -11.20% faster vs A.
- **E044 - Branchless merge unroll** - RETAINED at width 4
  - Generic k-way tournament merge was ~60.7% slower; the useful retained result was the width-4 branchless two-run merge unroll.
- **E045 - Final-layout 5/7 gallop-trigger routing** - RETAINED
  - The prior global 5-vs-7 A/B had Sorted+Noise regressions of +0.40% to +0.70%; the routed candidate removed that regression.
- **E046 - V1 early insertion routing + V2 merge ports** - RETAINED
  - 100k Random improved -1.90% and -1.79% across two independent seeds.
- **E047 - V1 structured-side exact-hint routing** - RETAINED
  - 100k, 300 trials seed 9100000 after the >=4-pile guard: Random 4767.26 -> 4769.90 us ~0.06% slower Sorted+Noise(5%) 1735.15 -> 1488.13 us 14.24% faster Random%100 1605.96 -> 1608.78 us ~0.18% slower Alternating 493.36 -> 493.33 us neutral all other routine families approximately neutral.
- **E048 - V2 insertion-route propagation** - MIXED: V3 retained; direct V5/V6 port rejected
  - Same-binary 100-trial A/B at 100k: seed 9100000: Random -3.02%, Random%100 -5.81%. seed 123456789: Random -3.06%, Random%100 -7.35%. Noise5/Saw were approximately neutral in the larger confirmation; the earlier 30-trial sweep also showed no systematic structured regression.
- **E049 - Early-freeze out-of-loop hint/no-hint routing** - RETAINED
  - RETAIN in V4-V6 under the E050 canonical numbering. With canonical inputs the result is stronger and cleaner than the invalid first E049 table: Random and Random%100 improve substantially, while canonical structured workloads remain near baseline.
- **E052 - Duplicate saturation / merge-threshold audit** - RETAINED signal; threshold later size-aware
  - Direct threshold-5 versus threshold-7 A/B across three seeds at 100k: K=1000: threshold 7 ~13.2% faster K=1500: ~6.8% faster K=2000: ~4.0% faster K=2500: ~1.7% faster K=3000: ~1.5% faster K=5000: neutral (~0.1% faster) full Random: essentially neutral (~0.4% slower mean, timing noise across seeds)
- **E053 - Size-aware final pile-density routing** - RETAINED
  - n=10k: density ~5.9 was near parity; >=6.5 clearly favored branchless. Full Random density ~7.15 and branchless was ~11% faster.
- **E054 - Remove early Random no-hint route** - SUPERSEDED/FALSIFIED by E065
  - Full Random: no-hint about 9.5% slower than hinted/inline.
- **E055 - Broad natural-run batching** - REJECTED broadly; mechanism revived narrowly in E098
  - At n=100k over 30 seeds, profiling strict source runs >=4 elements found that compatible whole-run-to-one-pile assignments are essentially nonexistent for Random and Random%100, but materially present on structured inputs. Among eligible run elements, compatibility was about 47.6% for Sorted+Noise(5%), 39.8% for BlockSorted, ~4.9% for Sawtooth, and ~100% for OrganPipe/Rotated.
- **E056 - V1 hint-miss scalar refinements** - REJECTED
  - The adjacent candidate initially appeared promising on Sorted+Noise(5%): ~5.1% and ~4.6% faster on the first two seeds.
- **E057 - V7 exact-eight AVX2 pile search** - RETAINED as experimental variation
  - Isolated lookup sweep compared scalar bit-walk to sequential AVX2 scans at pile counts 4..256. Exactly eight tails was the only clear SIMD win: about 0.75-0.77x scalar lookup time (~23-25% faster). Four tails was slower, and 12+ tails was substantially slower because scanning multiple vectors loses to logarithmic scalar search.
- **E058 - Probe-gated AVX2 small-pile continuation** - REJECTED
  - A final probe-only isolation (SIMD permitted during the first 64 values and unconditionally disabled afterward) averaged roughly: Random 0.9956 (~0.4% faster), Random%100 0.9988, Sorted+Noise5 0.9955, Sawtooth 0.9996, BlockSorted 0.9988, OrganPipe 1.0038, Rotated 0.9981. The Random result was not stable enough across seeds (one ~1.4% win, other seeds near parity/slightly slower) to justify a production route.
- **E059 - V5 fixed band-size response surface** - CHARACTERIZED; size 32 unchanged
  - Ratios below are candidate / fixed-32 baseline; lower is faster.
- **E060 - V5 simple adaptive/nonuniform shaping** - REJECTED; tree-aware idea remains open
  - Global metadata-derived sizing is not a general win. At 100k, run-count and median-derived sizes were roughly neutral on Random/Random%100 but regressed: Sawtooth ~10-11%, BlockSorted ~6-11%, Rotated ~8-14% across the two seeds.
- **E061 - V2 per-pair merge routing** - PROMISING; finalized in E063
  - A narrower transition-band version therefore applied pair routing only for 20k<n<500k and normalized final pile density <=7.3, above the existing ~6.75 branchless entry threshold. K3000 again appeared ~4.4-5.3% faster and K5000 ~2.2-2.6% faster across the measured seeds.
- **E062 - V5 power-of-two run shaping / scheduler audit** - REJECTED
  - Sorted+Noise and Sawtooth were typically ~0.5-1.8% slower.
- **E063 - V2 1.5x transition-band pair routing** - SUPERSEDED/RETIRED by E101
  - K3000: 0.9191, 0.9163, 0.9318; mean 0.9224 (~7.8% faster).
- **E065 - Restore early Random no-hint insertion** - RETAINED for V2/V3/V7
  - Restoring the early Random no-hint continuation recovered substantial Random/Random%100 insertion performance in V2/V3/V7.
- **E066 - Benchmark methodology hardening** - SUPERSEDED in cadence/seed design by E077/E100
- **E070 - Isolated checkpointed benchmark artifacts** - RETAINED infrastructure
- **E072 - include/src layout + Markdown benchmark output** - RETAINED
- **E073 - Four-size/100-trial benchmark sweep** - SUPERSEDED by E077 trial policy
  - `benchmark_results.md` is now variation-centric: one table per V1-V7/std::sort, input families as rows, and 1k/10k/100k/1m as columns. Each completed cell contains the median paired ratio to std::sort and median time in microseconds; unfinished cells say `pending`.
- **E074 - V7 capped 64-value SIMD probe** - NEUTRAL; experimental path only
  - Random%100 cap-8: 55/100 probes; 126 total spill values; max spill 6.
- **E075 - V7 128/256 capped-probe sweep** - LONGER ONE-SHOT PROBES CLOSED
  - Median paired timing ratios: 8x128 / baseline V7 = 1.003246 (~0.32% slower) 16x128 / baseline V7 = 0.999240 (effectively neutral; cap overflow almost absent) 8x256 / baseline V7 = 1.179726 (~18.0% slower) 16x256 / baseline V7 = 1.000473 (effectively neutral) baseline V7 / std = 0.905007
- **E076 - Default V7 8x128 capped probe** - RETAINED as SIMD demonstration, not speed claim
  - 8x128 / uncapped V7 median paired ratio: 1.003246 (~0.32% slower).
- **E077 - Unique deterministic input per trial** - RETAINED current benchmark methodology
- **E078 - Resumable benchmark execution** - RETAINED
  - Tested: make long canonical benchmark runs survive execution/process timeouts without restarting completed trial work.
- **E079 - V2 global cursor/step reconstruction** - RETAINED
  - Re-running the preserved E073 code on the current host reached 30/100 Random trials before timeout and already had a V2/std median paired ratio of ~1.0385. This agrees with E078/current local observations and indicates the older ~0.93 result was strongly affected by execution/build/host conditions rather than a later V2 algorithm regression.
- **E080 - Blueprint normalization initial small-sample test** - SUPERSEDED by E081
  - 100k, 60 trials: median candidate/baseline per-trial ratio-of-ratios = 0.9966 (~0.34% faster). Candidate V2/std median 0.8778 vs baseline 0.8786 on this host run.
- **E081 - V2 normalized blueprint reconstruction retest** - RETAINED
  - 100k, 500 paired Random trials: candidate/baseline median = 0.977205 baseline/std median = 0.890523 candidate/std median = 0.876217 => candidate about 2.28% faster end-to-end than E079 baseline.
- **E082 - V2 raw-pointer branchless merge kernel** - RETAINED
  - The retained raw-pointer branchless merge improved V2 end-to-end ~9.0% at 100k and ~8.1% at 1m versus E081; broader 4-run fusion was ~54-57% slower and rejected.
- **E083 - E081/E082 cross-variation propagation** - PARTIAL; later reconciled/fixed by E084/E095
- **E085 - V2 bidirectional branchless merge** - RETAINED; propagation completed E087-E089
  - Bidirectional low/high-end merging cut the targeted merge phase ~42-46% and whole V2 Random ~29-33% versus E082.
- **E086 - V2 large-n reconstruction step[] elimination** - RETAINED at >=500k
  - Removing reconstruction step[] was bad at 10k/100k (1.55x/1.26x) but crossed over at >=500k: ~3.2% faster at 500k, ~8.2% at 750k, ~13.1% at 1m.
- **E087 - V3 bidirectional merge propagation** - RETAINED
  - V3 propagation of E085: ~26.9% faster at 10k, ~27.7% at 100k, ~18.9% at 1m.
- **E088 - V4/V5 bidirectional merge propagation** - RETAINED
  - E085 propagation: V4 ~24.5/25.8/27.7% faster and V5 ~27.6/32.2/36.0% faster at 10k/100k/1m Random.
- **E089 - V6/V1/V7 bidirectional merge propagation** - RETAINED; family propagation complete
  - E085 propagation: V6 ~28.6/32.9/34.2%, V1 ~24.2/28.3/16.9%, V7 ~25.8/29.9% with a later 1m recheck ~23.9% faster.
- **E090 - V5 fixed 32-element smallsort network** - RETAINED for full bands
  - The 32-element V5 band network was ~28.4% faster than std::sort locally; whole V5 effect was ~2.9% faster at 10k, neutral at 100k, ~1.6% faster at 1m.
- **E091 - V2 4-ary instruction-parallel pile search** - REJECTED
  - 10k: 5,000 trials, 2.62902x (~162.9% slower).
- **E092 - V2 type-aware branchless merge cutoff <=96B** - RETAINED
  - Branchless bidirectional merge remained profitable through 96-byte trivially-copyable values (~15% merge-only win at 96B); 128B crossed to ~8% slower.
- **E093 - E092 cutoff propagation to V1/V3/V4/V5/V6/V7** - RETAINED across family
  - At 100k, propagation remained strong at 32B (~30-42% faster across V1/V3-V7) and still positive at 96B (~7-12%).
- **E094 - Hot/cold dedicated V2 merge continuation** - REJECTED / neutral
- **E095 - V7 normalized reconstruction public-path wiring** - RETAINED; discrepancy resolved
  - 10k: 0.356354 (~64.4% faster reconstruction)
- **E096 - V7 reconstruction step[] elimination** - RETAINED; no size threshold needed
  - Reconstruction-only median candidate/baseline ratios: n=10k: 0.994 (5000 trials; ~0.6% faster) n=100k: 0.966 (2000 trials; ~3.4% faster) n=200k: 0.960 (1200 trials; ~4.0% faster) n=300k: 0.954 (800 trials; ~4.6% faster) n=500k: 0.968 (500 trials; ~3.2% faster) n=750k: 0.9425 (350 trials; ~5.8% faster) n=1m: 0.940 (250 trials; ~6.0% faster)
- **E097 - V5 frozen-tail Eytzinger search** - REJECTED
  - Results (candidate / baseline median paired ratio): n=10k, 1000 trials: insertion 1.3773 (~37.7% slower) full sort 1.1635 (~16.4% slower) n=100k, 600 trials: insertion 0.9980 (effectively neutral) full sort 0.9971 (effectively neutral) n=1m, 120 trials: insertion 1.7233 (~72.3% slower) full sort 1.2353 (~23.5% slower)
- **E098 - Narrow gated V2 natural-run batching** - RETAINED
  - At 1m, narrow natural-run batching improved OrganPipe insertion ~42.8% / whole sort ~8.3%, and Rotated insertion ~33.8% / whole sort ~5.8%.
- **E099 - E098 natural-run propagation to V1/V3/V4/V5/V6/V7** - RETAINED; stricter V5/V6 gate
  - V3-V7 generally show real local improvements even where whole-sort movement is only a few percent. At 1m, OrganPipe insertion improvements are typically ~7-10%; Rotated improvements are smaller and variation-dependent.
- **E100 - Rotated split distribution + canonical A/B seeds** - RETAIN generator; seed policy centralized
- **E101 - Revalidate E063 after E085** - RETIRED E063; no propagation
  - K3000, 100k: 660 qualifying trials; E063/current median ratio ~1.060 (~6.0% slower merge phase).
- **E102 - V3 final-density routing recalibration** - RETAINED V3-specific 4/4.25/5 rule after high-trial recheck
  - A smaller set of fork-isolated whole-sort tests initially appeared to show regressions from those lower thresholds (roughly 5% at 10k K200 and 15% at 100k K600), so the first E102 decision conservatively retained 6.0 / 6.5 / 7.0.
- **E103 - Testing-process/revisit taxonomy** - RETAINED; rejection classes + high-trial revisit roadmap
  - E097 mutable Eytzinger search: target insertion up to ~72% slower and whole sort ~24% slower.
- **E104 - V3 E045 gallop-trigger high-trial revalidation** - RETAINED in V3; propagation audited
  - 100k Sawtooth (21 balanced runs, selector active, no PowerSort/branchless): phase: 3,000 pairs, trigger5/trigger7 median 0.9710 (~2.9% faster) full: 1,000 fork-isolated pairs, median 0.9840 (~1.6% faster)
- **E105 - V7 repeated one-register AVX2 continuation** - RETAINED narrowly at 5k-20k low-pile layouts
  - Exact-eight AVX2 lookup was ~23-25% faster locally, but only a narrow V7 5k-20k low-pile integration remained worthwhile; broad/large-N routing was rejected.
- **E106 - V5 data-aware first overflow-boundary shaping** - REJECTED; no production change
  - 4,000 reconstruction+merge phase pairs: 0.99667 (~0.33% faster locally);
- **E107 - V1 merge-density recalibration** - RETAINED V1-specific 3.5/4.25/5 rule
  - K150, density ~3.72: 0.8226 (~17.7% faster);
- **E109 - Common V1/V2/V3 convergence baseline** - RETAINED common policy baseline; regressions logged for retuning
- **E110 - V1/V2/V3 MATERIALIZED GAME/RUN ORDER REVALIDATION** - REJECTED AS COMMON CHANGE; E109 DESCENDING-FIRST BASELINE RETAINED
  - Deltas below are candidate ascending-first median paired ratio to std::sort divided by the E109 descending-first baseline median paired ratio; negative is faster with ascending-first.
- **E111 - V1/V2/V3 SELECTIVE POWERSORT REVALIDATION** - RETAINED REMOVAL; ADJACENT-PAIR SCHEDULING IS THE COMMON V1/V2/V3 BASELINE
  - Targeted paired guardrails used Sorted+Noise(5%) and OrganPipe at 500k (60 trials) and 1m (60 trials). Negative deltas below mean PowerSort OFF was faster.
- **E112 - V1/V2/V3 ORDERED-BOUNDARY PROBING REVALIDATION** - REJECT ALWAYS-ON; RETAIN ADAPTIVE PROBING AS COMMON V1/V2/V3 BASELINE
  - V1: Alternating -13.20%, Random +1.29%, Random%100 +4.41%, Sorted+Noise +17.18%; most other structured workloads were within about 1%.
- **E113 - V1/V2/V3 RANDOM-ROUTE CLASSIFIER / GAME-BALANCE REVALIDATION** - REJECT FINAL-BALANCE REPLACEMENT AND COMBINED GUARD; RETAIN EARLYRANDOMLIKE
  - E109 replaced V1's historical final >=25% minority-game balance prerequisite with the same first-64 `earlyRandomLike` prerequisite used by V2/V3. Was the old V1 final game-balance signal actually a generally useful classifier that should be restored or propagated?
- **E114 - V1/V2/V3 common non-freezing density threshold revalidation     RETAINED CURRENT COMMON POLICY** - RETAINED CURRENT COMMON POLICY
  - 10k: mostly near-neutral. No broad gain from the historical-high policy. The main isolated signal was V3 Rotated, where the midpoint was about 7.3% faster.
- **E115 - V1/V2/V3 POST-FLATTEN MERGE-DRIVER CONSOLIDATION** - RETAIN V1 ENDS-BASED DRIVER AS THE SINGLE COMMON V1/V2/V3 DRIVER
  - Consolidating on the ends-based driver reduced aggregate V1/V2/V3 timing ~2.3%; V1 structural gains included Sawtooth ~15.8%, BlockSorted ~10.6%, OrganPipe ~20.1%, Rotated ~39.3%.
- **E116 - IPNSORT SCORECARD PHASE PROFILING** - RETAIN PROFILER / DIAGNOSTIC
  - Profiling showed 100k d20 was ~72% insertion-bound; Random/Zipf were roughly split between insertion and merge; p5 spread cost across insertion/reconstruction/merge.
- **E117 - ADJACENT-EQUALITY INSERTION REUSE (V2/V3)** - RETAIN
  - Adjacent-equality reuse improved p5 ~7-9% and d20 ~3-4% in V2/V3.
- **E118 - ROUTED ADJACENT-EQUALITY REUSE (V1)** - RETAIN
  - Routed V1 adj-equality reuse improved p5 ~21.6% at 10k / ~26.7% at 100k and d20 ~7.3% at 100k while keeping Random flat.
- **E119 - MODERATE-CARDINALITY VALUE->PILE CACHE (V2/V3)** - RETAIN
  - V2 d20 improved ~34.2% at 10k / ~33.7% at 100k; V3 ~22.8% / ~19.4%.
- **E120 - MODERATE-CARDINALITY VALUE->PILE CACHE (V1)** - RETAIN
  - V1 d20 improved ~11.8% at 10k / 19.8% at 100k with Random essentially flat.
- **E121 - BULK SAME-TAG RECONSTRUCTION (V2)** - RETAIN
  - Bulk same-tag reconstruction improved V2 p5 ~9.4% at 10k / ~8.1% at 100k and s95 ~1.7-2.1%.
- **E122 - EXPLICIT EQUAL-SPAN MERGE CONSUMPTION** - REJECT
  - Explicit equal-span merge handling regressed p5 ~4.6% at 10k / ~5.6% at 100k because existing galloping already exploited those spans.
- **E123 - FIXED LOW-CARDINALITY DIRECT BACKEND (V2)** - RETAIN
  - Low-cardinality direct backend improved V2 d20 ~81% at 10k / ~84.4% at 100k; canonical Random%100 improved ~91%.
- **E124 - ONE-WAY NATURAL-PREFIX SCAN (SIMULATED PATH)** - RETAIN
  - One-way prefix scanning improved ascending ~33%, descending ~45-58%, with smaller Rotated/long-prefix gains.
- **E125 - DOMINANT-VALUE EXTRACT / RESIDUAL SORT / SPLICE (V2)** - SUPERSEDED BY E129
  - Dominant-value extraction initially improved p5 ~49% at 10k / ~63% at 100k, but was later superseded by E129.
- **E126 - MULTI-HEAVY ZIPF EXTRACTION** - REJECT
  - Multi-heavy Zipf extraction was ~43% slower at 10k and only ~2.4% faster at 100k; extra full-array passes did not amortize.
- **E127 - BROAD ZIPF VALUE->PILE CACHE** - REJECT
  - Broader Zipf value->pile caching regressed Zipf ~6.1% at 10k / ~3.8% at 100k.
- **E128 - ROUTED HIGH-ENTROPY EQUALITY-AWARE PARTITION BACKEND (V2)** - RETAIN
  - High-entropy backend improved V2 Random ~56.1% at 10k / ~54.6% at 100k and Zipf ~65.2% / ~68.7%.
- **E129 - DOMINANT-VALUE ROUTE TO E128 BACKEND (V2)** - RETAIN; SUPERSEDES E125 BACKEND
  - Routing dominant-value p5 to the equality-aware partition backend improved another ~45.9% at 10k / ~47.7% at 100k over E125.
- **E130 - TAIL-RECURSION CONVERSION IN E128** - REJECT
  - Tail-recursion conversion regressed Random ~3.2%; rejected.
- **E131 - RECOVER E128 ZIPF ROUTER THRESHOLD** - RETAIN; RECOVERY CORRECTION
  - Restoring the 44/64 high-entropy cutoff recovered the lost Zipf improvement (~65% at 10k) while keeping d20 separated.
- **E132 - RECOVER INTEGER TWO-HALF / BIDIRECTIONAL SMALLSORT IN E128** - RETAIN; RECOVERY CORRECTION
  - Recovered two-half/bidirectional smallsort improved Random ~3.55% at 10k / ~3.19% at 100k, with smaller p5/Zipf gains.
- **E133 - PROPAGATE SPECIALIZED PRE-PATIENCE BACKENDS TO V3** - RETAIN
  - V3 specialized-route propagation improved target workloads roughly 58-94%, while native structured paths remained V3.
- **E134 - PROPAGATE SPECIALIZED PRE-PATIENCE BACKENDS TO V1** - RETAIN
  - V1 specialized-route propagation improved Random/Zipf/d20/p5 roughly 59-92%.
- **E135 - PROPAGATE SPECIALIZED PRE-PATIENCE ROUTING TO V4-V7** - RETAIN
  - V4-V7 propagation improved Random ~46-56% and Random%100 ~92-93% at 10k; 100k retained similarly large target gains with structured paths neutral.
- **E136 - PERMANENT HYBRID VS FORCED-PATIENCE RESEARCH HARNESS** - RETAIN (research instrumentation only)
  - Sawtooth, BlockSorted, OrganPipe, Rotated and monotonic inputs are essentially native-Patience workloads; Forced Patience is within ~0-1.5% of Hybrid and massively faster than direct partition.
- **E137 - FORCED-PATIENCE V2 PHASE + GEOMETRY BASELINE** - RETAIN (research baseline)
  - Forced-Patience 100k baseline: Random 1215us insertion + 1422us merge (~2811us total); Random%100 1040us insertion (~1396us total); Sorted+Noise ~955us total.
- **E138 - ONLINE RECONSTRUCTION + FIRST-MERGE FUSION** - REJECT
  - Online reconstruction->merge fusion was ~53% slower even with fixed buffers; per-element queue bookkeeping overwhelmed one saved pass.
- **E139 - HIGH-CARDINALITY RANDOM SINGLE-GAME PATIENCE STUDY** - POSITIVE RESEARCH RESULT; NOT PRODUCTION DEFAULT
  - Single-game high-cardinality Random reduced ~872 runs to ~621 at 100k and improved Forced-Patience whole sort ~5%; kept as research evidence, not production policy.
- **E140 - TWO-GAME INTERLEAVED ILP INSERTION** - REJECT
  - Interleaved two-game searches preserved exact geometry but were ~49% slower at 10k / ~59% slower at 100k.
- **E141 - FREEZE GEOMETRY + RECURSIVE PATIENCE OVERFLOW STUDY** - DEFER / NOT RETAINED GLOBALLY
  - ~35% freeze reaches ~511 top-level runs, but creates ~28.6k overflow elements and is slower overall.
- **E142 - COHERENT VALUE->PILE CACHE FOR PATIENCE INSERTION** - RETAIN (V2/V3 core mechanism)
  - Coherent value->pile caching improved d20 ~32-33% and Random%100 ~8.7% at 10k / ~33.8% at 100k in V2/V3.
- **E143 - PROPAGATE E142 COHERENT CACHE TO V1 AND V4-V7** - RETAIN with variation-specific gates
  - Propagation retained variation-specific cache gates; duplicate-heavy 100k improvements remained substantial across the family while structured guardrails stayed neutral after gating.
- **E144 - NUMERICAL INTERPOLATION-GUIDED PILE SEARCH** - REJECT
  - Interpolation-guided pile search was ~82% slower at 10k / ~88% slower at 100k insertion despite decent index prediction.
- **E145 - V1 MONOTONE / EARLY-PROBE HANDOFF CORRECTIONS** - RETAIN
  - V1 handoff/scanner fixes removed artificial singleton topology; the one-direction scanner made Reverse dramatically faster while Random stayed neutral.
- **E146 - PROPAGATE E145 HANDOFF/SCANNER SEMANTICS TO V2-V7** - RETAIN
- **E147 - RESTORE NON-DEFAULT / CUSTOM-COMPARATOR GENERICITY AFTER E143** - RETAIN (correctness/API repair; numeric fast path unchanged)
- **E148 - POST-PREFIX LOCALITY / STRUCTURE-PROBE HANDOFF AUDIT** - PARTIAL RETAIN (V2/V7); V1 candidate REJECTED
  - V7 post-prefix locality sampling improved Sawtooth ~6% and BlockSorted ~11-14%; V2 long-prefix index fix improved a 5%-sorted-prefix + 95%-Random case ~34% at 10k / ~37% at 100k.
- **E149 - SAWTOOTH OVERLAPPING-RUN MERGE SPECIALIZATION** - REJECT
  - Sawtooth <=32-run tournament merge was ~5x slower; identical-run memcmp fast path was neutral/slightly slower.
- **E150 - FOUR-VALUE SNAPSHOT PILE-SEARCH BATCHING WITH CONFLICT REPAIR** - REJECT
  - Four-value snapshot/repair batching preserved exact geometry but slowed Random insertion ~38% at 10k / ~30% at 100k.
- **E151 - BLOCKED PILE-TAIL SEARCH + AVX2 LOCAL SCAN** - REJECT
  - Blocked tails + AVX2 local scan slowed Random insertion ~10% at 10k / ~8% at 100k.
- **E152 - FIRST-LEVEL TINY-RUN WEIGHTED PROFILE** - PROFILE / DEPRIORITIZE ITEM 8
  - At 100k Random, <=32-element first-level pairs were only ~3.8% of pairs and ~0.3% of elements; tiny-run kernels deprioritized.
- **E153 - V4 STRUCTURE-AWARE RECURSIVE PATIENCE OVERFLOW** - RETAIN (V4)
  - V4 structure-aware recursive overflow improved 100k Sawtooth ~23% and BlockSorted ~11%; 1m gains ~21%/~18%, while the gate rejects Alternating.
- **E154 - V5/V6 STRUCTURE-AWARE RECURSIVE OVERFLOW PROPAGATION** - RETAIN V5 / REJECT V6
  - V5 retained a narrower recursive-overflow route: Sawtooth/BlockSorted ~8.5% faster at 100k and ~5.7%/~8.1% at 1m; analogous V6 treatment regressed and was rejected.
- **E155 - WHOLE-ARRAY TINY PRIMITIVE SMALLSORT** - RETAINED for N <= 32 on trivially-copyable values with the default std::less<T> comparator.
  - For random u64 N<=32, the shared tiny path reduced current-V2 cost to roughly 0.21-0.28x at N=8-32; at N=32 it was ~0.66-0.74x std::sort.
- **E156 - V2/V3 PROPAGATION AUDIT + CONTROLLED-VARIATION RECONCILIATION** - RETAIN AUDIT/REFACTOR; REJECT V3 FLATTEN CUTOFF
  - Audit found V2/V3 native policy semantically aligned but duplicated router/merge-selector code posed future drift risk; those policies now share helpers. The provisional V3 flatten cutoff was rejected and removed.
- **E157 - V2/V3 RECONSTRUCTION-vs-FLATTEN SWEEP** - MEASUREMENT ONLY; NO PRODUCTION CHANGE.
  - V3 in-place flatten was slower than V2 reconstruction on every tested shape, with the gap growing sharply at large N; preserve V3 as an intentional experimental artifact.
- **E158 - BIASED DUAL-GAME ASSIGNMENT / PILE-GEOMETRY THROTTLING** - REJECT; geometry signal retained.
  - starving the secondary game removed ~17-21% of piles, but deeper dominant-game searches erased the gain and could trigger worse merge routing.
- **E159 - OUTER-QUARTER FIT-QUALITY DUAL-GAME OVERRIDE** - REJECT.
  - opposite-game rechecks almost never changed geometry and made insertion ~72-82% slower.
- **E160 - V2 ON-THE-FLY BLUEPRINT TAG DECODE** - INCONCLUSIVE / SUPERSEDED BY E161.
  - structured bulk spans benefited, but source/layout sensitivity and Alternating regressions made the first integration untrustworthy.
- **E161 - LAYOUT-CONTROLLED V2 BULK TAG DECODE** - RETAIN.
  - revalidated bulk-span decode under controlled layout and retained it in V2 reconstruction while preserving fallback for non-bulk layouts.
- **E162 - E161 PROPAGATION AUDIT + V7 RAW BULK-SPAN RECONSTRUCTION** - RETAIN V7 PROPAGATION; other variations resolved intentionally.
  - restored/retuned V7's raw bulk-span reconstruction; V1/V3/V4-V6 either use different representations or already-equivalent logic.
- **E163 - V4 DIRECT-TO-RECURSIVE-OVERFLOW GATHER** - REJECT; phase profile retained.
  - bypassing the normal gather did not pay; V4 overflow profiling instead identified where later work should focus.
- **E164 - HIGH-ENTROPY ADAPTIVE PIVOT SELECTION** - RETAIN for >=8192 high-entropy partitions; dominant-value route exempt.
  - adaptive sampling improved the general partition backend enough to retain without disturbing duplicate-specialized routing.
- **E165 - IPNSORT-STYLE CYCLIC PARTITION KERNEL PARITY** - PARTIAL RETAIN.
  - cyclic partitioning was retained for eligible 8-byte integrals but rejected for int where it did not beat the existing kernel.
- **E166 - PRIMITIVE 18-32 SMALLSORT PARITY** - RETAIN.
  - specialized primitive smallsorts improved partition leaves in the 18-32 range.
- **E167 - PARTITION CONTROL FLOW: RECURSE-LEFT / ITERATE-RIGHT** - RETAIN for the normal high-entropy route; dominant-value route exempt.
  - asymmetric recursion/iteration reduced control-flow overhead without helping the duplicate-specialized path.
- **E168 - ANCESTOR/EQUALITY PARITY AUDIT** - REJECT.
  - ipnsort-style ancestor/equality handling did not beat Jessesort's existing equality shortcut.
- **E169 - OPPOSITE-GAME NEW-PILE RESCUE** - REJECT.
  - rescue attempts produced essentially no useful pile-geometry change.
- **E170 - V4/V5/V6 FREEZE-PERCENTAGE SWEEP** - MEASUREMENT ONLY.
  - no tested alternative displaced the 50% production freeze point.
- **E171 - SAMPLED DUAL-GAME BLOCK ROUTER** - REJECT.
  - extra sampling/search cost raised insertion time while leaving downstream pile geometry effectively unchanged.
- **E172 - LOW-MEMORY V3 BOUNDED MERGE BUFFER** - REJECT.
  - memory savings were real but the bounded merge path was too slow.
- **E173 - INSERTION-TIME IN-PLACE FLATTEN DESTINATION AVAILABILITY** - CLOSED / NO CHANGE.
  - final permutation destinations are not certifiable online during ordinary insertion, so the proposed incremental flatten is not directly available.
- **E174 - FREEZE-FAMILY PHASE REPROFILE** - DIAGNOSTIC.
  - after prior merge work, insertion and reconstruction-not final merge-became the dominant V4-V6 costs.
- **E175 - V6 ADAPTIVE NATURAL LIVE OVERFLOW RUNS** - RETAIN.
  - natural-run-sized live overflow handling beat the older fixed-width adaptive rule on structured cases.
- **E176 - V6 BIDIRECTIONAL NATURAL OVERFLOW RUNS** - VALIDATED FOR ROUTER STUDY; not default alone.
  - bidirectional natural overflow was useful on mixed-direction structure but needed routing rather than unconditional use.
- **E177 - V6 OVERFLOW SHAPE SIGNAL AUDIT** - COMPLETE.
  - selected the multi-signal inputs used by the later V6 overflow router rather than relying on one pile-count threshold.
- **E178 - V6 OVERFLOW KERNEL SHOOTOUT** - COMPLETE.
  - natural-vs-live tiers were validated; repair/deferred alternatives were not retained.
- **E179 - V6 MULTI-TIER / GEOMETRY-FIRST ROUTER (A-E)** - RETAIN final E179E geometry-first router.
  - strict routing beat balanced routing; sampling tax was ~1%; final policy uses geometry/direction first and pile count second to avoid >10% structured regressions.
- **E180 - CANONICAL BENCHMARK GOVERNANCE** - RETAIN; generator details finalized by E180C.
  - established the 12-row canonical suite, deterministic/resumable methodology, and consistent cross-size structured generators.
- **E180B - 12-ROW BENCHMARK REDESIGN** - SUPERSEDED BY E180C.
  - useful intermediate redesign, reopened to fix structured scaling.
- **E180C - n^(2/3) STRUCTURED-GENERATOR SCALE + FROZEN BASELINE** - RETAIN.
  - standardized structured run/block counts across N and froze the corrected baseline methodology.
- **E181 - SHORT-PREFIX SAME-DIRECTION HANDOFF** - RETAIN.
  - corrected boundary ownership after short prefixes so continuation in the same natural direction avoids needless game switching.
- **E182 - CANONICAL DIRECTION / TOPOLOGY AUDIT** - COMPLETE / MEASUREMENT ONLY.
  - verified generator directionality, run topology, and boundary behavior used by later routing experiments.
- **E183 - GATED VALLEY BOUNDARY OWNERSHIP** - RETAIN.
  - selectively rescues valley boundaries back to the ascending game without broadly perturbing mixed-direction behavior.
- **E184 - DIRECTION-AWARE ROUTER REVISIT** - COMPLETE / NO GENERAL CHANGE.
  - broad >64 directional-dominance routing was rejected; only the exact-balance low-pile case survived in E184B.
- **E184B - EXACT-BALANCE LOW-PILE BIDIRECTIONAL ROUTE** - RETAIN in V6.
  - exact balanced low-pile geometry reliably identifies the bidirectional-natural overflow case.
- **E185 - PEAK OWNERSHIP REVIEW** - KEEP PEAKS ASCENDING; no production change.
  - alternative peak ownership did not justify changing the established boundary convention.
- **E186 - LINKED BLUEPRINT / POTENTIAL V8** - PLANNED / NOT RETAINED IN V2.
  - opened the linked-representation branch as a separate variation rather than complicating V2.
- **E187 - PARTITION-BACKEND COMPLETENESS AUDIT** - COMPLETE.
  - closed the primitive ipnsort follow-up after confirming the retained high-entropy backend pieces were already represented or not worthwhile.
- **E188 - DUAL-GAME ASSIGNMENT CLOSURE AUDIT** - COMPLETE; area dormant.
  - repeated assignment heuristics failed to improve geometry enough to cover their extra searches; reopen only with a genuinely new signal/state model.
- **E189 - V2/V7 RECONSTRUCTION TRAFFIC AUDIT** - RETAIN V7 restoration/retune.
  - restored the missed raw bulk-span decode in V7 and closed the conventional reconstruction-pass cleanup queue.
- **E190 - MODERN INDEX-TAIL RETEST** - UNIVERSAL REPLACEMENT REJECTED; V9/genericity path retained.
  - index tails were not a cheap-type V2 replacement but remained important for move-only and type-domain work.
- **E191 - V8 LINKED-BLUEPRINT BASELINE** - ESTABLISHED EXPERIMENTAL VARIATION.
  - linked reconstruction was made a separate V8 baseline rather than replacing V2.
- **E192 - V8 LINKED-BLUEPRINT STABILIZATION** - RETAIN BASELINE.
  - corrected/stabilized the linked implementation sufficiently for controlled follow-up experiments.
- **E193 - V8 ROUTING COVERAGE / FORCED-LINKED BASELINE** - RETAIN REFERENCE.
  - measured where linked reconstruction actually executes and established a forced-linked control for router studies.
- **E194 - V8 CATASTROPHIC-SHAPE FALLBACK ROUTER** - PROMISING CANDIDATE / superseded by later V8 routing work.
  - routing away from linked reconstruction on clearly hostile geometry removed the worst failures.
- **E195 - V8 NOISE-DENSITY FALLBACK ROUTER** - PROMISING CANDIDATE / superseded by later V8 routing work.
  - noise density provided another useful rejection signal for the linked path.
- **E196 - V8 LINKED FIRST-MERGE FUSION** - PARTIAL / not retained alone.
  - fusion helped Sawtooth/OrganPipe but did not eliminate linked metadata traffic; kept as a component for later layouts.
- **E197 - V8 LINKED FUSION BOUNDARY FAST PATHS** - NO standalone retain.
  - ordered/reverse-disjoint checks could not rescue cases dominated by metadata construction/traversal cost.
- **E198 - V8 IN-PLACE ELEMENT LINKS** - RETAIN into later V8.
  - reusing blueprint storage for links removed a second O(n) link array and broadly improved the linked path.
- **E199 - V8 IN-PLACE LINKS + FIRST-MERGE FUSION** - RETAIN into later V8.
  - combined the cheaper link representation with first-level fusion; strong on several structured/noisy rows but still needed locality routing.
- **E200 - V8 COMPACT SPAN-LINKED RECONSTRUCTION** - RETAIN into later V8.
  - span compression was excellent on contiguous structured layouts but poor on Alternating, which creates nearly one span per element.
- **E201 - V8 LINKED-ONLY LOCALITY ROUTER** - RETAIN V8 BASELINE.
  - first-64 span density routes between compact spans and in-place element links; full canonical acceptance passed.
- **E202 - V9 U64 INDEX-TAIL BASELINE** - RETAIN AS V9 CANDIDATE.
  - established a current index-tail baseline for primitive performance and later genericity work.
- **E203 - V9 MOVE-ONLY GENERIC COMPLETION + MAINTAINED INTEGRATION** - RETAIN.
  - completed non-copyable/move-only support while preserving the index-tail insertion mechanism as a maintained variation.
- **E204 - RANDOM BASELINE REGRESSION / EXPERIMENT-LEAKAGE AUDIT** - COMPLETE / NO REVERT.
  - V1-V7 hashes matched E189; apparent Random regression reproduced with untouched old code and was traced to host/session and benchmark-context differences, not experiment leakage.
- **E205 - BENCHMARK CONTEXT STABILITY / PRECONDITIONER AUDIT** - RETAIN existing rotating order; reject added preconditioner.
  - 8-vs-10-algorithm rotation drift stayed <~0.5%, while self-warming perturbed absolute V2 time by ~6%; same-session/CPU governance became mandatory.
- **E206 - TYPE-DOMAIN VALUE-TAIL VS INDEX-TAIL STUDY** - MEASUREMENT / TYPE MAP.
  - index tails remained slower for cheap trivial records but became attractive for nontrivial owners and were required for move-only types; later results were re-reduced order-neutrally.
- **E207 - TYPE-ROUTING BOUNDARY MAP** - COMPLETE.
  - no reliable trivial-size crossover through 96 bytes; copy/nontrivial traits were more predictive than sizeof(T).
- **E208 - LARGE-TRIVIAL / NONTRIVIAL-OWNER ROUTING COMPLETION** - TYPE MAP COMPLETE / NO SORT-CODE CHANGE.
  - no defensible byte threshold through 256-byte trivial records; conservative type-only guidance became non-copyable or nontrivial-owner -> indexed, trivial-copyable -> simulated/value-tail.
- **E209 - V10 ALLOCATION-FREE BOUNDED BASELINE** - RETAIN as maintained zero-allocation baseline.
  - fixed tail/run metadata plus allocation-free fallback sorted canonical and move-only cases with zero heap allocations, though structured performance was initially much slower than V2.
- **E210 - V10 FIXED-WORKSPACE COMPRESSION** - RETAIN.
  - reduced explicit fixed metadata peak to ~2.56 KiB with equivalent routing/correctness and essentially neutral timing.
- **E211 - BOUNDED-CHUNK CAPACITY ANALYSIS, RIGID 63/64 BASELINE** - COMPLETE / NO CODE CHANGE.
  - quantified where fixed directional capacities overflow and established rigid 63/64 as the control for adaptive-capacity work.
- **E212 - ADAPTIVE PER-CHUNK GAME-CAPACITY ANALYSIS** - COMPLETE / CANDIDATE IDENTIFIED.
  - simulations showed shared/adaptive capacity could materially reduce directional waste on some structured shapes.
- **E213 - V10 MONOTONE-CONFIRMED ADAPTIVE CAPACITY** - REJECT.
  - adaptive capacity postponed Sawtooth pile overflow only to hit the 64-run limit, making 1m Sawtooth ~35.6% slower.
- **E214 - NAIVE CHUNK SEALING + ALLOCATION-FREE ROTATION MERGE** - REJECT.
  - sealing/sorting chunks and recursively rotation-merging them was catastrophically slower on Noise/Alternating and still ~17% slower on targeted 1m Sawtooth.
- **E215 - LIVE PILE-RECYCLING STRUCTURAL SCREEN** - ANALYSIS COMPLETE / DEFERRED.
  - live recycling looked structurally plausible, but V10 lacks allocation-free membership metadata needed to merge active scattered piles safely.
- **E216 - V11 SHARED 127-TAIL POOL + SELECTIVE LOW-RUN IN-PLACE MERGE** - RETAIN / V11 BANKED.
  - shared bounded tail storage plus geometry-gated in-place natural-run merging materially improved the allocation-free branch while preserving zero-allocation/move-only guarantees.
- **E217 - V11 BOUNDED LIVE RUN-STATE RECLAMATION** - RETAIN.
  - when >64 long natural runs are encountered, V11 reclaims descriptors by merging the smallest adjacent completed pair; 1m MixedDirectionRuns fell from ~34.5 ms to ~19.1 ms without allocating.
- **E218 - V11 EARLY REPEATED-OVERLAP / SAWTOOTH ROUTER** - RETAIN.
  - early repeated-ramp detection avoids the full classification pass before allocation-free fallback; 1m Sawtooth dropped from ~22.1 ms to ~10.9 ms while E217's Mixed win remained.
- **E219 - V11 FREE-8 SMALLEST-ADJACENT RECLAMATION** - REJECT.
  - batching 8 minimum-adjacent merges reduced capacity events but added premature movement; 1m Mixed was ~1.0024x E218.
- **E220 - V11 ALL-21 LOCAL THREE-RUN RECLAMATION** - REJECT / useful baseline.
  - 21 disjoint a-vs-c triple merges in one event were nearly neutral (~0.9971x on 1m Mixed) despite extra merges, showing scheduler scans were not the bottleneck.
- **E221 - V11 POWERSORT-GUIDED 21-MERGE RECLAMATION** - REJECT / CLOSE SCHEDULER STUDY.
  - PowerSort-guided 21-way reclamation was ~1.0106x on 1m Mixed; deeper reclamation-policy microstudies were deprioritized.
- **E222 - V3-DERIVED BOUNDED PERMUTATION PRIMITIVE** - REJECT.
  - fixed-window V3 cycle reconstruction remained ~1.8-7.5x slower than the allocation-free partition fallback because the subsequent in-place combine dominated.
- **E223 - POST-E218 FULLY-SHARED-127 CAPACITY REASSESSMENT** - REJECT.
  - Noise5/10 still exhaust the total 127-tail budget; removing 63/64 directional caps only postpones fallback and does not solve the large-N cost.
- **E224 - DENSE-RANGE COUNTING BACKEND** - INVALID / BENCHMARK-DOMAIN OVERFIT.
  - the apparent huge Noise5/10 win exploited the generator-specific [0,n) numeric universe, not generic comparison-sort structure. Production remained pre-E224 and a guardrail against generator-domain overfitting was added.
- **E225 - `simulated` BOUNDED LONG-NATURAL-RUN DIRECT ROUTE - RETAINED** - RETAIN; major architectural pivot that introduced the direct structural-route family later propagated and extended by E227/E232/E233.
  - Summary: Retained the bounded long-natural-run direct route that later underpins the direct/noalloc structured path. Historical detailed timings are superseded by later audits and current descendant evidence.
- **E226 - BENCHMARK MULTI-VARIATION SELECTOR - RETAINED** - RETAIN; benchmark infrastructure only, no sorting behavior changed.
  - added comma-separated layer-tag selection with automatic `std::sort` inclusion for paired ratios; smoke tests preserved default/all behavior and rejected invalid selectors.
- **E227 - E225 NATURAL-RUN PRE-ROUTER PROPAGATION - RETAINED** - RETAIN for allocating `physical` through `indexed`; intentionally omitted from `noalloc` / `noalloc-low-run` because E225 requires O(n) scratch.
  - the shared comparator-only long-natural-run route produced large structured-input wins across compatible variations with no reproducible Random penalty; full canonical acceptance passed.
- **E228 - PERMANENT MIXED-PHASE ROUTER-SAFETY INPUTS - RETAINED** - RETAIN; benchmark/router-safety infrastructure only.
  - added permanent `MixedPhase3` (Noise5 -> MixedDirectionRuns -> Random) and `MixedPhase12` (all 12 frozen families as contiguous phases) without exposing labels/boundaries to sort code; these became canonical guards against prefix-only routing.
- **E230 - IPNSORT COMPARISON-KIT LAYER-TAG MIGRATION** - COMPLETE.
  - migrated the comparison kit to the E229 tagged variation interface without changing sorting behavior.
- **E232 - SPARSE DISTRIBUTED-DISORDER 128-BLOCK DIRECT ROUTE** - RETAINED in `simulated-direct`.
  - clean affected-variation routing audit and strong Noise5/10 gains through 1m; the route also generalized across multiple OOD sparse-disorder families.
- **E233 - TWO-LANE ZIGZAG DIRECT ROUTE** - RETAINED in `simulated-direct`.
  - proof-based two-monotone-lane route fired 500/500 on Alternating and 0/500 on the other canonical rows; paired timing was roughly 0.20x/0.22x at 10k/100k and 0.291x at 1m.
- **E234 - E232/E233 GENERALITY + PROBE-FIRST SAFETY AUDIT** - COMPLETE / NO PRODUCTION CHANGE.
  - E233 generalized cleanly across independent two-lane families and E232's backend remained profitable across broad OOD sparse disorder; first probe-first replacements were still roughly 9-15% slower on Noise5/10.
- **E235 - NEW PROBE-FIRST `simulated-direct` VARIATION** - RETAINED AS DISTINCT VARIATION.
  - added allocation-free 48-value Patience-geometry observation plus distributed safety confirmation while preserving E233's proved zigzag route; aggressive `simulated-direct` remained the faster shortcut-rich reference.
- **E236 - IMPROVED PROBE-DERIVED SPARSE ROUTING** - RETAINED.
  - broadened safe moderate-N sparse admission, materially improving unseen replacement/local-swap families while preserving E235's stricter policy at large N where broader admission was not profitable.
- **E237 - SELECTIVE PROOF-BASED ZIGZAG PROPAGATION** - RETAINED selectively.
  - propagated the proved zigzag shortcut to `simulated-inplace-direct`, `frozen-live-direct`, and `linked-direct` for n>=32768; sparse routing intentionally remained unique to the aggressive/probe-first branches.
- **E238 - REPEATED DUAL-PATIENCE OBSERVATION SCHEDULE STUDY** - RESEARCH RETAIN / NO PRODUCTION CHANGE.
  - fixed ~4k spacing with 64-value remote observations was the strongest initial deterministic cost/blind-gap compromise; the study established that deterministic sparse probing cannot eliminate adversarial blind gaps.
- **E239 - DUAL-PATIENCE PROBE-WIDTH STATISTICS / SAMPLE-COMPLEXITY STUDY** - RESEARCH RETAIN / NO PRODUCTION CHANGE.
  - short probes showed strong sample-efficiency, but the study's input-family framing was explicitly superseded; E240 redirected the objective to route sufficiency with even hardware-relevant widths.
- **E240 - ROUTE-SUFFICIENCY / FIRST-CLASS OBSERVATION STUDY** - RESEARCH RETAIN / NO PRODUCTION CHANGE.
  - fixed width 32 preserved 99.89%/99.77% of retained sparse-route opportunities at 10k/100k while sharply reducing random-like confirmation candidates versus width 24; simple pile-count early-stop rules did not beat fixed 32.
- **E241 - PRODUCTIVE 32-VALUE FIRST-CLASS PATIENCE OBSERVATION - REJECTED IMPLEMENTATION** - REJECT implementation; retain lesson.
  - wrapper-level reuse of a 32-value Patience state duplicated/omitted mature insertion semantics and created regressions; productive probing must live inside the mature engine, which E242 then implemented.
- **E242 - MATURE-ENGINE 32-VALUE LIVE PATIENCE CHECKPOINT - RETAINED** - RETAIN in `simulated-probe-direct`.
  - moved the 32-value checkpoint inside mature simulated Patience insertion, allowing optional 48-value continuation on the same state and zero restart on fallback; Noise5/10 routing improved while canonical controls remained clean.
- **E243 - ROUTING COVERAGE + DUAL-PATIENCE PRECHECK NAMING AUDIT - COMPLETE** - documentation/coverage audit; no production change.
  - quantified which canonical rows exit through monotone, two-game single-pile, bounded-natural-run, sparse, or full Patience paths, and standardized the dual-Patience terminology used by later experiments.
- **E244 - DUAL-PATIENCE STRUCTURAL PRECHECK EXECUTION-ORDER STUDY** - REJECT reorder changes; production order unchanged.
  - moving monotone proof earlier modestly helped Reverse but hurt long-prefix structured rows such as OrganPipe/Rotated; reusing already-proved prefix work gave no meaningful Sorted gain, so execution order was not the real monotone bottleneck.
- **E245 - SINGLE-PILE MONOTONE DUAL-PATIENCE PROOF KERNEL - RETAINED** - RETAIN generic branch-amortized monotone proof kernel.
  - batching post-prefix comparisons cut monotone proof cost roughly 11-13% on Sorted and 9-14% on Reverse while preserving fast Random rejection and generic comparator behavior; arithmetic AVX2 remained an unmerged research lead.
- **E246 - REPEATED LIVE DUAL-PATIENCE OBSERVATION, `simulated` PHASE TRAJECTORY - ACTION REJECTED / SIGNAL RETAINED** - retain trajectory signal; reject tested in-loop continuation switch.
  - periodic live pile geometry clearly detected MixedPhase3's late entropy transition, but switching the active insertion policy bought only ~1% and introduced >1% regressions elsewhere. This motivated phase-boundary/fresh-decomposition work instead.
- **E247 - GALLOPING DUAL-PATIENCE PHASE MAP / CHANGE-POINT STUDY** - RESEARCH RETAIN; no production change. Independent 32-value dual-Patience observations showed that x2 galloping capped at 8k with reset+confirmation preserved route-map fidelity while reducing stable-region probe work.
  - Established the mapping schedule used by E248-E251; later work retained late-biased no-lookback boundaries. Detailed timing artifacts were later pruned.
- **E248 - PRE-MAPPED ROUTE EXECUTION - RESEARCH FINDING RETAINED; NO PRODUCTION CHANGE** - HISTORICAL / see summary.
  - Summary: Pre-mapped execution showed useful route-state reuse but did not justify a production change; preserve only as phase-map execution evidence.
- **E249 - MIDPOINT BOUNDARY / NO-LOOKBACK EXECUTION - RESEARCH FINDING RETAINED; NO PRODUCTION CHANGE** - HISTORICAL / see summary.
  - Summary: Midpoint/no-lookback execution was informative but not productionized; preserve as a phase-map boundary/execution lead rather than a current optimization.
- **E250 - GATED GALLOPING PHASE-MAP VARIATION** - RETAIN as `simulated-phase-map`; ordinary `simulated` unchanged. For n>=65536, an endpoint diversity gate conditionally enables E247 x2/8k mapping; mapped regions use fresh `simulated` and a 75%-late no-lookback boundary.
  - Stable canonical rows were essentially neutral while MixedPhase3/MixedPhase12 improved strongly, including at 1m. E251 later added a separate zero-copy range-aware plant.
- **E251 - RANGE-AWARE PHASE-MAP EXECUTION** - RETAIN as `simulated-phase-map-range`; E250 and ordinary `simulated` remain distinct. A phase-map-local span-capable `simulated` engine removes mapped-region copy-in/copy-back while preserving E250 mapping, gating, and boundaries.
  - The zero-copy path improved mapped phase workloads and preserved generic comparator support.
- **E252 - PHASE-MAP ROUTE-STATE BACKEND SPECIALIZATION** - REJECT; restore E251 byte-for-byte. Broad STRUCTURED/ENTROPY/OTHER map states were too coarse for safe backend selection even though forced partition was very attractive on some contaminated entropy chunks.
  - Durable finding: phase-boundary metadata and backend-choice metadata are different problems; E254 later studied richer chunk-local signals.
- **E253 - ONLINE PHASE SPLIT / FRESH-DECOMPOSITION COMPETITOR** - REJECT maintained variation; research finding retained. Immediate online region finalization was effectively tied with E251 once both used fresh per-region decompositions, so execution order alone offered no advantage.
  - This checkpoint also consolidated layer-tag naming and the experiment log; detailed procedural artifacts were later pruned.
- **E254 - MAPPED-CHUNK BACKEND ECONOMICS / RICHER METADATA** - RESEARCH RETAIN; no production change. Counterfactual chunk timing showed broad ENTROPY was insufficient, but stored ENTROPY with local `(STRUCTURED, ENTROPY, ENTROPY)` strongly favored partition on contaminated transition chunks across canonical/OOD tests.
  - E255 subsequently production-tested the conservative signature.
- **E255 - TRANSITION-CONTAMINATED ENTROPY PARTITION DESCENDANT** - RETAIN as `simulated-phase-map-range-transition-partition`; E251 remains unchanged. Only stored ENTROPY with local `(STRUCTURED, ENTROPY, ENTROPY)` forces the high-entropy partition backend.
  - Activation was zero on stationary canonical families and profitable on targeted contaminated MixedPhase12 chunks.
- **E256 - CONTAMINATED-ENTROPY BODY-ENTROPY BROADENING** - RETAIN as separate descendant. Broadens E255 only to non-STRUCTURED tails, adding `(STRUCTURED, ENTROPY, OTHER)`; added activations stayed phase-specific and improved newly triggered MixedPhase12 cases.
  - E255 remains the conservative control.
- **E257 - REMAINING MAPPED-CHUNK BACKEND SIGNATURE AUDIT** - REJECT further broadening. Neighboring broad-entropy signatures looked attractive canonically but regressed toward neutral/slightly harmful across expanded OOD phase placements.
  - E256 is the safe specialization frontier absent a materially new discriminator.
- **E258 - Current ipnsort-gap revalidation / Random routing-tax + backend phase profile** - DIAGNOSTIC RETAIN; no production change. Native Rust ipnsort could not be rebuilt here, but same-session full-route/forced-backend ratios were only about 1.009x/1.011x at 10k, 1.008x/1.003x at 100k, and 1.001x/1.008x at 1m for `simulated`/`simulated-direct`; the 64-value entropy sampler itself was ~0.08-0.11 us.
  - A low-overhead sampled profile of the forced retained backend estimates partition/smallsort/pivot/control shares at ~44.7/44.4/4.5/6.4% (10k), ~52.3/37.2/3.7/6.8% (100k), and ~57.6/32.0/3.4/6.9% (1m). Equality filtering and heapsort fallback did not fire on canonical Random; ~95% of elements reach <=32 leaves (mean leaf ~18.8), making partition and smallsort the clear parity targets.
- **E259 - HIGH-ENTROPY PARTITION / SMALLSORT PARITY + LEAF-CUTOFF ECONOMICS** - DIAGNOSTIC RETAIN; no maintained sort change. Isolated tests reconfirmed the E166 ipnsort-style 18–32 smallsort at roughly 0.75–0.81x the older baseline. The generic Jesse cyclic partition is now ~1–2% faster than the retained u64 ipnsort-style gap loop on large isolated partitions under current GCC, but only ~0.1–0.5% end-to-end and not stable enough to replace it.
  - Cutoffs below 32 were scale-dependent: up to ~1–1.5% faster at 1m but about ~1% slower at 100k, with no monotonic crossover in 200k–750k checks. Keep current partition/cutoff; E260 moves to compiler/source-codegen parity.
- **E260 - GCC HIGH-ENTROPY SMALLSORT CODEGEN AUDIT / U64 NETWORK UNROLL** - RETAIN. GCC was walking the 9/13 comparator-network index tables at runtime; for eligible 8-byte integral/default-comparator instantiations, forcing network-loop unrolling emits straight compare-exchange min/max sequences while leaving other types/comparators on the E259 loop.
  - Same-binary Random u64: `simulated` 0.881x / 0.893x / 0.896x at 10k/100k/1m; `simulated-direct` 0.884x / 0.896x / 0.908x. Post-change 1m profiling shifts the backend to ~63.4% partition / ~25.5% smallsort, making partition the next parity target. i32 code shape remains unchanged and genericity smoke passed.
- **E261 - GCC HIGH-ENTROPY PARTITION CODEGEN / CALL-BOUNDARY AUDIT** - RETAIN. Keep the existing two-at-a-time ipnsort-style u64 partition algorithm, but place the eligible 8-byte integral/default-comparator specialization behind a shared noinline helper; generic types/comparators retain E260's inline path.
  - Scalar/prefix-batched/pivot-gap partition rewrites looked better in isolation but failed or went neutral in the integrated backend. Same-binary `simulated` Random improves ~2.7% paired at 10k and ~0.8% at 100k; full 14-row 10k/100k acceptance had no >1% paired regression.
- **E262 - E260/E261 PROPAGATION VALIDATION** - COMPLETE / RETAIN PROPAGATION AFTER DEEP ROUTE AUDIT.
- **E263 - GCC HIGH-ENTROPY PRODUCTION-DRIVER CODEGEN AUDIT** - RETAIN.
- **E264 - SHARED HIGH-ENTROPY PRIMITIVE CONSOLIDATION / DRIFT PREVENTION** - RETAIN source consolidation; REJECT call-level physical/indexed router consolidation.
  - Summary: Consolidated shared high-entropy primitives to prevent semantic drift across variations; no intended algorithmic behavior change beyond ownership cleanup.
- **E265 - HIGH-ENTROPY ANCESTOR / EQUALITY-CONTROL COST AUDIT** - REJECT simplification; retain E264 production behavior unchanged.
  - Summary: Revalidated the specialized ancestor/equality control; later E335 audit is the authoritative retain evidence for the exact-equality cleanup.
- **E266 - HIGH-ENTROPY COMPLETENESS / CURRENT-UPSTREAM SEMANTICS SECOND PASS** - RETAIN outlined upstream-style heapsort fallback; REJECT exact upstream pivot policy under GCC.
  - Summary: Completed a second-pass high-entropy semantics/completeness audit; later E336 is the authoritative audit for the depth-limit heapsort fallback.
- **E267 - PIVOT MATERIALIZATION / ALIASING + PARTITION-CALL ABI AUDIT** - RETAIN known-pivot-by-value production ABI; REJECT in-array reference and caller-split alternatives.
  - Summary: Audited pivot materialization, aliasing, and partition-call ABI; no retained broad algorithmic change emerged from this pass.
- **E268 - HIGH-ENTROPY GENERIC-ABSTRACTION / COMPARATOR-CODEGEN AUDIT** - COMPLETE / NO PRODUCTION CHANGE.
  - Summary: Audited comparator/generic-abstraction codegen in the high-entropy path and retained the resulting shared implementation baseline used by subsequent router work.
- **E269 - HIGH-ENTROPY ADMISSION / MONOTONE-PREFIX FALSE-NEGATIVE AUDIT** - REJECT production integration; preserve recovery mechanism for a future router/code-layout refactor.
  - Summary: Found a real monotone-prefix false-negative recovery signal, but production integration was rejected because semantically inactive paths moved materially from code-layout changes. Preserve the routing signal only for a future layout-stable router/TU boundary.
- **E270 - FIRST-EIGHT STRUCTURAL GATE** - REJECT / NO PRODUCTION CHANGE.
  - Summary: REJECT first-eight-only admission. Production sorting behavior remains E269/E268. Future work may combine the strong first-eight chance bound with an independent later-prefix/global structure confirmation, but should not treat a broken 8-element run as high-entropy proof by itself.
- **E271 - SHARED FIRST-EIGHT PREFLIGHT / PROOF-ELIMINATION ROUTER** - RETAIN for `simulated-direct`; broader propagation deferred pending architecture-specific tests.
  - Summary: Deep validation before propagation: 3,200 adversarial first-eight-prefix/tail cases (all inversion counts 0-7 crossed with random/sorted/reverse/alternating tails) produced 0 structural-route mismatches against the old serial router. A further 16,000 equal-heavy and reversed-comparator cases also produced 0 route mismatches.
- **E275 - E244 ORDERING LEFTOVERS AFTER E271/E272** - COMPLETE / NO PRODUCTION CHANGE.
  - Summary: reject all ordering changes; restore E274/E245 production byte-for-byte. E244 execution-order direction is closed. E271 should be propagated only where overlapping structural proofs allow prerequisite elimination.
- **E273 - E269 RECOVERY RECONSIDERATION INSIDE E271** - REJECT integration / NO PRODUCTION ROUTING CHANGE.
  - Summary: stop rather than tune source placement around GCC. Restore E272/E271 production. Preserve E269's previously validated routing signal for a future layout/compiler strategy.
- **E272 - DIRECT-VARIANT STRUCTURAL-ROUTER COMPATIBILITY AUDIT** - COMPLETE / analysis only / no production code change.
  - Summary: E273 retest E269 inside the retained outlined `simulated-direct` dispatcher; if clean, use that mature tree as reference, propagate reduced long+zig trees to the three compatible families with localized A/Bs, leave long-only families unchanged absent separate evidence, and handle probe-first/E244/E245 as its own later prefix-router project.
- **E276 - E271 REDUCED PREFIX-EVIDENCE PROPAGATION AUDIT** - PARTIAL RETAIN / EXPLICIT PER-VARIATION DISPOSITION.
  - Summary: Diagnostic note: an early fixed-seven-comparison reduced tree was rejected during E276 because it overpaid on Reverse. The retained reduced tree is deliberately smaller than E271's full three-proof inversion-count dispatcher. Same-binary wrapper experiments were used only for causal diagnosis; final retain/reject decisions are based on production-shape canonical runs because wrapper placement materially affects ultra-fast paths.
- **E277 - PRODUCTIVE PHASE-MAP INDEX-0 OBSERVATION REUSE** - REJECT / research finding retained.
  - Summary: no production change. The amount of duplicate work available at the one exact observation/start overlap is too small. Revisit phase-map productivity only with a materially new mechanism that reuses interior state across the retained economic boundary or changes mapping/execution jointly.
- **E278 - LINKED LOCALITY SELECTOR REFINEMENT** - RETAIN.
  - Summary: Full-sort binary-to-binary timings show the usual layout/session movement on unchanged rows; the retain decision is based on the causal reconstruction+merge comparison for only the selector-changed blueprints plus the full canonical correctness gate.
- **E279 - LINKED METADATA COMPACTION / ALLOCATION AUDIT** - REJECT production integration; causal lead preserved.
  - Element-link metadata compaction is materially faster in reconstruction-only A/B, but both combined-endpoint/start reuse and a narrower start-only integration caused severe 100k whole-sort regressions from header/code-shape sensitivity. Span path left unchanged. Production restored to E278; revisit only under a layout-controlled reconstruction boundary.
- **E280 - LINKED-DIRECT FINAL-GEOMETRY / CONCENTRATION SELECTOR** - RETAIN for `linked-direct`; backward propagation to ordinary `linked` explicitly deferred to E281.
  - Summary: Timing-distribution audit: aggregate medians are unsafe as sole evidence on multimodal rows. MixedDirectionRuns showed fast (~126–128 us) and slow (~534 us) modes with roughly half the mass in each mode despite E280 changing the selector on 0/1000 trials; execution-order splits could flip paired ratios from ~0.80x to ~1.28x. Alternating also showed multiple modes despite identical input every trial. Retention is therefore based on selector-changed causal evidence, OOD geometry, route counts, and controlled runs rather than unexplained raw production median swings.
- **E281 - E280 BACKWARD PROPAGATION TO ORDINARY `linked`** - REJECT backward propagation; retain E278 for ordinary `linked` intentionally.
  - Summary: E280 is intentionally specific to `linked-direct`; ordinary `linked` stays on retained E278. Bank the high-concentration sparse-noise override for a future layout-controlled selector implementation.
- **E282 - GENERAL PHASE-MAP META-ROUTER PROTOTYPE** - RESEARCH RETAIN / ARCHITECTURE SUCCESS; copy-based wrapper not productionized.
  - Summary: fresh mature routing/decomposition per region is valuable across multiple architectures even after paying extra O(n) movement. Next experiment should remove the copy tax with a range-capable base interface on one candidate before testing broad propagation.
- **E283 - MATURE `simulated-direct` PHASE-MAP RANGE EXECUTION** - RETAIN as new maintained descendant; propagation deferred to E284.
  - Summary: Propagation: intentionally NOT tested in E283. E284 owns propagation as a separate experiment.
- **E284 - MATURE PHASE-MAP PROPAGATION TO TWO INDEPENDENT DIRECT FAMILIES** - RETAIN as two new hybrid descendants; base variations unchanged.
  - Summary: RETAIN `linked-direct-phase-map-mature` and `simulated-inplace-direct-phase-map-mature` as separate maintained descendants. Do not silently propagate into `linked-direct`/`simulated-inplace-direct` themselves. Future propagation must be tested variation-by-variation, ideally reusing the shared mature range backend before investing in native range adapters.
- **E286 - SIMULATION-BACKED QUICKSORT CACHE-AWARE BUFFERED SCATTER / GROUPED FLUSH** - REJECT / grouped-blueprint concept remains open.
  - Summary: reject tiny per-bucket buffering/forced group flushes. This does not reject bounded route blueprints or phase-local splitter refresh; it only shows that this write-buffer mechanism does not recover E285's remaining gap.
- **E287 - SIMULATION-BACKED QUICKSORT ROUTE-WIDTH SPECIALIZATION** - REJECT.
  - Summary: reject scalar route narrowing. Preserve packed/SIMD route layouts as a distinct future mechanism; fewer bytes alone are not sufficient.
- **E288 - SIMULATION-BACKED QUICKSORT CLASSIFICATION ILP WIDTH / DEPTH INTERACTION** - RESEARCH RETAIN / NO BACKEND REPLACEMENT.
  - Summary: retain W4 as the scalar baseline for future SIMD/multi-pivot or grouped-blueprint work; no JesseSort routing change.
- **E289 - MICRO-BLUEPRINT WINDOWS / TWO-PASS RECLASSIFICATION** - REJECT mechanism / grouped-blueprint family remains open.
  - Summary: do not remove the global route array by blindly recomputing routes. A mapping/reconnaissance pass must make those decisions productive/reusable, or grouped reconstruction must preserve one classification per element.
- **E290 - INDEPENDENT MICRO-BLUEPRINT SORTS + RUN MERGE** - REJECT.
  - Summary: reject independently terminating scan groups as sorted runs for the Random backend. Phase-local splitter refresh should not imply general full-sort-then-merge boundaries unless JesseSort's existing phase machinery can make the merge essentially free.
- **E291 - GROUPED MICRO-BLUEPRINT BLOCK TRANSPOSE** - RESEARCH RETAIN / GROUPED BASELINE / NO PRODUCTION CHANGE.
  - Summary: block transpose is the first micro-blueprint design with the desired invariants-one classification per element, bounded blueprint storage, and linear reconstruction-so retain it as the starting point for mapping-scan fusion, phase-local splitter refresh, two-level coarse/fine routing, or SIMD classification. Do not keep tuning scalar group size alone without a new mechanism.
- **E292 - HIERARCHICAL MICRO-BLUEPRINT COARSE/FINE ROUTING** - REJECT / NO PRODUCTION CHANGE.
  - Summary: shortening the splitter dependency chain does not repay a second reconstruction/movement stage. Reject this scalar hierarchical scatter form; a future coarse/fine design must avoid another full physical movement.
- **E293 - AVX2 GATHER SPLITTER-TREE CLASSIFICATION** - REJECT / W4 SCALAR REMAINS CLASSIFIER BASELINE.
  - Summary: reject variable-index gather tree walking. SIMD remains open only if the pivot layout avoids a gather per tier (e.g. fixed multi-pivot comparisons or another representation).
- **E294 - EXACT PRODUCTION HIGH-ENTROPY BACKEND CONTROL FOR BLUEPRINT QUICKSORT** - RESEARCH RESET / NO PRODUCTION CHANGE.
  - Summary: the mature JesseSort random backend is materially stronger than the proxy used to motivate the early near-parity claim. Multiway simulation is still close enough at 10k to justify research, but it is not currently a replacement and the 100k gap is substantial. Future simulation-backend experiments must benchmark directly against the exact production driver; proxy-only improvements are insufficient.
- **E295 - PRODUCTION-BACKED BLUEPRINT CLASSIFICATION ILP-WIDTH REVISIT** - RESEARCH RETAIN W4 AS SCALAR BASELINE / NO PRODUCTION CHANGE.
  - Summary: retain W4 only as the scalar classifier reference for subsequent production-backed blueprint experiments. Do not integrate a blueprint fallback and do not use the faster multi-width-binary result as evidence of a win. Future E296+ revisit experiments must isolate candidate/control code shape where practical to reduce cross-instantiation layout contamination.
- **E296 - FIXED-8-BIT DIRECT-SCATTER BLUEPRINT CHUNKS** - RESEARCH RETAIN / FIRST PRODUCTION-BACKED BLUEPRINT WIN / NO PRODUCTION CHANGE.
  - Summary: immediate fixed-stride scatter is materially different from naive blueprint wrapping and from E291 block transpose. It amortizes eight logical partition decisions into one sparse write plus one local finish/linear compaction, and for the first time crosses the exact mature production backend. This is not yet integration evidence: broader random distributions, duplicates, 1k/1m scale, scratch footprint, splitter-quality/headroom policy, and code-layout sensitivity still need separate experiments.
- **E297 - DIRECT-SCATTER SPLITTER OVERSAMPLING / BUCKET-HEADROOM REVISIT** - RETAIN as lower-memory research alternative; E296 remains speed baseline.
  - 2x/4x sampling substantially tightens occupancy but sorting the larger splitter sample consumes the gain. Decision-grade 4x/CAP256 (512 KiB workspace), 500 trials/two seeds: 10k 0.983x production on both seeds; 100k 1.014x and 1.010x.
- **E298 - DIRECT-SCATTER SHARED-OVERFLOW BUCKET LAYOUT** - REJECT AS SPEED BASELINE / RETAIN AS MEMORY EVIDENCE; E296 remains champion. No production change.
  - Summary: shared linked overflow is a useful proof that E296 workspace can be reduced substantially without destroying the 10k gain, but irregular spill insertion/reassembly gives back the robust 100k win. Do not replace E296. Next memory-oriented revisit should test fixed intermediate headroom (CAP320/CAP384) before any more elaborate overflow structure.
- **E299 - DIRECT-SCATTER FIXED INTERMEDIATE BUCKET HEADROOM** - RESEARCH RETAIN / CAP448 preferred memory-speed point; no production change.
  - Summary: retain CAP448 as the preferred fixed-headroom research point because it preserves a reproducible win at both primary sizes while cutting E296 workspace by 12.5% and avoiding E298's irregular spill path. CAP480 remains the lower-fallback sibling. Do not integrate yet; broader random distributions / 1k-1m scaling / duplicates and code-layout sensitivity remain open.
- **E300 - PRODUCTION-BACKED SHALLOW DEFERRED BINARY SIMULATION REVISIT** - REJECT / old proxy-era conclusion strengthened; no production change.
  - Summary: reject shallow recursive deferral under the mature backend. The better production leaf sorter does not rescue the extra logical classification, route storage, reconstruction, and child-pivot imbalance. This strengthens the interpretation that blueprinting only becomes competitive after changing the architecture to multiway/direct-destination routing as in E296-E299. Do not rerun this family unless a materially new logical-child pivot or zero-reconstruction mechanism appears.
- **E301 - PRODUCTION-BACKED ROUTE-STATE REPRESENTATION REVISIT** - REJECT explicit narrow route-prefix state / retain E299 natural heap-index classifier; no production change.
  - Summary: retain the natural `size_t` heap-index W4 classifier. Narrow scalar route state does not save memory in the direct-scatter design and its extra conversions/node reconstruction materially hurt codegen. This strengthens rather than rewrites E287's older proxy-era lesson.
- **E302 - PRODUCTION-BACKED BUFFERED DIRECT-SCATTER REVISIT** - REJECT buffering / retain immediate CAP448 direct writes; no production change.
  - Summary: reject tiny per-bucket staging in the direct-scatter architecture. The destination for each bucket is already a monotonically advancing contiguous region; extra fill counters, staging storage, branch/flush logic, and copies only add work. E286 remains useful historical evidence and E302 confirms the conclusion against the mature backend.
- **E304 - MATURE PHASE-MAP PROPAGATION TO `physical-direct`** - RETAIN as separate `physical-direct-phase-map-mature` hybrid descendant; base `physical-direct` unchanged.
  - Summary: retain the hybrid descendant because it materially improves true phase-changing inputs at scale without changing stationary/base semantics, while documenting the 100k MixedPhase12 common-mode cost. E304 demonstrates that the shared mature range executor generalizes across a third genuinely distinct representation family; native range adapters should now require a representation-specific payoff rather than being built just for completeness.
- **E305 - INDEXED/MOVE-ONLY MATURE PHASE-MAP BOUNDARY AUDIT** - REJECT generic shared-range propagation; retain ordinary `indexed-direct` unchanged.
  - Summary: the shared mapper itself remains useful, but the shared simulated range executor erases a representation-specific indexed-direct advantage on many-short-phase input. Since move-only values cannot use that executor at all, a maintained generic indexed phase-map descendant has no clean architectural justification. Future phase-map work should target shared mapping/boundary/router quality rather than further representation propagation.
- **E306 - SHARED PHASE-MAP ADAPTIVE LATE-BOUNDARY AUDIT** - REJECT shared adaptive escalation; retain the mature 75%-late boundary policy unchanged.
  - Summary: later boundaries remain a credible local tool for many-short-phase maps, but boundary fraction should not be escalated globally based only on transition count. Reopen only with a stronger shared signal (for example region geometry/backend economics) that predicts when late bias is favorable across representations.
- **E307 - BACKEND-AWARE PHASE-MAP COMPLEXITY GATE** - RESEARCH RETAIN router abstraction; no maintained indexed descendant.
  - Summary: complexity gating removes E305's large MixedPhase12 regression while preserving a large simple-phase benefit. Retain the architectural lesson as `shared mapper + backend-specific map acceptance/economics policy`. Do not add an indexed hybrid yet because the shared simulated range executor remains unavailable for noncopyable/move-only T.
- **E308 - PHYSICAL-DIRECT BACKEND-AWARE SIZE / MAP-COMPLEXITY GATE** - RETAIN for `physical-direct-phase-map-mature`; base `physical-direct` unchanged.
  - Summary: retain the conservative size-aware complexity gate in the maintained physical hybrid. This eliminates the smaller many-phase hazard while preserving the large simple-phase gain and restoring many-phase range execution at clearly amortized scales. E308 reinforces the architecture: shared mapping/boundaries, backend-specific execution acceptance.
- **E309 - GENERIC PHASE-GEOMETRY OOD VALIDATION** - CLOSE threshold tuning; retain backend-aware acceptance architecture, demote E307 exact <=2 rule and E308 exact size/complexity gate to provisional research policies.
  - Summary: no production/shared mapper change. Preserve E306 rejection and 75%-late boundary. Keep `shared mapper + backend-specific economics` as the architectural result; stop phase-count/size threshold tuning until a more causal cost signal exists.
- **E311 - ORDINARY-LINKED SELECTOR / LOCALITY GENERALIZATION** - RESEARCH RETAIN signal; production E278 selector unchanged.
  - Summary: retain concentration as a real third selector dimension for future ordinary-linked work, but do not productionize cutoff 35 yet. Exact threshold remains empirical and E281 already demonstrated source-integration layout sensitivity. Next step requires a layout-stable production boundary or a more causal normalized concentration rule.
- **E312 - NORMALIZED ORDINARY-LINKED CONCENTRATION / DISPERSION MODEL** - RESEARCH RETAIN signal/model; production E278 selector unchanged.
  - Summary: retain the normalized geometry model as stronger causal evidence, not the exact 16/3 thresholds as production law. Do not continue canonical threshold tuning. Reopen only with layout-stable integration or a predictive cost model derived from held-out geometry.
- **E313 - PHASE-MAP ROUTE-STATE ECONOMICS SIGNAL** - RESEARCH RETAIN signal; REJECT exact gate; no production change.
  - Summary: Durable result: state orientation/history is a real map-economics dimension, but endpoint labels are not enough. Next phase-map predictor should distinguish profitable canonical Entropy->Other->Structured geometry from harmful OOD Entropy->Structured geometry using mapper-visible transition positions / length-weighted state occupancy rather than phase-count thresholds.
- **E314 - GENERALIZED PHASE-MAP ROUTE-COMPOSITION ECONOMICS** - RETAIN in `simulated-direct-phase-map-mature`; no propagation to other backends.
  - Summary: integrate the acceptance policy into `jessesort_simulated-direct_phase-map-mature_probe-route_adj-adaptive-buffer.h`. Keep mapper/boundary policy unchanged. Do not silently reuse the gate for physical/indexed; E307-E309 showed backend economics differ. Next reopening should test type-domain sensitivity or learn a cheaper/more continuous work predictor, not tune 55/30 on canonical workloads.
- **E315 - PHASE-MAP TYPE-DOMAIN ECONOMICS BOUNDARY** - RETAIN validation-domain guard; no new generic type heuristic.
  - Summary: E314's route-composition economics apply only to the domain actually validated in E314 (integral T <=8 B with default `std::less<T>`). Other type/comparator domains preserve pre-E314 mature execution of any semantically valid map until a causal type-aware cost model exists. The affected validated-domain TU emits byte-identical `.text` before/after the guard; 100k x100 canonical safety completed all 14 rows. CPU: Intel Xeon Platinum 8573C; GCC 14.2.0-19.
- **E316 - E299 HIGH-ENTROPY DIRECT-SCATTER ADOPTION REPLAY** - REJECT production integration / close E299-CAP448 as an active lead.
  - Same two 500-trial E299 validation seeds now cross over decisively: seed1 10k ~1.054x / 100k ~1.059x production; seed2 ~1.046x / ~1.064x. Fresh 200-trial CAP448/480/512 screens are all ~1.05x production at 100k, ruling out fixed-headroom choice as the explanation.
- **E317 - SIMULATED-INPLACE ONE-MOVE CYCLE RECONSTRUCTION** - RESEARCH RETAIN mechanism / no production change.
  - Summary: the mechanism is real but a production `sizeof(T)` dispatch would be overfit. Width crossover is non-monotonic around 128-224 B and some larger mixed rows remain cache/layout sensitive. Preserve E317 as reconstruction research; next step, if revisited, should improve locality with bounded block/cycle staging or derive a move-cost/locality signal independent of benchmark labels and record-size thresholds.
- **E318 - SIMULATED-INPLACE WIDE-RECORD CYCLE ROUTE DISTRIBUTION AUDIT** - RETAIN one-move cycle leader for `sizeof(T) >= 192` in `simulated-inplace` and its direct descendant. Same-process whole-sort A/B was strongly order-sensitive, but independent reconstruction-only distributions were favorable across all tested 192-byte shapes.
  - Treat 192 B as a pragmatic extreme-separating boundary, not a universal crossover.
- **E319 - ROUTINE VARIATION ROSTER + 10K/100K TIMING CHECKPOINT** - RETAIN repository cleanup / benchmark-roster reduction.
  - Summary: Cleanup: benchmark registry/includes reduced to the 7 routine headers; routine Makefile no longer compiles experimental TUs; indexed misleading-indentation warning fixed; experimental headers remain preserved and documented rather than deleted.
- **E320 - ISOLATED-PROCESS BENCHMARK HARNESS** - RETAIN benchmark methodology fix; no sorting-algorithm change.
  - Summary: Validation: clean GCC build; direct multi/default selection rejection; full 8-process 1k x2 smoke completed and aggregated successfully.
- **E321 - CLEAN BENCHMARK FOLDER + FRESH ISOLATED 10K DEFAULT SNAPSHOT** - RETAIN repository cleanup / timing checkpoint; no sorting-algorithm change.
  - Summary: Random no longer shows a frozen-specific advantage: physical 153.395 us, frozen-single 153.481 us, indexed 153.313 us; simulated measured slower in this session at 191.602 us, so absolute/cross-session timing shifts should not be compared to E320 without same-session controls.
- **E322 - FRESH ISOLATED 100K DEFAULT SNAPSHOT** - TIMING CHECKPOINT ONLY / now provisional under E323 methodology audit; no sorting-algorithm change.
  - Summary: Treat E322 as useful large-effect historical evidence, not as the final post-audit performance baseline.
- **E323 - BENCHMARK CACHE-INFLUENCE REVIEW + RETROSPECTIVE AUDIT PLAN** - METHODOLOGY HARDENING / AUDIT PLANNING ONLY; NO PERFORMANCE EXPERIMENT RUN.
  - Summary: No algorithm revalidation was started in E323. The first future timed work is Gate 0: verify the corrected harness itself under multiple recorded child orders/warmup conditions before auditing retained sorting decisions.
- **E324 - FIRST CORRECTED-METHODOLOGY DEFAULT TIMING BASELINE** - TIMING CHECKPOINT / benchmark infrastructure fix confirmed; no sorting-algorithm change.
  - Summary: This supersedes E321/E322 as the routine timing provenance baseline for future audit work.
- **E325 - RETROSPECTIVE AUDIT BATCH 1: HIGH-ENTROPY CODEGEN RETAINS** - PARTIAL TIER-1 AUDIT COMPLETE; E260/E263 REVALIDATED, E261 REVERSED, E267 DOWNGRADED TO TIMING-NEUTRAL.
  - Summary: Audit methodology lesson: a single isolated process can still suffer host/scheduler tails large enough to swamp a 1% codegen decision. For close retained-lineage audits, use distributions of independent process medians (and affinity when useful diagnostically), not one child median alone.
- **E326 - E320+ RETROSPECTIVE AUDIT: E271 FIRST-EIGHT PROOF-ELIMINATION ROUTER** - REVALIDATED; no production change.
  - Summary: keep the full E271 tree in `simulated-direct`. Its old timing evidence was susceptible, but the current mechanism itself survives corrected isolation.
- **E327 - E283 / E314 PHASE-MAP ECONOMICS RETROSPECTIVE AUDIT** - REVALIDATED current mature mapping mechanism and E314 safety gate; no production change.
  - Summary: E283's mature range execution remains a real high-upside mechanism but still needs economics control. E314's route-composition gate reproduces its intended safety/upside tradeoff under corrected isolation and is retained. Exact 55/30 percentages remain a coarse operating point, not an optimum claim.
- **E328 - RETROSPECTIVE AUDIT: E121 / E161 SIMULATED RECONSTRUCTION MICRO-RETAINS** - E121 REVALIDATED CAUSALLY; E161 REVALIDATED CAUSALLY; no production change.
  - Summary: E117 is not closed by E328. Newer specialized low-cardinality routes can intercept the integral duplicate-heavy regimes that originally justified adj-equality reuse, so its next audit must first establish current reachability rather than benchmark a mechanism that may not execute. E181/E183 remain pending.
- **E329 - RETROSPECTIVE AUDIT BATCH 5: E117 / E181 / E183** - E117 REVALIDATED ON REACHABLE PATIENCE INSERTION; E181 REVALIDATED; E183 REVALIDATED WITH SMALL-N TRADEOFF DOCUMENTED. No production change.
  - Summary: Audit lesson: for mechanisms that deliberately alter run geometry, insertion-only microbenchmarks can have the wrong sign relative to whole-sort cost. Use the narrowest causal phase that still includes the mechanism's intended downstream consequence.
- **E330 - RETROSPECTIVE AUDIT BATCH 6: E111 / E112 / E115 MERGE POLICIES** - E111 REVALIDATED AS COMMON ADJACENT POLICY; E112 REVALIDATED CAUSALLY; E115 AUDIT-NEUTRAL / KEEP ENDS-BASED CONSOLIDATION. No production algorithm change.
  - Summary: retain E111 adjacent scheduling and E112 adaptive probing. Keep E115 ends-based common driver primarily as a consolidation/maintenance choice; downgrade the historical broad performance claim to audit-neutral. Record the ends driver's duplicated two-run ordinary merge kernel as a future micro-optimization candidate, not an audit reversal.
- **E331 - RETROSPECTIVE AUDIT BATCH 7: E225 / E232 / E233 DIRECT STRUCTURAL ROUTES** - E225 REVALIDATED; E232 REVALIDATED; E233 REVALIDATED. No production change.
  - Summary: the three direct-route mechanisms remain foundational current architecture. No historical replay beyond the current-code toggles is warranted. Their original exact paired ratios should not be treated as modern baselines, but the architectural retains are decisively valid under the corrected methodology.
- **E332 - PRE-E320 AUDIT: MODERATE-CARDINALITY VALUE->PILE CACHE** - E142 REVALIDATED; E119 SUPERSEDED BY E142; NO PRODUCTION CHANGE.
  - Summary: Methodology note: an initial concurrent base/cache process screen was discarded because simultaneous execution introduces CPU contention. Final evidence uses sequential balanced mode order and fixed CPU affinity.
- **E333 - E216-E218 NOALLOC-LOW-RUN CORRECTED-ISOLATION AUDIT** - E216 REVALIDATED WITH OVER-ADMISSION CAVEAT; E217/E218 REVALIDATED; no production change.
  - Summary: E218: disabling early repeated-overlap routing is ~2.774x on 100k Sawtooth and ~2.034x at 1m, with tight process-level replication.
- **E334 - RETROSPECTIVE AUDIT: E164-E166 HIGH-ENTROPY FOUNDATIONS** - E164 REVALIDATED; E165 REVERSED/REMOVED FROM PRODUCTION; E166 REVALIDATED WITH DUPLICATE-CARDINALITY CAVEAT.
  - Summary: Production change: only E165 specialization routing changed. Shared backend ownership means the change propagates automatically to all current high-entropy owners.
- **E335 - RETROSPECTIVE AUDIT: E265 ANCESTOR / EQUALITY FILTER** - E265 REVALIDATED; RETAIN specialized exact-equality cleanup for integral/default-less high-entropy path.
  - Summary: Discarded evidence: a separately compiled control-vs-candidate whole-sort canonical sweep moved semantically unaffected rows by implausibly large amounts, including Rotated/OrganPipe. Those ratios are excluded from the decision and raw files are not banked. This reinforces the E328/E330 audit rule that micro-retains should be judged by the narrowest causal same-source toggle, not whole-header binary comparisons.
- **E336 - RETROSPECTIVE AUDIT: E266 DEPTH-LIMIT HEAPSORT FALLBACK** - REVALIDATED ON PRODUCTION-REACHABLE FALLBACK SLICES; NO PRODUCTION CHANGE.
  - Summary: Caveat: 100k MixedPhase3 is a real local loss for the outlined heap, but the fallback work is tiny relative to the whole sort (about 19 us median across roughly 20 fallback calls on fallback-bearing trials). The retained heap is materially better on the weighted production-reachable fallback population and remains the correct common cold escape. No attempt was made to add a workload- or size-specific heap selector.
- **E337 - RETROSPECTIVE TIMING AUDIT CLOSURE + PINNED CELL-ISOLATED BASELINE** - CURRENT ROUTINE-LINEAGE AUDIT COMPLETE; BENCHMARK METHODOLOGY HARDENED; NO NEW SORTING-ALGORITHM CHANGE IN E337.
  - Summary: Closed the post-E320 timing audit around pinned, cell-isolated benchmarking; this became the authoritative baseline methodology later strengthened by E364 provenance/null-control rules.
- **E338 - NOALLOC DENSE NEAR-OVERLAP GATE REFINEMENT** - RETAIN.
  - Summary: four independent pinned 10k screens reduce E333-like G40/G60 dense-overlap failures to ~0.737x/~0.701x current, while canonical MixedDirectionRuns/BlockSorted/OrganPipe remain ~0.997x/~0.995x/~1.000x. A broader randomized run corpus initially moved ~0.9%, but explicit route instrumentation showed the new gate fired 0/4,800 OOD trials, proving that movement was process noise on semantically unchanged execution.
- **E339 - E269 MONOTONE-PREFIX ENTROPY RECOVERY, POST-AUDIT REVISIT** - REJECT production integration; recovery signal remains valid.
  - Summary: restore `simulated-direct` production header exactly. Do not keep tuning call placement/branches inside the template header. Reopen E269 only behind a genuinely layout-stable compilation boundary or as part of a larger router/TU refactor whose layout is intentionally re-baselined.
- **E340 - SPECIALIZED ALL-EQUAL HIGH-ENTROPY LEAF ESCAPE** - RETAIN.
  - Summary: Retained the nearly-free all-equal escape around E166. E365 later re-audited it under the integrity rules: ~1.9% win on 100k MixedPhase12, smaller Noise5 gain, and no >1% stable regression.
- **E342 - ALLOCATION-FREE DIRECT-ROUTE / MOVE-ONLY CONTENDER** - RETAIN as a distinct experimental variation; current `noalloc-low-run` routine pillar unchanged.
  - Summary: Established the allocation-free/move-only direct-route contender and localized its remaining deficits to rearrangement/merge primitives rather than routing. This branch became the basis for E359/E366/E367/E368/E369.
- **E343 - ALLOCATION-FREE ZIGZAG CYCLE-LEADER PARITY GATHER** - REJECT; E342 recursive rotate gather restored exactly.
  - Summary: reducing move complexity alone is insufficient; cycle-leader discovery/index arithmetic costs more than the recursive rotate tree for inexpensive values. Do not introduce size-specific cycle tables or canonical-n special cases. The Alternating primitive gap remains open, but the active testing queue moves next to E342 long-run in-place merge cost.
- **E344 - ALLOCATION-FREE LONG-RUN OVERLAP-WINDOW TRIMMING** - RETAIN via E367 integration. The original E344 evidence was later superseded by E364/E367 raw-audited same-executable testing; its mechanism is now banked in the maintained noalloc-direct contender.
  - Summary: Overlap-window trimming was initially uncertain, then re-audited and ultimately retained via E367. Current decision evidence is the raw-audited E367 integration, not the original E344 timing summary.
- **E345 - ALLOCATION-FREE SMALLEST-ADJACENT NATURAL-RUN MERGE SCHEDULE** - REJECT; E342 pairwise merge schedule restored exactly.
  - Summary: the real schedule gain is too small to justify O(r^2) descriptor selection and extra code in the Rust-contract contender, and it does not address the two-run OrganPipe movement bottleneck. Keep the simpler balanced pairwise schedule; future long-run work needs a better in-place merge primitive, not merge-order tuning.
- **E346 - PHASE-MAP LARGE-N OUTLINING / SMALL-DIRECT LAYOUT BOUNDARY** - REJECT; mature phase-map header restored exactly.
  - Summary: do not use a noinline/outlined phase-map boundary as the crossover fix. The integration/code-layout sensitivity is real and can swamp modest wrapper savings. Future phase-map work should target mapper-visible economics/proof-based bypasses while preserving the current compiled integration shape.
- **E347 - PHASE-MAP WRAPPER ACTIVATION / COST ATTRIBUTION PROFILE** - DIAGNOSTIC RETAIN / roadmap correction; no algorithm change.
  - Summary: Consequence: E341 PRIORITY 1 was partly based on misattributed whole-binary timings. There is no evidence that a new phase-map profitability bypass is needed for stationary BlockSorted/Rotated/MixedDirectionRuns; the current gate already bypasses them. The remaining issue is code-layout identity between the phase-map wrapper and direct fallback, which E346 showed is dangerous to attack with naive outlining. Preserve current E314 mapper economics and move performance priority back to causal backend/direct work unless new OOD mapped false positives are found.
- **E348 - E115 ORDINARY TWO-RUN MERGE-KERNEL DEDUPLICATION** - REJECT production refactor; keep the current ends-driver inline ordinary merge body. Documentation/TODO priorities updated before testing to mark direct/structured work first, the Rust-contract branch active but primitive-gated, phase-map as an important deferred dedicated revisit, and high-entropy backend work as diminishing-return/deferred.
  - Summary: do not retain or propagate this source-level deduplication. The helper and inline body remain semantically equivalent, but changing their integration shape perturbs GCC code generation enough to violate the >1% guardrail on important ordinary rows. Keep E115's duplicated ordinary body as intentional code-layout ownership rather than dead duplication. Future cleanup requires a genuinely layout-stable compilation boundary or a broader intentional rebaseline, not another header-level helper substitution.
- **E349 - REPEATED ASCENDING-PATTERN DIRECT MATERIALIZATION** - REJECT common `simulated-direct` integration; RETAIN mechanism/evidence as a specialist direct-route lead. Production header restored exactly.
  - Summary: Repeated-ascending direct materialization was algorithmically strong but unsafe to bank in the monolithic common header because unrelated GCC layout moved materially. Preserve as a specialist/layout-stability lead.
- **E350 - EXACT TWO-ASCENDING-RUN ROTATION SPECIALIZATION** - REJECT common `simulated-direct` integration; RETAIN mechanism/evidence as a specialist/layout-stable lead. Production direct header restored to E349 state.
  - Summary: Exact two-ascending-run rotation showed a strong specialist win but was not promoted because common-header integration perturbed unrelated code. Preserve for a layout-stable boundary or specialist variation.
- **E351 - LAYOUT-STABLE SPECIALIST COMPOSITION / LARGE-ROTATED WRAPPER** - RESEARCH RETAIN architectural finding; preserve the separately named experimental large-Rotated wrapper, but REJECT promotion/routine benchmarking. No maintained common-header change.
  - Summary: Confirmed that separate-wrapper composition can isolate specialist behavior better than common-header edits, but promotion was not justified. Preserve the architectural finding for future layout-stable integration.
- **E352A - E142 SMALL-N MODERATE-CARDINALITY CACHE ADMISSION REFINEMENT** - REJECT production classifier refinement; CLOSE the E142 10k/~128-cardinality TODO as diminishing-return. E142 remains unchanged.
  - Summary: retain current E142 gate unchanged and remove this narrow edge from active optimization priorities. Reopen only if a broader cardinality mechanism can reuse the same second-stage estimate.
- **E352B - LOW-RUN / ADAPTIVE REVERSE-DISJOINT MERGE ROUTER** - REJECT policy change; retain diagnostic finding. Production common merge driver restored unchanged.
  - Summary: do not add either the hard low-run gate or the adaptive state machine. The diagnostic confirms a real asymmetry-high-run reverse-disjoint checks are wasted on high entropy but useful on coherent run geometry-but the comparison cost is too small to matter end-to-end where it misses, while each structured hit can avoid a full merge. Keep the simple always-on reverse-disjoint fast path. Revisit only if merge-phase-local profiling later shows the endpoint comparison itself has become material.
- **E353 - NATURAL-RUN RANGE-SEPARABILITY SHORTCUT** - REJECT; no production change.
  - Summary: reject convex endpoint-range decomposition as the next direct/structured mechanism. A future version would need finer sub-run boundary information rather than trying to recover it from already-coalesced natural runs.
- **E354 - SMALL-N E183 THREE-PILE MONOTONE-PREFIX EXCEPTION** - RETAIN for `simulated-direct` and inherited `simulated-direct-phase-map-mature`; other owners intentionally unchanged pending owner-specific validation.
  - Summary: Retained the small-N E183 three-pile monotone-prefix exception for simulated-direct and phase-map. E365 later re-audited the true effect at ~2–3% on the intended 1%-swap stress with neighboring cases near neutral.
- **E355 - E166/E340 LOW-CARDINALITY LEAF ROUTING** - REJECT; no production change. Close broader low-cardinality routing around the 18–32-element E166 leaf unless a materially different finish primitive appears.
  - Summary: E340's all-equal early return remains worthwhile because recognition is almost free once equality is proved, but extending that logic to small nontrivial cardinalities spends more on recognition/materialization than E166 spends sorting the leaf. Do not continue threshold sweeps around 2/4/8 distinct.
- **E356 - EXACT-TWO-NATURAL-RUN ORIENTATION-AWARE MERGE** - SUPERSEDED / NOT RETAINED after E357. The exact-two-run branch materially improved OrganPipe (~0.59x at 10k, ~0.72x at 100k direct-owner) by fusing descending-run normalization with the only merge, but the trigger was too topology-specific to accept without a broader low-run result.
  - E357 tested the same idea as a general <=8-run regime and disproved generality. Preserve E356 only as evidence that orientation fusion can be excellent for some two-run overlap geometries; production reverted to the pre-E356 mature reverse-then-merge behavior.
- **E357 - GENERAL <=8 NATURAL-RUN ORIENTATION-AWARE DIRECT MERGE** - REJECT; E356 production change reverted. Candidate generalized orientation fusion to every E225 case with 2-8 natural runs and any descending run: the first adjacent pass read runs in logical ascending direction, then the mature merge driver finished the resulting 1-4 runs.
  - Summary: Official confirmation still showed the attractive OrganPipe effect (0.689x at 10k, 0.786x at 100k) while MixedDirectionRuns was neutral (~1.005x/~0.998x), demonstrating why canonical-only evidence would have been misleading. Decision: do not retain an exact-two or <=8 orientation-aware exception; keep the pre-E356 merge behavior unless a future mechanism has broad structural evidence.
- **E358 - RUST-CONTRACT BOUNDED STACK-BUFFER MERGE** - SUPERSEDED / PROMISING SIGNAL; production header remained E342. E360 is the stronger descendant.
  - Summary: Top-level stack buffering exposed a real interleaved-run win but harmed BlockSorted by hiding disjoint substructure. Superseded by deeper leaf buffering in E360/E362 and ultimately E366.
- **E359 - RUST-CONTRACT PAGE-SIZED LOCAL TWO-LANE DEINTERLEAVE** - RETAIN in `experimental/jessesort_noalloc-direct_probe-route_inplace-adaptive_run-reclaim64_move-only.h`.
  - Retained the page-sized local two-lane deinterleave in `noalloc-direct`; canonical Alternating improved from 37.146->6.189 us at 10k and 360.495->73.459 us at 100k in the first decision run.
- **E360 - RUST-CONTRACT RECURSIVE-LEAF BUFFERED IN-PLACE MERGE** - PROMISING / MORE ANALYSIS NEEDED (corrected by E361). Tested a materially different descendant of E358: retain the mature binary-partition/endpoint recursion to expose ordered or reverse-disjoint substructure, then use one fixed 8 KiB move-only stack buffer only when a genuinely overlapping recursive leaf fits. A second prototype forced at least one structural split before buffering so the scratch path could not simply replace the top-level merge.
  - Recursive-leaf buffering strongly improved MixedDirectionRuns (~0.756x/0.767x at 10k/100k) but initially regressed 10k BlockSorted ~8.9%; E361 reopened it after mapping the crossover.
- **E361 - RECENT REJECTION AUDIT + E360 SIZE-CROSSOVER REOPEN** - RESEARCH RETAIN / E360 remains PROMISING; no production code change.
  - Size sweep showed the BlockSorted penalty falling from ~7–12% at 10k–30k to ~0.3% at 100k and neutral/favorable near 150k, while MixedDirectionRuns stayed strongly favorable.
- **E362 - DESCENDING-RUN-GATED RECURSIVE-LEAF BUFFERED MERGE** - PROMISING / MORE ANALYSIS NEEDED; preserved as a separate experimental header, not promoted into the E359 production Rust-contract contender.
  - Descending-run presence cleanly separated target long-run cases (MixedDirectionRuns/OrganPipe) from BlockSorted/Rotated in the profiled direct-route geometry.
- **E363 - PROMISING-REJECTION REVISIT: E344 + E362** - SUPERSEDED / EVIDENCE SYNTHESIS INVALIDATED BY E364; no production change.
  - Historical synthesis invalidated: the consolidated table mixed timings/ratios from different cohorts, and the original paired buffers introduced avoidable cache/process asymmetry.
- **E364 - EVIDENCE-INTEGRITY REPAIR + E363 RAW REBUILD** - RESEARCH RETAIN / PROCESS GUARDRAIL RETAIN; no production algorithm promotion in this experiment.
  - Established the evidence-integrity hard gate: no mixed-cohort rows, preserve raw trials/seeds/metadata/hashes/derivation scripts, validate suites before aggregation, and treat inactive-row movement as contamination.
- **E365 - E354 / E340 RETAINED-CHANGE EVIDENCE-INTEGRITY AUDIT** - AUDIT PASS for both retained changes; no production algorithm change.
  - E340 result: 10k is effectively neutral (Random 1.001x, Noise5 0.998x, Noise10 1.000x, MixedPhase3 0.999x, MixedPhase12 1.001x). At 100k: Random 1.001x, Noise5 0.992x, Noise10 1.003x, MixedPhase3 1.005x, MixedPhase12 0.981x.
- **E366 - E362 PRODUCTION PROMOTION / RUST-CONTRACT INTEGRATION ACCEPTANCE** - RETAIN / PROMOTE.
  - Promoted the descending-run-gated recursive-leaf buffer into `noalloc-direct` after repeated isolated matched-seed cohorts; MixedDirectionRuns was ~0.928x at 10k and ~0.806x at 100k.
- **E367 - E344 WHOLE-SORT INTEGRATION / TOP-LEVEL OVERLAP TRIM PROMOTION** - RETAIN in the maintained noalloc-direct Rust-contract contender.
  - Promoted E344 top-level overlap trimming into `noalloc-direct` using identical-executable runtime-toggle cohorts with deterministic matched seeds and repeated process pairs.
- **E368 - ALLOCATION-FREE E232 SPARSE-DISORDER DIRECT EXECUTION** - RETAIN / PROMOTED into the maintained Rust-contract `noalloc-direct` contender.
  - Promoted the allocation-free sparse-disorder direct route. Three-cohort evidence: Noise5 ~0.699x at 10k/~0.816x at 100k; Noise10 ~0.941x/~0.998x, with Random/mixed-phase controls near neutral.
- **E369 - RUST-CONTRACT COMPARATOR-PANIC / UNWIND PRESERVATION HARDENING** - RETAIN.
  - Hardened comparator-panic/unwind behavior without a stable >1% regression versus E368; earlier slower RAII prototype was discarded.
- **E370 - ROUTINE ROSTER REFRESH / CURRENT NOALLOC-DIRECT PROMOTION** - RETAIN roster change. Promote the E369 `noalloc-direct` Rust-contract contender into the seven-JesseSort routine benchmark set; demote the older `noalloc-low-run` control to non-routine status without deleting it. The maintained noalloc-direct header now lives at `include/jessesort/` with an experimental-path compatibility wrapper.
  - Promoted current `noalloc-direct` into the routine roster and demoted `noalloc-low-run` at that time; later E458 restored `noalloc-low-run` as an eighth routine specialist.
- **E371 - E349 REPEATED-ASCENDING SPECIALIST RECOVERY / GCC LAYOUT AUDIT** - RETAIN in maintained `simulated-direct`; inherited by `simulated-direct-phase-map-mature`. The earlier layout-blocked interpretation was corrected: unrelated code-layout changes are not a rejection criterion unless they produce a validated slowdown.
  - Results: direct Sawtooth ~0.392x (10k) / ~0.303x (100k); phase-map Sawtooth ~0.357x / ~0.266x. Complete direct 14-input acceptance found no validated >1% regression after seven-cohort rechecks (10k Random%25 median seed-paired 1.0083x). Phase-map propagation likewise cleared its only >1% flag (10k MixedDirectionRuns -> 1.0053x over seven cohorts).
- **E372 - POST-E344 REDUNDANT ROOT ORDERED-CHECK ELIMINATION** - REJECT; production noalloc-direct restored exactly. Tested skipping the ordered-boundary comparison immediately after E367/E344 overlap trimming had already proved the root pair crosses.
  - Three independent pinned 500-trial seed-aligned cohorts were neutral: 10k MixedDirectionRuns 1.0057x, OrganPipe 0.9965x, BlockSorted 1.0027x; 100k 0.9992x/1.0016x/0.9976x. The saved comparison is real but too small to justify added state/branching.
- **E373 - TWO-LANE DIRECTION / EQUAL-PREFIX REUSE** - RETAIN in maintained `noalloc-direct`.
  - Decision evidence, fixed CPU 0, separate control/candidate processes, 2 warmups + 500 trials. Ordinary canonical-orientation Alternating is effectively neutral: repeated process median candidate/control ~1.009x at 10k and ~1.001x at 100k (within the calibrated fast-cell noise / 1% guardrail).
- **E374 - RECONSTRUCTED-RUN MERGE-GEOMETRY / POWERSORT REALIZATION AUDIT** - REJECT PowerSort restoration; RETAIN diagnostic/roadmap finding. Length-only simulation found large adj-vs-optimal alphabetic merge-cost gaps on structured reconstructed runs (roughly 37–45% on 5–10% noise, 20–24% BlockSorted, and 24–42% mixed-phase), while the existing PowerSort tree was usually within ~1–3% of the oracle.
  - Runtime result: the dormant PowerSort materializer did not reliably monetize that theoretical advantage. Broad skew gating regressed noise/MixedPhase; a compact-run gate was neutral/unstable under 500-trial causal rechecks (including 100k MixedDirectionRuns process ratios spanning ~0.86–1.23x, median ~1.02x). Keep adjacent production scheduling unchanged; future work should reduce irregular-tree realization/source-ownership overhead while preserving mature merge kernels.
- **E375 - ADAPTIVE BEST-2-OF-3 MERGE SCHEDULER FAMILY** - RETAIN selectively in maintained simulated-direct: sliding best-2-of-3 with stride 3 only for n>=32768, 8-64 reconstructed runs, and max/mean>=1.5; broad replacement, streaming reuse-2, stride 1/2, and stride-3 immediate absorb rejected.
  - Key evidence: 100k stride-3 repeated 500-trial process cohorts gave a large MixedDirectionRuns win (~0.729x median). Broad stride-3 regressed MixedPhase3 ~2-4%, motivating the compact+imbalanced gate. E377 later instrumented the retained gate directly: across 100 canonical 100k trials it fired on 54% of MixedDirectionRuns and 6% of Random%25, and 0/100 on Noise5/10, Sawtooth, BlockSorted, MixedPhase3/12, Random, Alternating, OrganPipe, and Rotated. Therefore earlier BlockSorted/Sawtooth gate-table movement must be treated as null-control/process movement, not evidence of E375 execution.
- **E376 - E375 BEST-2-OF-3 PROPAGATION TO ORDINARY SIMULATED** - REJECT propagation; maintained ordinary `simulated` remains on its E115 adj-pair scheduler. The exact E375 size/run-count/max-mean gate and stride-3 tree were transplanted behind a runtime toggle using the ordinary simulated reconstruction/materialization machinery.
  - Canonical 10k/100k x500 decision cells and repeated 100k process cohorts show no robust retainable gain. Five-ish completed 100k cohorts center at MixedDirectionRuns ~0.994x candidate/control (range ~0.981-1.066x), BlockSorted ~0.994x (completed range ~0.989-1.003x), and Sawtooth ~1.002x (~0.990-1.010x). Initial screens produced much larger swings, including a transient ~1.27x BlockSorted regression, while the 10k gate-inactive nulls also moved materially, confirming host/process sensitivity.
- **E377 - E375 TREE REALIZATION / FIXED-CAPACITY MATERIALIZER AUDIT** - REJECT; maintained `simulated-direct` E375 implementation remains unchanged.
  - Gallop result: isolated repeated 100k cells did not show a stable trigger improvement over the retained trigger 7; candidate trigger effects moved across cohorts and inactive controls, so no kernel-threshold change is justified. Same-process always-second sweeps were explicitly discarded after gate-inactive 10k rows showed impossible large speedups from cache/order contamination.
- **E378 - INDEXED OWNER-SPECIFIC BEST-2-OF-3 GEOMETRY / STRIDE-3 GATE** - REJECT; indexed production remains on adj-pair scheduling. Owner-specific geometry shows real alphabetic-tree waste (100k adjacent/oracle ~1.14 MixedDirectionRuns, ~1.24 BlockSorted, ~1.41-1.42 Noise), but a compact strongly-imbalanced stride-3 gate did not produce a stable >1% win.
  - Key evidence: gate activated only MixedDirectionRuns/BlockSorted in the sampled canonical geometry. Three isolated 500-trial cohorts gave median candidate/adjacent ratios: MDR 1.006x/1.010x at 10k/100k; BlockSorted 0.997x/0.991x. Preserve `e378_indexed_geometry.cpp` and `e378_indexed_cell.cpp` as owner-specific scheduler diagnostics.
- **E379 - PHYSICAL OWNER-SPECIFIC SLIDING BEST-2-OF-3 MERGE GATE** - RETAIN in maintained `physical` (`jessesort_physical_probe-route_adj-adaptive-buffer.h`).
  - Decision evidence: five isolated 500-trial cohorts with process order reversed on alternating cohorts. Median candidate/adjacent: BlockSorted 0.9868x at 10k and 0.9966x at 100k; MixedDirectionRuns 0.9568x and 0.9726x; inactive Sawtooth 0.9982x and 1.0035x.
- **E380 - MATURE PHASE-MAP GALLOPING SCHEDULE REVALIDATION** - RETAIN in maintained `simulated-direct-phase-map-mature` only: x2 stable-gap galloping now caps at 4k instead of 8k; reset+two-confirmation logic, route states, late boundary placement, mapped executor, merge, and E314 economics are unchanged. Other phase-map owners intentionally keep x2/8k pending owner-specific validation.
  - Key evidence: canonical 100k x500 paired cap4/current results were MixedPhase3 1090.71/1193.96 us = 0.9135x and MixedPhase12 1117.28/1202.10 us = 0.9294x, with identical accepted-map counts; three balanced 1000-trial paired cohorts confirmed ~0.901-0.918x / ~0.923-0.934x. Mean probes rise by ~10-11 observations, but improved boundary sampling repays that cost. Full 14-input 10k/100k x100 maintained-header smoke passed; canonical activation remained concentrated on MixedPhase3/12.
- **E382 - MATURE PHASE-MAP GALLOP-GROWTH SCHEDULING REVISIT** - REJECT; maintained `simulated-direct-phase-map-mature` remains on E380 x2 stable-gap growth capped at 4k.
  - Decision evidence: x3 was the only plausible candidate. Three additional isolated 100k x500 cohorts with alternating process order gave x3/x2 MixedPhase3 ratios 1.0553x, 1.0396x, 1.0162x (median 1.0396x), consistently violating the >1% regression guardrail.
- **E383 - STATE-DEPENDENT LEADING-STRUCTURE PHASE-MAP SCHEDULING** - RETAIN in maintained `simulated-direct-phase-map-mature`.
  - Decision evidence: three independent pinned 100k x500 cohorts with alternating process order gave candidate/E380 MixedPhase3 median-time ratios 0.932x, 0.957x, and 0.943x (median ~0.943x). Accepted-map counts matched exactly in every cohort; mean probes rose from ~31.1-31.6 to ~35.9-36.5, so the gain comes from better leading-boundary resolution rather than changed acceptance.
- **E384 - INDEXED MERGE-TREE REALIZATION AUDIT** - REJECT all candidates; maintained indexed production remains on E115 adj-pair scheduling.
  - Mixed-buffer realization: built the same near-optimal PowerSort alphabetic tree but allowed adjacent children resident in opposite ping-pong buffers to merge directly by overlapping safely into the left buffer backwards or the right buffer forwards; added ordered and reverse-disjoint fast paths. This removes the conceptual requirement to normalize both children into one source buffer before an irregular merge.
- **E385 - INDEXED RECONSTRUCTION / FIRST-MERGE FUSION** - SUPERSEDED / REVERTED by E386; retained only as mechanism evidence.
  - Decision evidence: 100k OrganPipe 322.069 us -> 207.299 us = 0.6436x. The 10k gate is inactive; worst official 10k ratio was Sawtooth 1.0094x, below the >1% investigation threshold.
- **E386 - E385 STRUCTURAL-GENERALIZATION / OVERFITTING AUDIT** - REJECT E385 production gate and revert indexed header to E384 behavior.
  - Generalization result: fusion is genuinely strong near balanced overlapping peak/valley geometry (Peak p=.5 ~0.678x; Valley p=.5 ~0.713x; overlapping two-run p=.5 ~0.677x), so the cross-stage mechanism itself is real. However, the exact same gate activates 500/500 on Peak p=.10/.90 and Valley p=.10/.90, where ratios regress to ~1.070x/~1.156x and ~1.049x/~1.091x.
- **E387 - INDEXED FUSION ECONOMICS + CODEGEN-STABILITY AUDIT**
  - Followed E386 by deriving a non-benchmark-specific profitability model for indexed reconstruction/first-merge fusion. Neutral structural sweeps show two necessary economics: each of the two contiguous monotone piles should hold at least ~25% of n, and at least ~50% of the smaller pile should participate in value-range overlap.
- **E388 - ADAPTIVE CROSS-BUFFER MERGE / IRREGULAR-TREE REALIZATION REVISIT** - REJECT production change; preserve primitive/harness evidence.
  - Replaced E384's simple overlap-safe cross-buffer merge with mature adaptive forward/backward variants: ordered and reverse-disjoint fast paths plus galloping in both directions. In a merge-only 100k matrix, the best cross direction was commonly ~5-15% faster than normalize-then-use-the-mature-same-buffer merge on overlapping asymmetric pairs and dramatically faster on disjoint pairs; balanced fully overlapping pairs were roughly break-even/slightly adverse.
- **E389 - IMPLICIT MERGE ANCESTRY / LEAF-PARITY RECONSTRUCTION** - REJECT; production headers unchanged.
  - Decision-grade 100k x500 canonical targets after fixing the lookup artifact: Sorted+Noise(5%) 1333.19 -> 1301.76 us (~0.9764x), MixedDirectionRuns 650.178 -> 725.130 us (~1.1153x), BlockSorted 473.335 -> 685.020 us (~1.4472x). The apparent Noise5 benefit did not generalize: an independently seeded neutral sparse-noise family at the same 5% perturbation rate was 1322.97 -> 1322.77 us (~0.9998x) over 500 trials; independent 8% was ~1.0015x and 10% ~1.0006x.
- **E390 - SEQUENTIAL ENDPOINT-CARRY REASSOCIATION** - REJECT; production headers unchanged.
  - Geometry analysis predicted meaningful comparison-span reductions without changing full-buffer write-round count: endpoint-carry/current-adjacent merge-span medians were ~0.966 on 100k MixedDirectionRuns, ~0.946 on 100k Random%25, ~0.994 on 100k Sawtooth, and near 1.0 on 100k BlockSorted/Noise5 controls. A 120-trial paired end-to-end screen did not monetize this: the strongest apparent gains were sub-1%, while 10k Random%25 and MixedPhase12 initially moved >1% adverse.
- **E391 - E390 RANDOM%25 REVISIT / MERGE-SPAN ACCOUNTING AUDIT** - E390 rejection CONFIRMED; production headers unchanged.
  - Revisited why E390 geometry predicted endpoint-carry/current-adjacent ~=0.946 on Random%25. Important clarification: Random%25 is cardinality-25 random data, not 25%-random data; it consistently leaves ~45 reconstructed runs at both 10k and 100k.
- **E392 - TRUE ZERO-COPY CARRY / MIXED-BUFFER SEQUENTIAL REALIZATION** - PROMISING / MORE ANALYSIS NEEDED; production unchanged.
  - Corrected prototype preserves the mature global ordered-boundary scan where same-buffer runs can coalesce for free. An unconditional form exposed a repeatable Sawtooth regression; run-shape analysis showed Sawtooth's surviving runs are unusually uniform (100k largest run ~2.2% of n, CV ~0.17), where skipped carry traffic is too small to justify mixed-buffer complexity.
- **E393 - INDEXED TRUE ZERO-COPY CARRY / EXACT-HEADER RETENTION** - RETAIN in maintained `jessesort_indexed_probe-route_adj-adaptive-buffer_move-only.h`; no propagation yet.
  - Exact paired same-process diagnostic, identical inputs/randomized order, 10k/100k x500 over all 14 official inputs: no final cell regressed >1%. Representative 100k ratios after the corrected gate: MixedDirectionRuns 0.9850x, BlockSorted 0.9934x, Sorted+Noise10 0.9952x, MixedPhase3 0.9949x, Sawtooth 0.9962x, Random%25 1.0012x; 10k Random%25 1.0007x.
- **E394 - ZERO-COPY BEST-2-OF-3 SEQUENTIAL REALIZATION REVISIT** - REJECT; production remains E393.
  - Initial 100k screen showed an apparent ~7.5% BlockSorted win, but three independent x500 cohorts disproved it: candidate/control 0.9959x / 1.0104x / 1.0066x. MixedDirectionRuns was only mildly favorable across three x500 cohorts (0.9956x / 0.9892x / 0.9949x), while MixedPhase3 was consistently adverse (1.0296x / 1.0304x / 1.0285x).
- **E395 - E394 DEEP ANALYSIS: FIXED-DEPTH INTERIOR CARRY + RESIDENCY COST** - PROMISING / MORE ANALYSIS NEEDED; no production change.
  - New realization insight: an odd pairwise round does not have to carry only an endpoint. With an odd run count, any even-indexed run can be left unmatched while all others still form adjacent pairs.
- **E396 - E394/E395 DEEPER CARRY-REALIZATION ANALYSIS** - ANALYSIS / KEEP OPEN; production remains E393.
  - Limited 1/2/3-stage 3->2 tests still made MixedPhase3 ~1.5-3.6% slower in screens, so the stage-count penalty begins immediately rather than only after a long 3->2 cascade. A one-round linear objective `carry_saved - lambda*next_cross_volume` likewise had no stable lambda across the structured families.
- **E397 - PARETO / MULTI-ROUND RESIDENCY-AWARE INTERIOR CARRY** - ANALYSIS / DO NOT RETAIN; production remains E393. A strict one-round Pareto rule (meaningful skipped-write or cross-buffer improvement with no deterioration in the other dimension) still regressed 100k MixedPhase3 consistently by ~2.3–4.0% across 3x300 paired cohorts, proving one-round residency cost is too myopic.
  - Two-round endpoint-vs-largest-interior lookahead removes that specific failure pattern and can produce favorable cells, but results are not stable enough for retention: repeated cohorts oscillate between small gains/losses on MixedDirectionRuns, BlockSorted and MixedPhase12, with ~1% inactive/control movement still present.
- **E398 - SEQUENTIAL WHOLE-LEVEL / ORDERED-SUBTREE ELIMINATION AUDIT** - REJECT tested realizations; production remains E393.
  - Late ordered-subtree reprobe: E393's global ordered-boundary scan can disable itself after a low-yield early round, so tested re-enabling it once surviving run count falls to <=32/64/128/256. The <=64 form was decision-tested across three 100k x500 paired cohorts.
- **E399 - E393 ZERO-COPY RESIDENCY PROPAGATION TO SIMULATED-DIRECT** - REJECT propagation; maintained `simulated-direct` remains on E375 for its compact/imbalanced gate and the mature adj-pair driver elsewhere.
  - Key evidence: active 100k MixedPhase3 (zero-copy on ~84-93/100 sampled seeds) was neutral/slightly adverse in paired cohorts: 1.0049x, 1.0043x, 1.0019x. Inactive 100k Sorted+Noise(10%) executed zero-copy 0/300 sampled seeds yet regressed 1.0392x, 1.0471x, 1.0329x; outlining the resident helpers (`noinline,cold`) did not remove it (1.0419x, 1.0363x, 1.0247x).
- **E400 - E393 ZERO-COPY RESIDENCY PROPAGATION TO PHYSICAL** - REJECT propagation; maintained physical owner remains E379 stride-3 on its retained compact/imbalanced gate and mature adjacent merging elsewhere.
  - Scope/method: preserved E379 exactly and tested E393 resident-buffer odd-run zero-copy only after E379 declined ownership. Paired same-process A/B on identical canonical inputs with alternating order, 500 trials, seed offsets 0/1000/2000; flagged 10k BlockSorted was replayed at offsets 3000/4000/5000.
- **E401 - E380/E383 PHASE-MAP SCHEDULING PROPAGATION TO PHYSICAL** - PARTIAL RETAIN for experimental `physical-direct-phase-map-mature`: below the existing E308 complex-map threshold (n < 262144), use E383 `mapX2LeadStructuredCap3k`; at/above the threshold retain the owner's prior x2/8k mapper. Linked/inplace mature wrappers already inherit the maintained E380/E383 mapper through `sortWithFallback`, so no separate code change was needed for them.
  - Decision evidence: three pinned 100k x500 cohorts on AMD EPYC 9V74 gave lead-STRUCTURED/current MixedPhase3 ratios 0.8708x, 0.8942x, 0.8418x (accepted maps essentially unchanged: 478/477, 475/475, 462/463). The inactive Sorted+Noise(10%) sanity row accepted 0 maps for both schedules and ratios were 0.9890x, 0.9973x, 0.9940x. Two 200k x500 cohorts confirmed MixedPhase3 0.9622x and 0.9665x; MixedPhase12 remained rejected by the existing simple-map economics gate and was neutral/slightly noisy.
- **E402 - E354 SMALL-N THREE-PILE PROPAGATION TO ORDINARY SIMULATED** - RETAIN in maintained `simulated`; `simulated-direct` and its mature phase-map descendant already retained E354. Below 32768 elements, a perfectly nondecreasing first-64 probe producing exactly three piles now skips E183 valley rescue.
  - Decision evidence: AMD EPYC 9V74, CPU 0, identical runtime-toggle executable, separate pinned control/candidate processes, 2 warmups, eight independent 10k x500 cohorts with alternating process order. On the intended 1%-adj-swap stress, median process ratio was ~0.917x and median seed-paired ratio ~0.983x; neighboring Noise5/Noise10/MixedRuns controls centered ~0.999x/~0.998x/~1.004x by process medians. A one-shot full canonical timing pair showed impossible movement on inactive rows and was discarded as host/process contamination rather than used for retention.
- **E403 - E354 RANGE-ENGINE PHASE-MAP PROPAGATION ELIGIBILITY AUDIT** - NO CODE CHANGE / NOT APPLICABLE under the maintained phase-map workloads. The range-engine family only takes ownership for whole inputs n>=65536, while E354 itself is a small-subrange exception (subrange n<32768, exactly three probe piles, nondecreasing first 64).
  - Eligibility evidence: diagnostic instrumentation counted the exact E354 structural condition inside `detail_phase_range_engine.h`. On AMD EPYC 9V74, `simulated-phase-map-range` produced 0 eligible chunks in 500x100k MixedPhase3, 0/500x100k MixedPhase12, 0/100x1m MixedPhase3, and 0/100x1m MixedPhase12. These are the official mixed-phase workloads that actually exercise range phase mapping; the original 10k E354 stress cannot enter this owner because of its 65536 top-level minimum.
- **E404 - PRODUCTIVE PHASE-MAP ENTROPY-STATE EXECUTOR REUSE** - REJECT tested realizations; no production change. Tested a concrete productive-reuse mechanism in the maintained mature phase-map owner: when a mapped region was already classified `Entropy`, bypass `sortDirect` structural pre-route probing and enter the range engine main `sortImpl` directly. This uses mapper state as downstream routing evidence rather than retuning map spacing.
  - Broad reuse, AMD EPYC 9V74 CPU 0, identical executable and alternating process order: five independent 100k x500 cohorts centered at MixedPhase3 ~0.9979x candidate/control, MixedPhase12 ~0.9930x, and inactive Sorted+Noise(10%) ~0.9989x. Three 1m x100 secondary cohorts split: MixedPhase3 median ~1.0050x while MixedPhase12 was consistently favorable at ~0.9835x. The effect therefore did not generalize cleanly enough to retain broadly.
- **E405 - IDENTICAL-CODE HOST PROCESS-VARIANCE / BIMODALITY AUDIT** - METHODOLOGY RETAIN; no sorting-code change. CPU: AMD EPYC 9V74 80-Core Processor, fixed CPU 0, GCC repository benchmark binary, 2 warmups, canonical seeds. Twelve fresh 500-trial processes ran literally identical `simulated` code per audited cell.
  - Identical code on a pinned AMD EPYC host showed long-lived process-speed modes: 10k Reverse medians ranged 3.405–6.209 us across identical 500-trial processes, proving host-state bimodality independent of seed/input.
- **E406 - E371 VOLATILE SPECIALIST-TOGGLE CLEANUP** - SUPERSEDED by E410. Direct removal was rejected because it changed code partitioning and caused a large Sawtooth regression; E410 later removed the artifact safely by replacing it with a genuine repeated-pattern precondition that preserves favorable hot/cold code shape. Removal left the specialist logically always enabled but changed emitted code/layout. Six independent 500-trial alternating-order cohorts on AMD EPYC 9V74 showed a large active-path Sawtooth regression: 100k simulated-direct median candidate/control ~1.158x and mature phase-map ~1.146x, while 100k Random%25 null controls centered ~1.000x/~0.998x.
  - Interpretation: despite being experiment-facing state, the volatile guard currently has favorable codegen/layout consequences. Do not remove solely for cleanliness.
- **E407 - E360 LEAF-BUFFERED-MERGE TOGGLE CLEANUP** - REJECT cleanup; retain `e360EnableLeafBufferedMerge`. Removing the mutable always-on toggle and making the retained path unconditional produced adverse repeated evidence on 100k MixedDirectionRuns (six-cohort process-median ratio ~1.079x; four of six cohorts slower), while 100k Random centered ~1.002x.
  - Interpretation: the flag remains intentionally because deleting it perturbs production performance; revisit only with a stronger codegen-preserving cleanup.
- **E408 - E344 TOP-LEVEL OVERLAP-TRIM TOGGLE CLEANUP / EXTENDED RECHECK** - REJECT cleanup after extended evidence; restore and retain `e344EnableTopLevelOverlapTrim`. The first six cohorts looked neutral enough to provisionally remove the flag, but a requested recheck expanded the same unchanged candidate/control binaries to 14 full cohorts and then 8 additional targeted 100k MixedDirectionRuns + Random cohorts (22 total on those cells), each 500 trials with alternating process order.
  - Final evidence: 10k BlockSorted no longer showed the original concern (14-cohort process-median ratio ~1.0038x; matched-seed cohort-median center ~0.9998x). However, 100k MixedDirectionRuns showed a small persistent candidate regression in matched-seed evidence: 22-cohort center ~1.00219x, with 17/22 cohort seed-paired medians slower; the final targeted eight centered ~1.00300x. The corresponding 100k Random null targeted eight centered ~0.99984x seed-paired. Process medians on MixedDirectionRuns remained highly bimodal and were not used alone for the decision.
- **E409 - E368 SPARSE-DISORDER-DIRECT TOGGLE CLEANUP** - SUPERSEDED by E413. Direct removal was rejected; E413 later removes the artifact safely through a staged real admission proof. Original E409 status was REJECT cleanup; retain `e368EnableSparseDisorderDirect`. Six 500-trial cohorts showed a stable 10k Sorted+Noise(5%) regression when the toggle was removed (median ~1.015x; all six cohorts slower), with 10k MixedDirectionRuns also adverse (~1.026x median).
  - Interpretation: keep the runtime toggle because deleting it measurably worsens active paths.
- **E410 - E371 CODEGEN-PRESERVING CLEANUP VIA REAL REPEATED-PATTERN PRECONDITION** - RETAIN; remove the experiment-only `e371EnableRepeatedPatternSpecialist` volatile runtime toggle from the maintained simulated-direct lineage. Replace it with a genuine cheap proof precondition: before entering the cold full repeated-pattern verifier, require the first element of run 1 to compare equal to the first element of run 0. This condition is necessary for the specialist to succeed and therefore improves rejection work rather than existing only to perturb code layout.
  - Assembly evidence (GCC, repository `-O3 -march=native`): deleting the volatile guard and calling the cold specialist unconditionally radically changed partitioning (`tryLongAscendingNaturalRunDirect<int>` hot clone ~0x7ed -> ~0x2a5 bytes; cold clone ~0xae -> ~0x290). The retained real-precondition form restores essentially the original partitioning (hot ~0x7e0, cold ~0xae) and keeps the specialist behind a branch into `.text.unlikely`, without mutable experiment state.
- **E411 - E360 CODEGEN-PRESERVING CLEANUP VIA BUFFERED-LEAF FUNCTION SEPARATION** - RETAIN; remove `e360EnableLeafBufferedMerge` from maintained `noalloc-direct`. The naive flag deletion grew `mergeRecursiveLeafBufferedNoAlloc<int>` from ~0x1942 to ~0x19d6 bytes and regressed the E407 target. A `[[unlikely]]` branch-hint variant shrank the function further but still regressed 100k MixedDirectionRuns by ~1.016x matched-seed, and a scratch-null precondition reproduced symbol size (~0x1941) but still regressed ~1.011x.
  - Retained structure: move the terminal buffered leaf implementation into a real `noinline` `mergeBufferedLeafNoAlloc` helper and call it only after the existing `allowLeafBuffer && total<=scratchCapacity` proof. This removes the experiment switch and separates two genuinely distinct responsibilities: recursive partitioning versus terminal buffered merge. GCC emits the recursive partitioner at ~0x10a4 bytes and the helper at ~0x989, substantially reducing the recursive hot body without fake runtime state.
- **E412 - E344 CODEGEN AUDIT AND CLEANUP**
  - Scope: maintained `noalloc-direct` owner only. Audited the legacy `e344EnableTopLevelOverlapTrim` runtime experiment toggle before removal. The retained algorithm already keeps top-level overlap trimming enabled; cleanup removes only the mutable switch and dead disabled branch.
- **E413 - E368 CODEGEN-PRESERVING CLEANUP VIA STAGED SPARSE-DISORDER ADMISSION** - RETAIN; remove `e368EnableSparseDisorderDirect` from maintained `noalloc-direct`. Assembly showed naive deletion changed only the sparse helper modestly (~0x44a -> ~0x457) while leaving `sort<int>` unchanged, but timing remained layout-sensitive. Several legitimate decompositions were tested rather than preserving a fake flag.
  - Retained structure: split the existing admission proof into a cheap inline first-eight inversion gate (`<=2`, already required by E368) followed by a `noinline` distributed-window probe for the eight 64-element windows. This is algorithmically useful staged rejection, not a layout knob. GCC keeps `sort<int>` at ~0x1972; the expensive window probe is isolated at ~0x21d.
- **E414 - HIGH-ENTROPY PROFILING-PLUMBING CLEANUP AUDIT (NOALLOC FALLBACK)** - RETAIN after cross-host recheck; maintained `noalloc-direct` now outlines `allocation_free_v10::fallbackNoAlloc` and routes its integral/copyable branch through E263 `highEntropyQuickSortProduction`, removing the generic profiling-capable recursive path from this production fallback. The algorithmic controls are unchanged (adaptive large pivot, ipnsort-style smallsort, iterate-right production recursion).
  - Retained the high-entropy fallback ownership cleanup in `noalloc-direct`; cross-host checks showed repeatable broad gains but explicit narrow regressions (notably AMD OrganPipe and earlier Intel Random%25) accepted as a tradeoff pending routing recovery.
- **E415 - E414 ORGANPIPE / REVERSE LOSS RECOVERY AND CODEGEN AUDIT** - NO RETAINED SOURCE CHANGE; keep E414. Live host remained AMD EPYC 9V74, so no publication/default-roster sweep was run. Fresh E413-vs-E414 targeted A/B confirmed the E414 OrganPipe cost is real and isolated: ~1.059x at 10k and ~1.056x at 100k across two same-session cohorts, while Random remained favorable and Random%25 neutral/favorable. The earlier ~1.5% 100k Reverse loss did not reproduce in the fresh same-session A/B and is treated as host/process variance rather than a routing target.
  - Route/assembly finding: OrganPipe never executes the changed `fallbackNoAlloc`; it takes the two-long-natural-run direct route and descending-run buffered merge. Reverse also bypasses the changed fallback and returns through the descending classifier. The retained E414 change therefore perturbs these rows only through generated-code placement/register/layout effects. The actual `mergePairLeafBufferedNoAlloc<int>` executor was ~99% instruction-identical across the audited builds; observed differences were dominated by function/loop placement and alignment rather than changed sorting logic.
- **E416 - V11 NOALLOC METRICS-PLUMBING COMPILE-TIME SPLIT AUDIT** - REJECT; production remains E415/E414. Live host AMD EPYC 9V74, so no Intel publication/default-roster sweep was run.
  - Source/codegen experiment: replaced the shared runtime `Metrics*` production path in `detail_noalloc-low-run-merge_overlap-routed_inplace-adaptive_run-reclaim64.h` with a single `template<bool CollectMetrics>` implementation, using `if constexpr` to remove profiling stores/checks from production while retaining the metrics-enabled instantiation for diagnostics. Added production-null overloads for the three helpers reused transitively by maintained `noalloc-direct` (`classifySharedBoundedPatience`, `tryEarlyRepeatedOverlapRoute`, `handleRunCapacityOverflow`). GCC 14.2 `-O3 -march=native` reduced the V11 production body from roughly 0x16bc bytes to ~0x0ac3; the early-overlap helper likewise shrank (~0x334 metrics-enabled vs ~0x319 production), confirming the runtime metrics plumbing was genuinely present in current production code rather than already fully constant-folded away.
- **E417 - NOALLOC-DIRECT LOCAL METRICS RESIDUE CLEANUP** - RETAIN source-only cleanup; live host AMD EPYC 9V74, so no Intel publication/default-roster sweep was run.
  - Scope: maintained `jessesort_noalloc-direct_probe-route_inplace-adaptive_run-reclaim64_move-only.h` only. Removed the local `allocation_free_direct::Metrics` struct, the unused `Metrics*` parameter/default from `tryLongNaturalRunDirectNoAlloc`, and its null-guarded `directLongRun` store. Maintained production `sort()` was the only current maintained caller and always passed null; no maintained consumer reads this local metric.
- **E418 - INDEXED DEAD CLOCK ALIAS CLEANUP** - RETAIN source-only cleanup; live host AMD EPYC 9V74, so no Intel publication/default-roster sweep was run.
  - Scope: maintained `jessesort_indexed_probe-route_adj-adaptive-buffer_move-only.h` only. Removed the unused `using Clock = std::chrono::steady_clock;` alias. No maintained code referenced the alias; active chrono usage remains in the shared high-entropy profiling backend and was not removed.
- **E419 - TRANSITIVE EXPERIMENTAL V10 DEPENDENCY-BOUNDARY CLEANUP** - RETAIN source/dependency cleanup; live host AMD EPYC 9V74, so no Intel publication/default-roster sweep was run.
  - Scope: maintained noalloc lineage imported the entire experimental `jessesort_noalloc_bounded-fallback_inplace-adaptive_partition-heapsort-fallback.h` only to reuse stable shared primitives (`kMaxRuns`, `RunDesc`, natural-run discovery, cheap merge-geometry proof, cheap natural-run merge, and `fallbackNoAlloc`). Extracted those primitives unchanged into maintained `include/jessesort/detail/decomposition/noalloc/bounded_common.h`; the maintained V11 detail header now includes that maintained detail file, while the experimental V10 header includes the same detail file for its own implementation. Profiling/classification-only V10 code remains experimental and no longer sits in the maintained include lineage merely as a dependency container.
- **E420 - FROZEN DEFERRED-BANDS DEPENDENCY-BOUNDARY CLEANUP** - RETAIN source/dependency cleanup; live host AMD EPYC 9V74, so no Intel publication/default-roster sweep was run.
  - Scope: maintained `jessesort_simulated-frozen_probe-route_adj-powersort-adaptive-buffer_single-overflow.h` imported the entire experimental deferred-bands V5 header only to reuse the shared early-freeze insertion core (`FreezePolicy`, frozen insertion result/simulator, pile-search helpers, and random-like classifier). Extracted that shared early-freeze core unchanged into maintained `include/jessesort/detail/decomposition/frozen/early_freeze_core.h`. The maintained single-overflow owner now includes the detail core directly; the experimental deferred-bands header includes the same core and retains only its V5-specific overflow-band reconstruction/public pipeline.
- **E421 - PHASE-MAP RANGE DEPENDENCY-BOUNDARY CLEANUP** - RETAIN source/dependency cleanup; live host AMD EPYC 9V74, so no Intel publication/default-roster sweep was run.
  - Scope: maintained `jessesort_simulated-direct_phase-map-mature_probe-route_adj-adaptive-buffer.h` directly imported the experimental `jessesort_simulated_phase-map-range_probe-route_adj-adaptive-buffer.h`, but only reused the stable range-mapping core (`RouteState`, `Observation`, route observation/state helpers, diversity gate, `PhaseMap`, x2 mappers including the mature lead-STRUCTURED 3k-cap schedule, and mapped-run merge). Extracted those primitives unchanged into maintained `include/jessesort/detail/classification/phase_map/range_core.h`. The maintained mature wrapper now includes that core and `detail_phase_range_engine.h` directly; the experimental range header includes the same core and retains ownership only of its legacy public phase-map-range `sort()` pipeline.
- **E422 - E370 HISTORICAL ROSTER ANOMALY / NOALLOC-DIRECT SAME-HOST PROVENANCE AUDIT** - NO SOURCE CHANGE. Live host AMD EPYC 9V74. The exact E370 source/archive was not physically available, so the old E370 roster ratios were not treated as a causal control. The strongest available physical control is E413; for maintained `noalloc-direct`, E414 is the only later retained runtime/codegen change, while E415 has no source change, E416 was rejected, and E417-E421 are zero-codegen/source-only for this owner.
  - Fresh same-host GCC `-O3 -march=native`, CPU0-pinned, 2-warmup + 500-trial E413-vs-E421 cells: Random reproduced favorable in two process-order cohorts (10k 0.9285x/0.9302x; 100k 0.9662x/0.9691x). MixedDirectionRuns 10k also reproduced favorable (0.9716x/0.9691x). Thus no recent-lineage regression is visible on these cells.
- **E423 - EXPERIMENTAL SIMULATED-DIRECT HEADER SELF-CONTAINMENT / INCLUDE-GUARD FIX** - RETAIN experimental-header correctness cleanup; live host AMD EPYC 9V74, so no Intel publication/default-roster sweep was run.
  - Audit finding: `experimental/jessesort_simulated-direct_e377-materialize.h` reused E375's include guard (`JESSESORT_E375_TWOOFTHREE_H`), so E375 and E377 could silently suppress one another when included in the same translation unit. The audit also exposed a copy/paste namespace defect in E374/E375/E377: each header defines its own `confirmedShortSameDirectionPrefix` helper but later called `jessesort::simulated_direct_merge::confirmedShortSameDirectionPrefix`, making the experimental header accidentally depend on some other direct-merge header having been included first.
- **E424 - EXPERIMENTAL DEAD CHRONO / CLOCK RESIDUE CLEANUP** - RETAIN source-only experimental cleanup; live host AMD EPYC 9V74, so no Intel publication/default-roster sweep was run.
  - Audit result: removed genuinely unused `<chrono>` includes from the two linked experimental headers (`jessesort_linked_probe-route_linked-fused-adj-adaptive-buffer.h` and `jessesort_linked_direct-merge-probe-route_linked-fused-adj-adaptive-buffer.h`) and removed the unused `using Clock = std::chrono::steady_clock;` alias from `jessesort_indexed_direct-merge-probe-route_adj-adaptive-buffer_move-only.h`. All three headers syntax-compile standalone after the cleanup.
- **E425 - EXPERIMENTAL NOALLOC-DIRECT INCLUDE-GUARD COLLISION FIX** - RETAIN experimental-header correctness cleanup; live host AMD EPYC 9V74, so no Intel publication/default-roster sweep was run.
  - Audit scope: mechanical integrity pass over all 32 headers under `include/jessesort/experimental`. All 32 syntax-compile standalone. One duplicate include guard remained: `jessesort_noalloc-direct_e373-single-pass-lanes.h` and `jessesort_noalloc-direct_probe-route_inplace-adaptive_run-reclaim64_move-only_descending-leaf-buffered-experimental.h` both used `JESSESORT_E342_NOALLOC_DIRECT_H`.
- **E426 - LEGACY V-NUMBER ACTIVE-IDENTIFIER / COMMENT MIGRATION** - RETAIN rename-only cleanup; no functional algorithm change. Scope intentionally excludes pre-E229 historical experiment records and legacy filenames whose names are provenance. Active `include/`, `benchmarks/`, and `src/` identifiers/comments now use descriptive architecture names instead of V1-V11 labels.
  - Mechanical rename examples: `allocation_free_v10` -> `allocation_free_bounded`, `allocation_free_v11` -> `allocation_free_low_run`, `linked_v8_legacy/direct_merge` -> `linked_legacy/direct_merge`, `simulated_simd_v7_legacy/direct_merge` -> `simulated_avx2_legacy/direct_merge`, and reconstruction helpers `...ForV2/V7` -> `...ForSimulated/Avx2`. Current explanatory comments now say physical, simulated, simulated-inplace, frozen-single/deferred/live, avx2, linked, indexed, noalloc-bounded, and noalloc-low-run.
- **E427 - LONGITUDINAL CHAMPION / FULL VARIATION-BANK AUDIT** - AUDIT COMPLETE / NO PRODUCTION SORT CHANGE. Current-bank measurements used AMD EPYC 9V74, CPU 0 pinned, GCC `-O3 -march=native`. A first hand-transcribed broad-screen harness was discarded before decision use after finding noncanonical workload generators. The retained screen directly reuses the canonical benchmark generator code and covers 38 isolated algorithm headers x 14 inputs x 10k/100k at 50 trials + 2 warmups; all potential champion changes were rechecked at 500 trials.
  - Winner-bank result: current `simulated-direct-phase-map-mature` is the best broad header in the bank by geometric-mean distance to each cell's fastest screened implementation; current `simulated-direct` also remains among the strongest broad implementations. No experimental header is a broadly superior forgotten champion. Most large 50-trial specialist gaps collapse at 500 trials. Deferred-bands keeps only a small ~1-2% BlockSorted edge; large-rotated, indexed-direct, bounded-noalloc, and descending-leaf variants do not displace the current champions broadly.
- **E428 - E229 BENCHMARK-METHOD CORRECTION / CURRENT-LINEAGE COMPARISON** - AUDIT COMPLETE / NO PRODUCTION SORT CHANGE. User supplied the actual E229-era repository. On the current AMD EPYC 9V74 host, the unchanged E229 algorithms were benchmarked first with their original paired/same-process methodology and then with the current E337 isolated-cell methodology (500 trials, 2 warmups, CPU 0 pinned, one algorithm/input cell per process, randomized cell order, 1 s cooldown). Historical roster included the six E229 default-era pillars plus `physical-direct`, `frozen-single-direct`, `avx2-direct`, `linked-direct`, and `indexed-direct`; current E427 defaults were measured under the same isolated method at 10k and 100k.
  - Method effect: the old benchmark materially flattered JesseSort at 10k. Across all E229 JesseSort cells, isolated/original geometric mean was ~1.1828x and median ~1.0976x; at 100k geometric mean ~1.0760x but median only ~1.0183x, showing a smaller and more workload-specific effect. E229 simulated-direct Random changes from 134.187 -> 153.552 us at 10k (+14.4%) and 1572.834 -> 1609.579 us at 100k (+2.3%) when the algorithm source is held fixed and only the benchmark method changes. Thus old same-process timings are method-contaminated, especially for small-N/shared-code routes, rather than safe longitudinal speed baselines.
- **E429 - 100K MIXEDDIRECTIONRUNS REPEATED-PROCESS PROVENANCE CORRECTION** - AUDIT COMPLETE / NO PRODUCTION SORT CHANGE. E428's one-process-per-implementation isolated snapshot suggested a large 100k MDR regression. Because E405/E422 already established severe EPYC process-state bimodality on MDR, E429 repeated the historical/current comparisons across six independent x500 process cohorts with alternating process order, CPU 0 pinning, canonical current MDR generator/seed schedule, 2 warmups, and 1 s cooldown.
  - E229 frozen-single-direct vs current frozen-single-direct: both sides remain highly bimodal; paired current/old ratios span ~0.767x to ~1.385x with median ~0.975x. Source audit shows the frozen-single-direct/deferred implementation itself is essentially unchanged; current still contains this historical capability under `experimental/`, while its shared simulated-direct dependency evolved. There is no stable evidence of a lost frozen-single MDR executor.
- **E430 - XEON 8573C HISTORICAL PROVENANCE / COMPLETE** - AUDIT COMPLETE / NO PRODUCTION SORT CHANGE. CPU: Intel Xeon Platinum 8573C. Exact E229 source and the packaged E429-current source snapshot were both completed with GCC `-O3 -march=native` under the E337 isolated-cell method at 10k/100k x500, 2 warmups, pinned CPU, randomized cell order, and 1 s cooldown. All isolated evidence validated: E229 168/168 cells at each size; current 112/112 at each size.
  - Historical-protocol decomposition: the optional exact E229 paired/warmed benchmark was also completed on the same 8573C. Across JesseSort rows, original-method / E229-isolated geometric mean was ~0.8534x at 10k (~14.7% faster) but ~0.9950x at 100k (~0.5% faster). Example Random simulated-direct was 154.586 vs 158.662 us at 10k (0.9743x) and 1852.623 vs 1882.113 us at 100k (0.9843x). This closes the benchmark-protocol question: historical warming materially flatters small-N results but cannot explain broad 100k lineage differences.
- **E457 - POST-E430 BRANCH PROVENANCE CAPTURE / CONSOLIDATION PLAN** - RETAIN documentation/provenance structure; no sorter or benchmark behavior change. The uploaded E438 Intel roster branch, E443 corrected indexed zero-copy branch, and E449 causal/reconciliation branch were snapshotted under `docs/consolidation_sources/` with archive SHA-256 hashes and original logs/README files.
  - Started the canonical multi-branch consolidation. Preserved donor logs/README snapshots and hashes, chose the newer audit branch as the source base, and prohibited older donor headers from overwriting retained newer code.
- **E458 - EIGHT-DESCRIPTOR ROUTINE ROSTER / INTEL SNAPSHOT MERGE** - RETAIN benchmark-roster + documentation merge; no maintained sorter implementation was reverted. Ported the Intel branch's `noalloc-low-run` routine descriptor into the newer E449-source benchmark driver and synchronized `benchmark.sh` so the routine isolated-cell runner actually schedules all eight JesseSort descriptors, while preserving `noalloc-direct` as the general allocation-free production pillar.
  - Restored `noalloc-low-run` as the eighth routine benchmark descriptor after E430/E436 showed the apparent Noise10 loss was a roster regression, not lost capability.
- **E459 - E430 PROVENANCE / ANOMALY / ROSTER RECONCILIATION** - AUDIT CONSOLIDATED / NO SORTER CHANGE. Merges the independent warm-state/protocol decomposition with the independent 8573C high-sample anomaly reruns. Longitudinal claims use the E430 same-model isolated E229/current benchmark, not the historically paired/warmed E229 absolute timings. The old protocol materially flattered 10k through immediate pre-timed reference/execution state, while the geometric old-protocol effect was only ~0.5% at 100k; synthetic warming is therefore not a valid recovery mechanism.
  - Consolidated E430 protocol/anomaly evidence: several alarming best-bank regressions disappeared under high-sample same-8573C reruns, while Noise10 was explained by the missing `noalloc-low-run` specialist.
- **E460 - RETAINED POST-E430 SORTER CHANGES CONSOLIDATION** - RETAIN existing newer-base source; no new algorithm change during consolidation. Two post-E430 sorter changes survive the independent branches and are present in the canonical source.
  - Verified both retained post-E430 sorter changes are present: larger two-lane gather scratch in `noalloc-direct`, and indexed true zero-copy run-end reuse via `initialOffset`.
- **E461 - POST-E430 COMPILED-CODE / OWNERSHIP / CLEANUP AUDIT CONSOLIDATION** - CONSOLIDATED REJECTIONS / NO NEW SOURCE CHANGE. Independent audits repeatedly showed that source-equivalent cleanup or ownership changes are not codegen-neutral in this repository and can move official cells by several percent.
  - Consolidated codegen/ownership audits: several source-equivalent cleanups changed emitted code and regressed official cells; Random's high-entropy backend itself was byte-identical across the historical/current comparison.
- **E462 - E229->CURRENT SAME-METHOD LINEAGE RECONCILIATION CONSOLIDATION** - AUDIT CONSOLIDATED / NO ROLLBACK. Only E430/E337-style isolated old-vs-current timings are used for longitudinal conclusions. Cross-CPU AMD replication is diagnostic for portability; Xeon-specific regressions require Xeon confirmation before source rollback.
  - Same-method E229->current reconciliation narrowed the genuine regression cluster. Broad simulated/simulated-direct/indexed losses did not reproduce on AMD; physical Rotated/OrganPipe remained the strongest repeated old->current losses.
- **E463 - CANONICAL POST-E430 HISTORY / TODO REWRITE** - RETAIN documentation consolidation; no sorter behavior change. Removed collided branch-local E431+ experiment entries from the active/canonical ledger after reconciling their findings into the canonical E457+ entries; duplicated raw snapshots are omitted from the slim distribution. Canonical post-E430 findings are now represented only by E457-E463 grouped entries.
  - Replaced collided branch-local post-E430 IDs with canonical grouped E457+ history and namespaced/archived donor evidence.
- **E464 - FINAL POST-E430 MULTI-BRANCH CONSOLIDATED BASELINE** - CONSOLIDATION COMPLETE / NEW CANONICAL BASELINE. CPU checked before validation: Intel Xeon Platinum 8573C. No new performance claim was made from smoke-scale validation.
  - Final multi-branch consolidated baseline: newer audit source plus the Intel branch's eight-descriptor roster/evidence, with retained sorter changes preserved and correctness/integration smoke passing.
- **E465 - VERIFIED-8573C README SNAPSHOT RESTORATION** - DOCUMENTATION / PROVENANCE UPDATE ONLY; no sorter or benchmark behavior change. Current host at edit time is Intel Xeon E5-2673 v4, so no new performance run was made.
  - README/provenance update only: restored faster verified-8573C publication evidence and confirmed E425->E430 had no intervening production-sort algorithm change, so their absolute timing difference is cohort/process sensitivity rather than a source regression.
- **E467 - PHYSICAL SINGLE-GAME PATIENCE BASELINE** - RESEARCH BASELINE RETAINED IN EXPERIMENTAL BANK; no production/default change. CPU: Intel Xeon Platinum 8573C. Added `experimental/jessesort_physical-single-game_probe-route_adj-adaptive-buffer.h` as a source-order, ascending-only physical Patience control derived from the maintained physical owner. Descending-game routing and dual-game natural-run batching are disabled; no reverse traversal, front insertion, or compensating single-game-specific heuristic was added. The maintained physical flatten/adjacent merge backend remains the downstream reference.
- **E468 - SIMULATED SINGLE-GAME PATIENCE BASELINE** - RESEARCH BASELINE RETAINED IN EXPERIMENTAL BANK; no production/default change. CPU: AMD EPYC 9V74 80-Core Processor. Because the 8573C environment recycled after the interrupted first attempt, this experiment intentionally uses same-host before/after A/B timing against the maintained simulated control; these ratios are architectural evidence and are not substituted for 8573C provenance. Added `experimental/jessesort_simulated-single-game_probe-route_adj-adaptive-buffer.h`, which reuses the maintained simulated family’s front-end direct routes, tagged-blueprint reconstruction, random-merge selector, and adjacent merge backend while replacing dual-direction Patience assignment with one source-order ascending game. No reverse traversal, front insertion, freeze logic, or single-game-specific structural rescue was added.
- **E469 - PHASE-MAP SINGLE-GAME REVERSE TRAVERSAL** - RESEARCH RETAIN; no production/default change. CPU: AMD EPYC 9V74 80-Core Processor. Added `experimental/jessesort_simulated-single-game_phase-map-reverse-traverse_probe-route_adj-adaptive-buffer.h`. The experiment keeps one ascending simulated Patience game and changes only logical visitation order for mapped descending regions: ascending/mixed regions are consumed left-to-right, descending regions right-to-left, with assignments stored against original source indices. The source array is never physically reversed and no direct descending-run bypass is used. E468 remains the plain forward single-game A/B control.
  - Mapper mechanism: the mature phase map previously discarded direction by collapsing observations to `Structured/Entropy/Other`, making ascending and descending structured phases indistinguishable. E469 therefore keys map stability on `(route state, orientation)`, with orientation derived from the existing 32-value dual-Patience observation using one fixed conservative 75% asc/desc criterion. The mature x2 galloped lead-structured 3k/4k schedule is otherwise preserved. Endpoint gating is direction-aware so an OrganPipe-style Structured-Ascending -> Structured-Descending input can enter mapping. No orientation threshold sweep was performed.
- **E470 - PHASE-MAP SINGLE-GAME REVERSE TRAVERSAL / GLOBAL SOFT BOUNDARIES** - RESEARCH RETAIN; no production/default change. CPU: AMD EPYC 9V74 80-Core Processor. Added `experimental/jessesort_simulated-single-game_phase-map-reverse-traverse-soft-boundaries_probe-route_adj-adaptive-buffer.h`. E469 orientation detection, 75% asc/desc criterion, galloped map boundaries, and reverse logical visitation are unchanged. The only mechanism change is boundary realization: instead of independently sorting each mapped region, one global ascending-game tails/counts/blueprint state continues across all regions. Descending regions are still visited right-to-left and all others left-to-right; reconstruction replays exactly the same region visitation sequence before the maintained adjacent pile-run merge. No physical reversal or direct-run bypass is introduced.
- **E471 - PHASE-MAP SINGLE-GAME REVERSE TRAVERSAL / SELECTIVE ROUTE-STATE BOUNDARIES** - RESEARCH RETAIN; no production/default change. CPU: AMD EPYC 9V74 80-Core Processor. Added `experimental/jessesort_simulated-single-game_phase-map-reverse-traverse-route-boundaries_probe-route_adj-adaptive-buffer.h`. E469/E470 map detection, fixed 75% orientation criterion, and forward/reverse logical visitation are unchanged. The only mechanism change is selective boundary realization: adjacent mapped regions continue through one ascending Patience game while `RouteState` remains identical; orientation transitions alone are soft. A Structured/Entropy/Other transition terminates the current game, reconstructs that route-compatible group as one sorted run, and starts a fresh game. Group runs are then merged with the maintained phase-map run merger.
- **E472 - PHASE-MAP SINGLE-GAME / EXACT DESCENDING-RUN BYPASS** - RESEARCH RETAIN; no production/default change. CPU: AMD EPYC 9V74 80-Core Processor. Added `experimental/jessesort_simulated-single-game_phase-map-reverse-exact-desc-bypass_probe-route_adj-adaptive-buffer.h`. E471's directional map, fixed 75% orientation criterion, selective RouteState boundaries, and reverse ingestion remain unchanged for non-exact regions. A mapped region already labeled Descending is additionally verified element-by-element as non-increasing; if exact, it skips Patience entirely and remains physically untouched until the first merge consumes that run from end to begin. Thus this experiment does not pre-reverse the source block.
  - Realization guard: the initial implementation accidentally replaced E471's maintained final group merge even when no bypass fired; that confound was removed before final evidence. Final E472 uses E471's original group sorting + `simulated_phase_map_range::mergeRuns` whenever zero exact regions qualify. The only extra work in no-bypass cases is exactness verification of descending-oriented map regions.
- **E473 - PHASE-MAP EXACT-DESCENDING SAMPLE-PURITY GATE** - REJECT; keep E472 exact-run bypass as the architectural control. CPU: AMD EPYC 9V74 80-Core Processor. Added `experimental/jessesort_simulated-single-game_phase-map-reverse-exact-desc-sampled-gate_probe-route_adj-adaptive-buffer.h`. The final candidate preserves E472's mapper decisions exactly and adds only one rolling eligibility bit per admitted mapped region: a Descending-oriented region is allowed to run the expensive element-by-element exactness proof only when every existing 32-value phase-map observation assigned to that region was purely descending. No additional probes, physical reversal, threshold tuning, or benchmark-family routing were added.
  - Implementation audit: an initial probe-log realization was discarded before final evidence because its heap vector imposed measurable overhead. A second draft accidentally changed E472's behavior when a confirmed transition was too close to admit as a boundary; all timings from that draft were discarded. Final evidence comes only from the rolling-bit implementation whose boundary/state transition control flow matches E472, including the subtle `cur=s` update even when no boundary is admitted. Ambiguous/unadmitted transition evidence conservatively disables direct-run eligibility without changing the map.
- **E474 - PHASE-MAP DESCENDING-PREFIX SALVAGE FROM FAILED EXACTNESS PROOF** - RESEARCH RETAIN; no production/default change. CPU: AMD EPYC 9V74 80-Core Processor. Added `experimental/jessesort_simulated-single-game_phase-map-reverse-desc-prefix-salvage_probe-route_adj-adaptive-buffer.h`. This is the generic follow-up to rejected E473: it does not add sampling or tune the phase map. E474 reuses the exact adj-order scan E472 already performs for every Descending-oriented mapped region. If the scan reaches the region end, behavior is E472 exact-run bypass. If it fails, the comparator-proven non-increasing prefix `[begin, first_violation)` is retained as a direct descending run and only the unproved suffix is sent through ordinary reverse single-game Patience. No minimum salvage length beyond the structural requirement of two values is introduced, so this experiment measures the mechanism without benchmark-tuned amortization thresholds.
- **E475 - PHYSICAL SINGLE-GAME EXACT DESCENDING-RUN REVERSE TRAVERSAL** - RESEARCH RETAIN; no production/default change. CPU: AMD EPYC 9V74 80-Core Processor. Added `experimental/jessesort_physical-single-game_exact-reverse-traverse_probe-route_adj-adaptive-buffer.h` and localized `benchmarks/single_game_physical_exact_reverse_bench.cpp`. Starting from the E467 physical single-game control, maximal exact non-increasing source runs encountered after the maintained prefix/probe front end are detected from untouched adjacent values and consumed right-to-left through the same ascending physical Patience pile machinery. No phase map, physical block reversal, front insertion, empirical minimum run length, or descending-game ownership is introduced. Ordinary source-order visitation is retained outside those exact descending runs.
- **E476 - PHYSICAL SINGLE-GAME GALLOPED MOSTLY-DESCENDING REVERSE TRAVERSAL** - REJECT POLICY; retain header/evidence as a detector negative control, no production/default change. CPU: AMD EPYC 9V74 80-Core Processor. Added `experimental/jessesort_physical-single-game_galloped-mostly-desc-reverse-traverse_probe-route_adj-adaptive-buffer.h` and localized `benchmarks/single_game_physical_galloped_reverse_bench.cpp`. Starting from E467 physical single-game behavior, n>=65536 suffixes are observed with the same fixed 32-value phase observations, 75% orientation criterion, x2 galloped spacing, and 3k/4k caps used by the E469 directional-map control. Mapped Descending regions are consumed right-to-left through the same ascending physical pile game; all other regions are consumed forward. The source is never physically reversed. No minimum run length, input-family gate, probe-density sweep, or tuned threshold is introduced. E475 exact traversal and E467 plain single-game are both timed controls.
- **E477 - PHYSICAL SINGLE-GAME TWO-ENDED / RAINBOW REVIVAL** - RESEARCH CONTROL RETAINED IN EXPERIMENTAL BANK; no production/default change. CPU: AMD EPYC 9V74 80-Core Processor. Added `experimental/jessesort_physical-single-game_two-ended-rainbow_probe-route_adj-adaptive-buffer.h` and localized `benchmarks/single_game_physical_two_ended_bench.cpp`. The candidate revives the original one-family rainbow idea without phase mapping or regional reverse traversal: every physical pile remains ascending, but an incoming value may legally extend either the back (`tail <= value`) or front (`value <= head`). Rising source arrivals prefer a legal tail candidate; falling arrivals prefer a legal head candidate; if the preferred end is unavailable the other legal end may be used, otherwise a new pile is created. Head and tail candidates are maintained in ordered endpoint indexes (`std::multimap`) so routing is logarithmic rather than a linear pile scan; pile storage is `std::deque` for O(1) front/back extension. The source array is never physically reversed and no empirical length/orientation threshold is used.
- **E478 - PHYSICAL SINGLE-GAME REVERSE-STREAM + FRONT-INSERT HYBRID** - RESEARCH CONTROL RETAINED IN EXPERIMENTAL BANK; no production/default change. CPU: AMD EPYC 9V74 80-Core Processor. Added `experimental/jessesort_physical-single-game_reverse-stream-front-insert_probe-route_adj-adaptive-buffer.h` and localized `benchmarks/single_game_physical_reverse_front_bench.cpp`. E475 exact non-increasing runs remain the independent reverse-processing context. Ordinary ascending tail placement retains priority. Only when that placement would create a new pile may a reverse-context value prepend to the smallest legal existing logical head. Prepends use a parallel per-pile front segment (append O(1), emitted in reverse before the ordinary pile at flatten) so the experiment does not pay `vector::insert(begin)` shifts or globally replace pile storage. The source array is never physically reversed and no length/span/input threshold is introduced.
  - Correctness/invariant audit: an initial direct-vector-front prototype exposed that E475's adj-equivalent shortcut assumes the previous source value was appended at a pile back. After a front insertion, that assumption can append an equal successor behind a larger tail and break pile sortedness. E478 therefore records one-step front ownership and suppresses the adj-equal back shortcut, including the E183 manual-equivalence path. The final candidate passed 100 trials across all 14 canonical families at 10k plus 20 trials across all 14 at 100k before timing. Partial timings from the invalid direct-vector prototype were discarded.
- **E479 - SINGLE-GAME VERSUS DUAL-GAME DECISION MATRIX** - COMPLETE; no production/default replacement. CPU: AMD EPYC 9V74 80-Core Processor. Fresh family-local same-host benchmark matrix using 14 canonical inputs at 10k and 100k, 500 deterministic trials/cell, 2 warmups, CPU 0 pinning, GCC `-O3 -march=native`, aligned canonical seeds, post-timing `std::sort` validation, and alternating algorithm order by input. 196/196 CSV cells are complete. Physical family: maintained `actual_piles_legacy` dual owner vs E467 physical one-game baseline and E475 physical exact reverse traversal. Simulated family: maintained simulated dual owner vs E468 simulated one-game baseline. Phase-map family: E471 orientation-aware one-game route-boundary control vs maintained simulated-direct mature phase-map owner. Full ratio + median-us tables are in pruned raw experiment cohort.
  - 100k physical plain-single/dual paired ratios: Random 1.0205x, Noise5 1.0144x, Noise10 **0.8101x**, Random25 1.0007x, Alternating **5.1350x**, Sawtooth **0.9717x**, MDR **4.6535x**, BlockSorted **0.9728x**, OrganPipe **6.2809x**, Rotated 1.1595x, MixedPhase3 1.2679x, MixedPhase12 1.0186x. This confirms that removing the second game can save real work in some regimes, but direction-heavy structure collapses without normalization.
- **E480 - E471 DIRECT-COORDINATE BLUEPRINT BASELINE** - ACTION REJECTED / SIGNAL RETAINED; no production/default change. CPU: AMD EPYC 9V74 80-Core Processor. E471 phase mapping, reverse visitation, and selective RouteState groups are unchanged. Candidate replaces the 32-bit pile-only blueprint plus orientation replay with a packed 64-bit `(pile, insertion-ordinal)` coordinate, so flatten scans source order once and scatters directly to `pileStart + ordinal`. Full 10k/100k x500 same-binary A/B is complete; candidate-first decision-row replication confirms MixedPhase12 ~0.885x paired, BlockSorted ~0.989x, but OrganPipe ~1.218x, MixedPhase3 ~1.042x, MDR ~1.018x, Sawtooth ~1.030x, and Noise5 ~1.027x. Interpretation: reject this 64-bit direct-coordinate realization for production/propagation, but retain the coordinate/flattening signal as promising research evidence. Direct coordinates are viable, yet the first implementation doubles blueprint bandwidth, so representation cost and replay-elimination benefit remain mixed. Revisit only with materially cheaper/compressed coordinates or a layout-stable realization; do not tune from canonical rows. Next test is the user's simulated E477/rainbow shifted-coordinate blueprint, where nonnegative shifted coordinates enable logical pile prepends without physical front insertion.
- **E481 - SIMULATED SINGLE-GAME SHIFTED-COORDINATE RAINBOW** - ACTION REJECTED / SIGNAL RETAINED; no production/default change. CPU: AMD EPYC 9V74 80-Core Processor. Added `experimental/jessesort_simulated-single-game_shifted-coordinate-rainbow_probe-route_adj-adaptive-buffer.h` and localized `benchmarks/single_game_simulated_shifted_rainbow_bench.cpp`. This is the simulated analogue of E477: one logical ascending pile family may extend either tail or head. Each new pile starts at shared virtual coordinate `n`; tail extension increments, head extension decrements, so logical prepends never require negative blueprint indices or physical front insertion. Blueprint entries pack `(pile id, shifted coordinate)`; flatten subtracts the pile's final low coordinate and scatters directly into the pile-local sorted position. Endpoint routing matches E477's head/tail legality and rising/falling preference. No phase map, region reversal, empirical span gate, or length threshold is used.
  - Correctness/audit: an initial draft moved the monotone proof ahead of E468's normal front-end and produced a deterministic ~2x Sorted artifact; all timings from that draft were discarded. Final E481 restores E468's front-end control-flow order and changes only pile ownership/coordinate reconstruction. Final candidate passed randomized correctness plus all 14 canonical families at 10k before timing.
- **E482 - E471 DIRECTIONAL SINGLE-GAME + MATURE STRUCTURAL PREROUTES** - RETAIN AS EXPERIMENTAL DOUBLE-ENDED-RAINBOW REPRESENTATIVE. CPU: AMD EPYC 9V74 80-Core Processor. Added `experimental/jessesort_simulated-single-game_phase-map-reverse-route-mature-preroutes_probe-route_adj-adaptive-buffer.h` and localized `benchmarks/single_game_phase_mature_preroutes_bench.cpp`. The phase mapper, 75% orientation rule, reverse visitation, and E471 selective RouteState grouping are unchanged. For each RouteState group of at least 10k values, the candidate first calls the maintained simulated-direct structural preroute set `tryStructuralPreRoutesE271E283`; if no preroute independently proves/sorts the whole group, control falls back to E471's original one-ascending-game directional decomposition. The candidate never calls the mature dual-game `sortDirect` fallback, so Patience decomposition remains single-game.
- **E483 - ROUTESTATE-STAGED MATURE PREROUTE ADMISSION** - PROVISIONAL REJECT ON AMD / DO NOT SUPERSEDE E482. CPU: AMD EPYC 9V74 80-Core Processor. Candidate changes only E482 preroute admission: groups already labeled `Structured` may call `tryStructuralPreRoutesE271E283`; `Entropy` and `Other` groups immediately use the unchanged E471 directional single-game fallback. Full 10k/100k x500 same-binary A/B plus reversed-order 100k decision rows are under pruned raw experiment cohort. Primary 100k paired ratios included Sawtooth 0.9703x, Noise5 0.9582x, MixedPhase3 0.9342x, but MDR 1.0975x, BlockSorted 1.0729x, Rotated 1.0872x. Reversed order collapsed MDR/BlockSorted toward neutral but Rotated remained 1.0843x paired; OrganPipe and MixedPhase3 also changed materially with order. Interpretation: the simple `Structured`-only gate is too coarse and process-sensitive; do not replace E482 or derive a named-input/per-label exception table. Per user direction, defer decision-bearing RouteState/routing-edge optimization until Intel Xeon Platinum 8573C is available. Next non-routing follow-up: revisit E480 with compressed/layout-stable direct-coordinate metadata.
- **E484 - COMPRESSED 32-BIT DIRECT-COORDINATE BLUEPRINT** - ACTION REJECTED / SIGNAL RETAINED on AMD; no owner change. CPU: AMD EPYC 9V74 80-Core Processor. Revisited E480 by packing `(pile id, pile-local ordinal)` into one dynamic 32-bit word: ordinal receives `ceil(log2(group_size))` bits and pile ID uses the remainder; if actual pile geometry cannot fit, the group safely reruns through E480's 64-bit representation. This removes the fixed 64-bit blueprint bandwidth penalty without introducing benchmark-derived constants. Full E480/E484 10k/100k x500 A/B plus reversed-order 100k decision rows were run, followed by a same-binary E471/E484 100k comparison and reversed-order decision cohort. E484 vs E480 replicated gains included MDR 0.9571x paired, MixedPhase12 0.9189x, MixedPhase3 0.9595x, OrganPipe 0.9363x, Sawtooth 0.9550x, but Noise10 remained 1.0572x and Alternating/BlockSorted were order-sensitive. More importantly, direct E471/E484 rows were highly process-order sensitive: primary suggested gains on Noise5/Rotated/MixedPhase3/MDR, while candidate-first reversed checks flipped many of them (Alternating 1.1020x, MixedPhase3 1.0929x, Rotated 1.0501x, Noise5 1.0290x paired); Sawtooth remained favorable at ~0.978x and OrganPipe was near neutral in the reversed cohort. Interpretation: 32-bit compression validates that E480's bandwidth cost was material, but coordinate flatten versus orientation replay remains layout/order sensitive on this AMD host. Keep the header as experimental representation evidence; do not supersede E471/E482 or tune named-input gates. Revisit on Intel Xeon Platinum 8573C with layout-stable/codegen inspection if the coordinate branch is resumed.
- **E485 - SIMULATED STREAMING OUTER-LOOP BASELINE** - ACTION REJECTED / ARCHITECTURAL SIGNAL RETAINED. CPU: Intel Xeon Platinum 8573C. Added `experimental/jessesort_simulated_streaming-outer-loop_probe-route_adj-adaptive-buffer.h` plus localized `benchmarks/e485_streaming_outer_bench.cpp`. Prototype repeatedly harvests natural monotone runs, reverses descending runs into ascending retained runs, otherwise performs a bounded dual-Patience probe and commits an exactly one-pile probe as a retained run; unresolved probes sorted only the next 1024 values with the maintained simulated pillar. Correctness passed all canonical families at 10k/100k. Same-host x500 A/B showed very large structured wins but catastrophic high-entropy behavior because unresolved Random-like input was fragmented into many independently sorted chunks and merged later. E485 is preserved only as the causal baseline establishing that the outer-loop harvesting order is productive on structure.
- **E486 - SIMULATED STREAMING OUTER-LOOP + MAINTAINED HIGH-ENTROPY ESCAPE** - ACTION REJECTED / SIGNAL RETAINED; no maintained-owner change. CPU: Intel Xeon Platinum 8573C. Added `experimental/jessesort_simulated_streaming-outer-loop-entropy-routed_probe-route_adj-adaptive-buffer.h` and `benchmarks/e486_streaming_outer_entropy_bench.cpp`. Only change from E485 is reuse of the maintained simulated pillar's existing 64-value `earlyRandomLike` rule: if the bounded dual probe reaches at least six piles in both games, the entire remaining suffix is handed to the maintained simulated pillar. No benchmark-derived threshold was introduced. Candidate passed correctness on all 14 canonical inputs at both sizes before timing.
- **E487 - PHYSICAL STREAMING OUTER-LOOP + MAINTAINED HIGH-ENTROPY ESCAPE** - ACTION REJECTED / STRONG ARCHITECTURAL SIGNAL RETAINED; no maintained-owner change. CPU: Intel Xeon Platinum 8573C. Added `experimental/jessesort_physical_streaming-outer-loop-entropy-routed_probe-route_adj-adaptive-buffer.h` and `benchmarks/e487_physical_streaming_outer_entropy_bench.cpp`. The control flow mirrors E486: repeatedly harvest maximal natural runs (reverse descending runs), otherwise dual-Patience probe 64 values, commit a one-pile probe as a retained run, hand a `>=6 asc piles && >=6 desc piles` suffix to the physical pillar, and otherwise physically sort the next 1024-value span before restarting the outer loop. No medium-entropy threshold was added.
  - Physical range ownership note: maintained physical currently accepts only `std::vector<T>&`, so the experimental span driver uses a move-only temporary-vector range adapter. A separate whole-array `physical-adapter` control was benchmarked in the same binary to expose this cost rather than hide it. Candidate/native physical remains the production-relevant comparison; candidate/adapter shows that the large structured wins are not caused by the adapter.
- **E488 - PHYSICAL STREAMING OUTER-LOOP + 3-PILE SUFFIX OWNERSHIP** - ACTION REJECTED / ROUTING SIGNAL RETAINED; no maintained-owner change. CPU: Intel Xeon Platinum 8573C. Starting from E487, changed only the unresolved dual-probe route: one pile is still committed as a retained run; exactly two piles still use the bounded structured path; any probe with three or more total useful piles hands the entire remaining suffix to the maintained physical pillar. This deliberately reuses the existing physical router's nontrivial-pile boundary instead of fitting a Noise-specific threshold. Correctness passed all 14 canonical inputs at 10k and 100k before timing.
- **E488 - PHYSICAL STREAMING OUTER LOOP, CATEGORICAL 1/2/3+ PROBE ROUTER** - ACTION REJECTED AS OWNER / STRONG ROUTER SIGNAL RETAINED. CPU: Intel Xeon Platinum 8573C. Starting from E487, changed only the unresolved probe ownership rule: one pile is committed as a run, exactly two piles retain the bounded structured path, and any 3+ pile probe hands the entire remaining suffix to the maintained physical pillar. The 3-pile boundary is categorical and consistent with the maintained E183 3-pile boundary concept; it was not fitted to named inputs. Full canonical 10k/100k x500 native-physical A/B plus full candidate-first 100k replicate completed. Instrumentation across 500 canonical 100k seeds showed Random, Noise10, Random%25, MixedPhase3 and MixedPhase12 route through 3+ suffix ownership immediately; Noise5 does so in 500/500 trials with only two residual two-pile chunks total. Alternating is the only workload repeatedly exercising the exactly-two-pile path (~98 chunks/trial, overwhelmingly 1+1 game geometry). This proves the medium/noisy fragmentation failure from E487 is fixed. Candidate/native 100k replicate still shows Random 1.0647x, Random%25 1.7986x, Noise10 1.0615x because the physical pillar currently requires a temporary-vector suffix adapter. Fresh whole-array adapter controls confirm this interface cost: E488/adapter paired is Random 0.9472x, Noise5 0.7800x, Noise10 0.8438x, Random%25 1.0230x, while structured rows remain dramatically faster. Do not retain E488 as owner until physical range ownership is native; retain the 1/2/3+ routing result and two-pile diagnostics.
- **E489 - SIMULATED STREAMING OUTER LOOP, CATEGORICAL 1/2/3+ PROBE ROUTER** - PROMISING ARCHITECTURAL CONTROL / NOT YET OWNER. CPU: Intel Xeon Platinum 8573C. Applied the exact E488 1-pile commit / 2-pile bounded / 3+-pile suffix rule to E486 simulated, where suffix sorting is natively span-capable. Full canonical 10k/100k x500 primary A/B and full candidate-first 100k replicate completed. Replicated 100k paired candidate/simulated: Random 1.1089x, Sorted 1.9925x (null-path artifact/plumbing; route never reached), Reverse 0.9834x, Noise5 0.9559x, Noise10 0.9073x, Random%25 1.0198x, Alternating 0.9959x, Sawtooth 0.9486x, MDR 0.2848x, BlockSorted 0.2447x, OrganPipe 0.4397x, Rotated 0.2821x, MixedPhase3 0.7339x, MixedPhase12 1.0043x. The categorical medium/noisy route therefore converts E486's Noise5/10 regressions into wins while preserving the large structured benefits. Remaining regressions are dominated by driver plumbing: the driver reserves retained-run metadata before learning the whole input is monotone, and a 3+ probe at pos==0 hands the whole vector through the phase-range entry rather than the maintained whole-vector owner. Next experiment: remove these general ownership costs with lazy run metadata plus direct whole-vector pillar handoff when pos==0; do not alter the 1/2/3+ routing rule.
- **E490 - PHYSICAL STREAMING OUTER-LOOP + DIRECT WHOLE-ARRAY SUFFIX ESCAPE** - RESEARCH RETAIN / STRONG ARCHITECTURAL SIGNAL; no maintained-owner change. CPU: Intel Xeon Platinum 8573C. This follows E488 without changing its routing: natural-run harvesting, one-pile commit, exactly-two-pile bounded path, and `3+ piles => suffix ownership` remain unchanged. The only mechanism change is representation/ownership: if a `3+` handoff occurs before any prefix has been consumed (`pos == 0`), E490 calls the native physical pillar directly on the original vector instead of moving the entire range through the temporary span-to-vector adapter; genuine later suffixes still use the adapter. A separate simulated E489 prototype existed in the workspace, so this physical experiment is numbered E490 to avoid ID collision.
  - Adapter diagnosis motivating E490: E487's explicit whole-array adapter control cost at 100k was Random%25 ~109.6 us native vs ~188.0 us adapter (+72%), Random ~1687.6 vs ~1844.6 (+9%), Noise10 ~2747.9 vs ~2978.5 (+8%), Noise5 ~2113.1 vs ~2247.2 (+6%). This closely matched E488's residual losses and showed they were not purely routing failures.
- **E491 - SIMULATED-DIRECT STREAMING OUTER LOOP (PREPARED; AWAITING 8573C)** - Built from E489 to test the post-E490 architectural hypothesis that streaming discovery and mature direct routing are complementary layers. Routing policy is intentionally unchanged: maximal natural-run harvesting, one-pile commit, exactly-two-pile bounded ownership, and 3+ pile suffix ownership. The only mechanism delta is ownership: E491 delegates unresolved work to simulated-direct rather than ordinary simulated range sorting; at `pos==0`, a 3+ handoff uses the exact maintained whole-vector `simulated_direct_merge::sort`, while genuine suffixes and bounded chunks use the span-compatible `simulated_phase_range_engine::sortDirect` owner. This avoids fitting new structure gates before testing composition itself.
  - Correctness precheck: all 14 canonical inputs passed at 10k for simulated-direct, phase-map, and E491 on AMD EPYC 9V74. AMD timing was not retained as performance evidence.
- **E492 - PHYSICAL STREAMING NATIVE RANGE OWNER (PREPARED; AWAITING 8573C)** - CPU for preparation: AMD EPYC 9V74; no timing evidence retained. Follow-up to E490 that removes the remaining suffix representation bridge without changing the streaming router. E492 adds an experimental span-native physical engine (`experimental/detail_physical_range_engine_E492.h`) and routes both genuine suffix handoffs and bounded two-pile chunks directly through it. The maintained physical pillar header was deliberately restored unchanged after an initial generic-refactor attempt altered vector codegen; assembly of the restored maintained physical wrapper is byte-identical to the pre-E492 control. Span-specialized primitive pre-routes reuse the existing phase-range span helpers, while pile construction/flatten/merge remains the physical algorithm. Correctness passed all 14 canonical inputs at both 10k and 100k. A same-binary E492/E490/physical 8573C runner is prepared at `benchmarks/run_e492_8573c.sh`, using 2 warmups, 500 deterministic trials/cell, cell-atomic resume, candidate-first ordering, and a complete 100k replicate cohort. Retain/reject awaits Intel Xeon Platinum 8573C evidence.
- **E493 - PHYSICAL STREAMING E145-STYLE MONOTONE SCAN (PREPARED; AWAITING 8573C)** - CPU for preparation: AMD EPYC 9V74; no timing evidence retained. Follow-up to E492 isolating only the streaming outer-loop monotone scanner. E493 preserves E492 native span physical ownership, 32-value natural-run admission, one-pile commit, exactly-two-pile bounded ownership, 3+ pile suffix handoff, retained-run stitching, and final merge. The only source-policy delta is replacing the shared generic `discoverNaturalRun` helper with a literal span-relative mirror of the maintained physical E145 prefix scan: establish direction once through the equal prefix, then test only the edge that can invalidate that direction. Standalone GCC 14.2 codegen inspection shows this is a real branch-layout change rather than a source-only rewrite; notably the pre-E493 generic scanner already receives a cheap equality-loop optimization, so E493 is not presumed faster and must be judged empirically rather than retained for cleanliness.
  - Correctness precheck: E493 passed all 14 canonical inputs at both 10k and 100k on AMD. The maintained physical header SHA-256 remains exactly equal to the packaged E492 control (`e487c646ea65502762f2960ce6574a52239a3303569e7b9be709ee37d8e0a114`), preserving the physical control's source/codegen lineage.
- **E494 - STREAMING PROBE-GEOMETRY AUDIT (DIAGNOSTIC ONLY; NO ROUTING CHANGE)** - CPU: AMD EPYC 9V74; no timing evidence retained. Instrumented a diagnostic mirror of E493's exact state machine across 500 canonical seeds at 10k and 100k. The mirror records pile split/occupancy, local adjacent direction changes, and probe position, then performs the same E493 mutation/ownership action so subsequent observations see production-equivalent state. Raw aggregates are under pruned raw experiment cohort.
  - Exactly-two-pile geometry is almost exclusively Alternating: 100k produced 49,000 two-pile probes (98/trial) and 10k produced 5,000 (10/trial), every one exactly 1 ascending + 1 descending pile with mean occupancy 31/33 and 62 direction changes across each 64-value probe. All other 100k inputs combined produced only two two-pile probes (Noise5), averaging one direction change and occupancy 2/62. At 10k, non-Alternating inputs produced only four total two-pile probes (one Noise5, three MixedPhase3), with roughly 1-3 direction changes and similarly extreme occupancy skew.
- **E495 - EXACTLY-TWO-PILE DENSE-DIRECTION-REVERSAL DIRECT ESCAPE (PREPARED; 8573C REQUIRED)** - CPU during preparation: AMD EPYC 9V74; no AMD timing evidence retained. Starting from exact E493, changed only the existing exactly-two-pile branch. After the unchanged 64-value dual-patience probe reports two piles, a second local scan counts reversals between consecutive non-equal edge directions. If reversals cover at least half of all adjacent probe edges (`2*changes >= probe_size-1`), the remaining suffix is handed to `simulated_phase_range_engine::sortDirect(span)`; otherwise E493's 1024-value native-physical bounded path is unchanged. This coarse majority rule comes directly from E494's categorical geometry (Alternating 62/63 reversals; rare Noise5/MixedPhase3 probes ~1-3) and is not fitted to a named input. Equal edges cannot inflate qualification.
  - Correctness: E495 passed all 14 canonical inputs at both 10k and 100k on AMD (2 canonical seeded trials plus two warmups per cell). Exact E493 source SHA-256 remains `dc7ffba9207eb4c849d94829e2a65dee6e3e6ea818ea4caf83b174e12303dc3e`, identical to the packaged E493 control.
  --- E492-E574: compact modern transition ledger ---
E492 AMD same-host screening (provisional; not canonical provenance)
CPU: AMD EPYC 9V74 80-Core Processor; GCC 14.2.0-19. Canonical isolated 500-trial cells, 2 warmups, 10k/100k, E492 vs E490 vs maintained physical in the same binary. All 84 primary cells completed; four interrupted temp files were discarded only after their corresponding atomic final CSVs were present.
Result vs E490: 100k Alternating 0.9343x, MixedPhase3 0.9460x, Random 0.9818x, SortedNoise5 0.8948x, Sawtooth 0.9879x, Rotated 0.9906x; regressions OrganPipe 1.0463x and SortedNoise10 1.0250x. Most 10k differences are under 1%, except Alternating 0.9512x and MixedDirectionRuns 0.9876x. This is screening evidence only; E492 remains pending Intel Xeon Platinum 8573C confirmation, and >1% regressions must be investigated before retain.
E495/E496 AMD screening correction and routing result
The initial AMD screens were unpinned and are invalid for retain/reject timing. A corrected E496 experiment was built directly on E492 with the dense two-pile gate controlled by a runtime boolean inside one shared sort body, then run on AMD EPYC 9V74 pinned to core 2 with 500 trials/cell, 2 warmups, cooldown, candidate-first primary and control-first 100k replicate. Even pinned, this shared host showed severe temporal instability on paths that are behaviorally identical (e.g. 100k Reverse on/off 1.6496x in primary but 0.9945x in reverse-order replicate; Random 1.2999x then 0.9968x). Therefore AMD timing is non-decisional for small/medium effects.
The dense-direction mechanism itself is robust: Alternating 100k gate-on/gate-off = 0.2885x candidate-first and 0.2700x control-first, with E494 diagnostics showing 62/63 adjacent direction reversals and overwhelmingly exclusive two-pile ownership. E495 is superseded because it inherited rejected E493 scanner code. E496 is the clean candidate: E492 + dense exactly-two-pile escape to span-native direct owner, pending 8573C canonical confirmation. Other AMD E496 ratios are not used as evidence.
E497 - FUSED DENSE-DIRECTION COUNT AUDIT - REJECTED BEFORE TIMING. CPU used only for compilation/correctness: AMD EPYC 9V74 80-Core Processor; no timing evidence retained. Audit found E496's second direction scan is reached only after an exactly-two-pile probe and a qualifying dense probe immediately hands the entire remaining suffix to the direct owner, so the rescan can occur only once per qualifying sort. E497 fused direction-change counting into the already-paid dual-Patience adjacent comparisons. It matched E496 gate decisions and pile metadata over 200,000 randomized 64-value probes and passed all 14 canonical families at 10k/100k. Comparator counts: Alternating 10k 15,297 -> 15,203 and 100k 150,297 -> 150,203 (exactly 94 comparisons saved at either size); Random 10k/100k saved zero. Reject fusion: fixed one-probe saving is too small to justify adding direction-state bookkeeping to every probe.
E498 - PRODUCTION-FORM E496 DENSE TWO-PILE ESCAPE - PREPARED, PENDING 8573C. E498 is direct E492 lineage with E496's E494-derived coarse dense-direction discriminator always enabled, removing E496's runtime diagnostic boolean. Exactly-two-pile probes with direction reversals on at least half of all probe edges hand the remaining suffix to `simulated_phase_range_engine::sortDirect`; all other E492 ownership is unchanged. Correctness passed all 14 canonical families at 10k and 100k on AMD (correctness only). GCC 14.2 noinline wrapper hot code size: E492 0xB47 bytes, E498 0xBD6 (+0x8F / 143 bytes, ~7.8%), consistent with a localized discriminator/handoff. Canonical decision requires `./benchmarks/run_e498_8573c.sh`: 500 trials, 2 warmups, pinned core, candidate-first 10k/100k E498 vs E492 and reversed-order full 100k cohort. E496's shared-body runner remains available to separate mechanism benefit from production code-layout effects if needed.
STREAMING RUNNER AUDIT - PRE-8573C HARNESS CORRECTION
CPU during audit: AMD EPYC 9V74; no performance evidence collected. Before the pending Intel session, the prepared E491 and E492 runners were re-audited against the current benchmark protocol. Both were cell-atomic/resumable but lacked CPU affinity pinning and inter-cell cooldown, unlike the newer E498 runner. This was corrected before any 8573C evidence was collected: both now choose the minimum allowed affinity core, invoke every benchmark process through `taskset -c`, record `pin_core`, and sleep 0.2 s after each committed cell. `benchmarks/run_pending_streaming_8573c.sh` now provides a single hard-gated resumable launcher in priority order E498 -> E492 -> E491. This is a harness-preparation correction, not an algorithm experiment; experiment numbering is unchanged.
E501 — simulated-direct streaming + dense two-pile escape (2026-08-29)
CPU: Intel Xeon Platinum 8573C. Canonical isolated/pinned suite, 500 trials, 2 warmups, 10k+100k, plus reversed-order 100k confirmation. Compared E501 vs E491 vs E498 in the same binary.
Change: propagate E498's dense adj-direction-reversal escape only into E491's existing exactly-two-pile branch; all other E491 streaming/simulated-direct ownership remains unchanged.
E504 — greedy gold router v1 (functional donor-composition baseline)
CPU: Intel Xeon Platinum 8573C. GCC -O3 C++20. 500 trials, 2 warmups, one cell per process, pinned core. Built a functional greedy meta-router that selects among E501, physical-direct, simulated-direct, phase-map, and noalloc-direct using an intentionally simple whole-array structural fingerprint. Compared gold against the donor selected for each canonical input/size in the same binary.
Result: routing is functionally correct across all canonical 10k/100k cells, but the O(n) classifier dominates cheap structured cases. Gold/donor ranges from 1.165x (100k Noise10) to 14.47x (10k Sorted); even Random pays 1.53x at 10k and 1.28x at 100k. This is retained as a baseline/scaffold, not a performance candidate. Next step: preserve this donor map while replacing the full scan/unordered-set fingerprint with early exits + bounded probes, then measure approach toward donor oracle.
E506 — configuration-driven benchmark pipeline recipes (structural benchmark tooling; 2026-08-29)
CPU used for compile/correctness validation only: AMD EPYC 9V74 80-Core Processor. No performance evidence retained. Replaced the canonical benchmark's hard-coded pipeline include/dispatch roster with a validated recipe system. `benchmarks/config/benchmark_layouts.json` selects named known-valid base pipelines; `pipeline_registry.json` owns each base's header, entry point, descriptive module dictionary, and any legally exposed override slots. The generator emits C++ dispatch, shell roster, and a fully resolved JSON provenance snapshot. The default roster is provisional E504 Gold + the eight maintained reference pipelines + `std::sort`.
Composability policy: repository folders describe conceptual/source ownership but do not imply interchangeability. At E506 the optimized bases expose no arbitrary override slots because the hot physical/simulated/direct boundaries are still fused. Illegal overrides fail generation explicitly. Future extraction can add a slot to the registry only after its interface/compatibility is proven; representation glue belongs in explicit `detail/adapters/` and remains part of measured cost. This preserves the recipe format without prematurely forcing generic runtime or template boundaries.
E507 — AMD EPYC 9V74 PRE-REFACTOR E504 BASELINE (2026-08-29)
CPU: AMD EPYC 9V74 80-Core Processor. Purpose: establish a persistent, commonly available AMD reference before judging E506+ modular/configuration changes, rather than waiting for the preferred 8573C for every exploratory comparison. Source under test was the unchanged pre-refactor `jessesort-E504-greedy-gold-v1-8573C.zip`; no E506 source was used for the timing baseline.
Routine baseline: completed the old E504 canonical isolated-cell runner at 10k+100k over all 14 official inputs, eight routine JesseSort variations plus `std::sort`, 500 measured trials/cell, 2 warmups, deterministic canonical seeds, 1 s cooldown, one process per size/algorithm/input cell. Completion 252/252 cells. Full raw checkpoint, exact old cell order, source/binary/runner metadata, Markdown table, and CSV are retained under `benchmarks/baselines/E504-EPYC-9V74/`.
E508 — post-reorganization physical/simulated timing audit (AMD EPYC 9V74)
- Compared pre-refactor E504 against the reorganized recipe-based build for the legacy physical/simulated pipelines. Routing/source audit found no semantic route changes; principal generated hot symbol sizes are identical, though linked addresses moved.
- Exact 10k protocol (500 trials, 2 warmups, isolated cells, 1 s cooldown) showed unstable one-cohort swings. Same-session paired reruns plus three additional 500-trial cohorts collapsed the worst anomalies: physical Random +1.08%, physical BlockSorted -3.45% (very noisy), simulated Random%25 +2.25%, simulated OrganPipe +2.15% by median-of-cohort medians.
E509 — SINGLE-PIPELINE BINARY BENCHMARK ISOLATION + 100K RESTRUCTURE AUDIT (2026-08-29)
CPU: AMD EPYC 9V74 80-Core Processor. Purpose: resolve post-E505/E506 timing ambiguity by removing cross-pipeline code-layout contamination. Built old E504 and reorganized versions of frozen-single, indexed, noalloc-direct, noalloc-low-run, simulated-direct, and phase-map as one-pipeline executables with GCC C++20 `-O3 -march=native -DNDEBUG`. Ran all 14 canonical 100k inputs at 500 measured trials + 2 warmups, one isolated process per cell, pinned CPU 2, 1 s cooldown, alternating old/new order. Any >1% first-pass regression was repeated with reversed order; unresolved cells received a third cohort. Raw timing cohorts are retained in `benchmarks/baselines/E504-EPYC-9V74/comparisons/E509-single-pipeline-raw-evidence.zip`; summary is `E509-single-pipeline-restructure-audit.md`.
Timing cohorts still showed unstable structured-path swings when old/new benchmark metadata embedded different header-path identities. A final stabilized A/B held benchmark metadata constant so the only source difference was the included old vs reorganized pipeline header. Under that control, the COMPLETE benchmark executable `.text` section was byte-identical for all six audited pillars. Together with E508 physical/simulated routing/codegen audit, this resolves the repository restructuring as algorithmically/codegen neutral when benchmark layout is controlled; earlier large deltas were benchmark-binary layout artifacts/noise rather than route changes.
E509 FINAL ACCEPTANCE — RESUMED 2026-08-29: completed the interrupted default-roster acceptance sweep. `make benchmark-config` and the public Gold+noalloc API compile/run smoke pass. All 10 shipped recipe layouts build successfully as independent `-O3 -march=native -DNDEBUG` executables: gold, physical, simulated, frozen-single, indexed, noalloc-direct, noalloc-low-run, simulated-direct, simulated-direct-phase-map-mature, and std::sort. `benchmarks/canonical/run.sh` passes shell syntax; config generator, validator, and aggregator pass Python byte-compilation. No timing evidence was added by this acceptance step. E509 is complete and ready to package.
E511 — GOLD THRESHOLDED DISTINCTNESS TABLE: 128 SLOTS (2026-08-30)
CPU: AMD EPYC 9V74 80-Core Processor. Reconstructed the interrupted E510 bounded Gold classifier from the E509 packaged base, then isolated the planned table-size follow-up. Candidate changes only the fixed open-address distinctness table from 2048 slots to 128 slots; thresholded counting still stops after proving 82 distinct values in the 1024-point sample. Across 500 deterministic seeds for every 10k/100k canonical cell, E511 and reconstructed E510 made identical owner choices (0/14,000 route mismatches). 500-trial same-binary/process-isolated timing with alternating process order was followed by reversed-order repeats for every >1% row. REJECT 128 slots: repeatable regressions included Random%25 10k 1.0260x, BlockSorted 10k 1.0489x, Rotated 10k 1.0674x, Noise10 10k 1.0123x, and BlockSorted 100k 1.0228x. Interpretation: at the 82-distinct decision threshold, a 128-slot table is already ~64% full, so extra collision/probe work can dominate the saved stack initialization. MixedPhase12 100k remained strongly order-sensitive, so no decision is based on that row alone. Next test: 256 slots (~32% threshold load).
E512 — GOLD THRESHOLDED DISTINCTNESS TABLE: 256 SLOTS (2026-08-30)
CPU: AMD EPYC 9V74 80-Core Processor. Candidate changes only reconstructed E510's fixed distinctness table from 2048 to 256 open-address slots; thresholded counting still stops at 82 distinct values. Route audit over 500 deterministic seeds x all 28 canonical 10k/100k cells found 0/14,000 E512-vs-E510 owner mismatches. Full 500-trial cell timings used one algorithm process per cell with alternating process order; every >1% first-cohort row received a reversed-order repeat. REJECT: most structured swings collapsed under reversal, but Random%25 10k remained 1.0164x and Noise10 10k 1.0104x. Random improved in the reversed cohort (10k 0.9928x, 100k 0.9883x), and monotone/alternating paths also improved, so 256 is materially better than E511's 128-slot version but still violates the >1% regression guardrail on a repeatable fast low-cardinality path. Next: 512 slots (~16% threshold load).
E513 — GOLD THRESHOLDED DISTINCTNESS TABLE: 512 SLOTS (2026-08-30)
CPU: AMD EPYC 9V74 80-Core Processor. Candidate changes only reconstructed E510's fixed distinctness table from 2048 to 512 open-address slots; thresholded counting still stops at 82 distinct values. Route audit over 500 deterministic seeds x all 28 canonical 10k/100k cells found 0/14,000 E513-vs-E510 owner mismatches. Full 500-trial cell timings used process isolation and alternating process order; every >1% absolute first-cohort row received a reversed-order repeat. REJECT: reversed-order repeats confirmed Random%25 10k 1.0356x, MixedDirectionRuns 100k 1.0239x, and MixedPhase3 100k 1.0414x. Several monotone/structured paths improved, but the repeatable regressions violate the >1% guardrail. Next: 1024 slots (~8% threshold load), the final meaningful table-size shrink before returning to E510's 2048.
E514 — GOLD THRESHOLDED DISTINCTNESS TABLE: 1024 SLOTS — RETAINED (2026-08-30)
CPU: AMD EPYC 9V74 80-Core Processor. Final table-size follow-up to E510. Candidate changes only the bounded classifier's fixed open-address distinctness table from 2048 to 1024 slots; thresholded counting still stops at 82 distinct values (~8% table load). Route audit over 500 deterministic seeds x all 28 canonical 10k/100k cells found 0/14,000 E514-vs-E510 owner mismatches. Primary timing used 2 warmups + 500 measured trials/cell with process isolation, pinned CPU 2, deterministic seeds, validation and alternating order. Every >1% absolute row received a candidate-first reversed cohort. RETAIN: all first-pass regressions >1% collapsed under reversal; repeated ratios included Noise5 10k 1.0027x, MDR 100k 1.0022x, Noise5 100k 0.9900x, Noise10 100k 1.0034x, Alternating 100k 1.0035x. Random 100k retained a 0.9706x result (-2.94%). E514 therefore halves E510's table footprint without a repeatable >1% regression. Public `jessesort::sort` and the Gold benchmark recipe were promoted from E504 to E514; all 10 shipped layout binaries compile and randomized public API correctness smoke passes. E511/128, E512/256 and E513/512 remain rejected.
- **E515 - GOLD VS PHASE-MAP GENERALIZATION AUDIT** - CURRENT GOLD MAP NOT VALIDATED AS GENERAL ROUTER; no public-owner change. CPU: AMD EPYC 9V74 80-Core Processor. Compared E514 Gold directly with the strongest maintained single pipeline, `simulated-direct-phase-map-mature`, using two 500-trial/2-warmup seed-aligned cohorts at 10k and 100k. Canonical cohort 1 used the E509 cell-atomic per-pipeline runner with only the two pipelines; cohort 2 invoked the same isolated binaries in reversed algorithm order. Combined median seed-paired Gold/phase-map ratios give geometric means 1.1250x at 10k (Gold wins 3/14) and 0.8416x at 100k (Gold wins 5/14). The 100k aggregate win is highly concentrated in exact structured families such as Rotated (0.3425x), Sorted (0.5450x), Alternating (0.5590x) and OrganPipe (0.5789x), while Random%25, Noise10, Reverse, Sawtooth, MDR and both mixed-phase controls are worse or neutral.
E516 — PRODUCTION GOLD ROLLBACK AFTER GENERALIZATION FAILURE (2026-08-30)
CPU: AMD EPYC 9V74 80-Core Processor. E515 demonstrated that the E514 multi-donor fingerprint classifier was specialized to the canonical benchmark geometry: on the frozen OOD suite it lost 7/8 families at 10k and 8/8 at 100k against simulated-direct-phase-map-mature. Corrective action: public `jessesort::sort` and the `gold` registry key are restored to `simulated-direct-phase-map-mature`, the strongest maintained single pipeline. E514 remains available only as `gold-e514-experimental`; no classifier thresholds or E515 holdout generators were changed. Router research must use a new coarse, causally justified design and must not tune against the E515 OOD holdout.
E517–E520 — COARSE PROPERTY ROUTER RESEARCH (2026-08-30)
CPU: AMD EPYC 9V74 80-Core Processor. Goal: replace the overfit E514 fingerprint map with a deliberately coarse property router using phase-map as the default and noalloc-direct only for globally monotone/one-turn geometry or extreme local alternation. No E515 OOD thresholds or family identities were used. E517 used 129 global anchors plus local alternation and exposed two implementation problems: classifier/wrapper cost was unacceptable on very fast 10k paths, and sparse anchors could alias long mixed-direction structure. E518 gated routing to n>=65536 and used 1025 global anchors; canonical 100k showed large wins on Sorted/Alternating/OrganPipe but substantial fallback-path overhead. E519 isolated the small phase-map path syntactically and moved the large router out of line, fixing the catastrophic 10k wrapper regression in targeted repeats. E519 owner audit across 500 seeds routed only Sorted, Alternating, and OrganPipe among the 12 ordinary canonical families; all others stayed phase-map. E520 added an early fallback for descending-leading global geometry, eliminating Reverse classifier overhead in targeted testing (1.0004x phase-map) while keeping large targeted wins on Sorted (~0.452x), Alternating (~0.548x), and OrganPipe (~0.568x). These versions remain experimental and were never promoted to public `jessesort::sort`.
E521 — NEW FROZEN COARSE-ROUTER GENERALIZATION HOLDOUT (2026-08-30)
CPU: AMD EPYC 9V74 80-Core Processor. A new holdout was frozen before observing E520 results; source SHA-256 bb240d3aeeccdef8c7556c1a261c1e8f568f228fd0e2cb1ca86888c647e8ced5. Sizes: 75k, 150k, 300k. Six property families, 500 trials + 2 warmups/cell, alternating algorithm order: positive cases AscendingAffine, BitonicVariable, AlternatingBands; negative controls DescendingAffine, ManyLongRuns, RandomControl. REJECT E520 for production. AscendingAffine ratios were 0.5965x/0.5478x/0.5243x and BitonicVariable 0.7958x/0.7121x/0.8801x, but AlternatingBands failed robustness at 75k (1.4288x) and was still 1.0316x at 300k. Negative controls were near parity overall. The failure is traceable to the descending-leading global early exit suppressing the local alternation detector for some valid alternating geometries. Do not modify E520 based on E521; preserve E521 as rejection evidence. Future router work must start from an independently designed policy and use a new holdout. Production remains the E516 phase-map rollback.
E522 — promote phase-map mature; rename former Gold router descriptively
Status: RETAINED repository/status cleanup; no algorithmic behavior change.
Decision: `jessesort::sort` remains the E516/E521 production rollback target, `simulated-direct-phase-map-mature`. The former "Gold" E514 family is explicitly experimental and is renamed in active source/registry terminology to `canonical-fingerprint-multi-donor-router` (E514 implementation: `research/experimental/routers/canonical_fingerprint_multi_donor_e514_distinct1024.h`). Coarse E517–E520 research is likewise named `coarse-structural-router` rather than Gold. Historical experiment logs/results retain the old Gold label where required for provenance. The default canonical benchmark roster removes the redundant public `gold` alias and contains the eight maintained reference pipelines plus `std::sort`; therefore phase-map mature appears once under its explicit benchmark identity. This change follows E515/E521 generalization evidence and does not retune any router or holdout.
E531 — absorb E510/E511 canonical-fingerprint oracle evidence (2026-08-30)
Status: RETAINED documentation/evidence reconciliation; no algorithm changes.
Imported the E510 bounded-classifier diagnostic and E511 selected-donor-oracle harness/results from the separately developed E511 audit branch. Active donor/reference sources were verified byte-for-byte identical to E522, so no code propagation was required. Historical "Gold" terminology is preserved inside raw evidence for provenance; current docs identify the family as `canonical-fingerprint-multi-donor-router`. This closes the divergent E511 evidence branch before continuing the simulated/noalloc/phase-map hybrid work.
E532 — E530 independent 100k holdout (2026-08-30)
Status: REJECT E530.
On EPYC 9V74, frozen unseen families confirmed large generalized wins for irregular one-turn, rotation, and two-lane structures, but RareLocalInversions repeated at 1.0231x candidate/phase-map in reversed order. The sqrt(n)-sampled ascending nomination therefore imposes an O(n) exact-monotonicity fallback tax on nearly-sorted inputs with hidden local inversions. E530 is retained only as experimental evidence. E532 must not be used to tune or validate its successor.
E533 — NARROW NON-MONOTONE SQRT(N) NOALLOC NOMINATION (2026-08-30)
CPU: Intel Xeon Platinum 8370C. Reconstructed from E532 after the prior EPYC execution context was lost. Candidate intentionally removes E530's exact O(n) monotonicity verification and gives up the Sorted noalloc escape; only exact two-lane alternation or sampled non-monotone simple geometry may nominate noalloc, with phase-map as fallback. 100k canonical screen used 500 trials + 2 warmups per cell, pinned CPU 2, isolated per-pipeline binaries. REJECT as-is: reversed-order repeat confirms Sorted at 1.0375x phase-map, a mechanically credible classifier tax on the very fast sorted path. Large intended wins remain stable (Alternating 0.0809x, OrganPipe 0.1895x, Rotated 0.0458x). Noise5/Sawtooth changed sign across order and are treated as host/order-sensitive rather than stable routing regressions. Next: add a tiny coarse monotone-like checkpoint gate that falls directly to phase-map before sqrt(n) nomination; do not add any canonical-family fingerprints.
E534 — FIVE-CHECKPOINT MONOTONE FALLBACK GUARD (2026-08-30)
CPU: Intel Xeon Platinum 8370C. Follow-up to rejected E533. Added only a five-point globally nondecreasing checkpoint gate that sends monotone-like shapes directly to phase-map before sqrt(n) nomination; the gate can never grant a noalloc route. Targeted 100k 500-trial/2-warmup screen preserved E533's large structural wins but still failed the >1% regression guardrail. Candidate-first/baseline-after reversed comparison: Sorted 1.0147x, Noise5 1.0595x, Alternating 0.0813x, MDR 0.9690x, OrganPipe 0.1878x, Rotated 0.0460x. REJECT: the separate hybrid wrapper/fallback architecture still perturbs ordinary phase-map paths enough to produce repeatable regressions. Next candidate should integrate the escape into a frozen phase-map implementation so fallback executes the same phase-map body directly rather than through a wrapper.
E535 — INTEGRATED PHASE-MAP BODY + NARROW NOALLOC ESCAPE (2026-08-30)
CPU: Intel Xeon Platinum 8370C. Replaced the E533/E534 wrapper architecture with a frozen phase-map-mature body whose entry point optionally attempts the narrow noalloc escape. Ordinary fallback executes the copied phase-map body directly. 100k screen: 500 trials + 2 warmups, pinned CPU 2, isolated per-pipeline binaries; >1% rows received candidate-first/baseline-after reversal. Large intended wins remain stable (Alternating ~0.076x, OrganPipe ~0.191x, Rotated ~0.047x). Sorted clears at 1.0052x; Noise10 1.0018x; MixedPhase12 1.0074x; MixedPhase3 0.9846x. REJECT: Random%25 repeats at 1.0346x. This fast duplicate-heavy path still pays structural nomination work despite never benefiting from the noalloc escape. Next: add a tiny generic fixed-sample duplicate/low-cardinality guard that only forces phase-map fallback; do not add benchmark-family identity logic.
E538 — E537 NEW INDEPENDENT HOLDOUT / FAILED-CLASSIFIER TAX (2026-08-30)
CPU: Intel Xeon Platinum 8370C. E537 added an early descending-edge rejection before E536's duplicate/sqrt(n) work and cleared the complete 100k canonical screen after reversed-order repeats: Reverse 1.0052x, Sorted 1.0062x, Random%25 0.9957x, Random 0.9978x; large intended Alternating/OrganPipe/Rotated wins remained. A brand-new 100k holdout was frozen before timing (source SHA-256 440b2b1971081864f8b3c884f3154571d648f99bc7c888c02338d311b9364b4d). Positive WarpedBitonic/GappedSingleReset/CurvedTwoLane generalized strongly in reversed order (0.1911x/0.0433x/0.0609x phase-map), and NeedleInversions cleared at 1.0021x, but RandomRunMosaic repeated at 1.1442x. Route audit: 0/500 mosaic seeds actually claimed noalloc; 250/500 merely paid sqrt(n) sampling before falling back. REJECT E537. Failure is classifier tax on many-run fallback geometry, not donor misrouting. Do not tune E537 against E538; successor requires a new holdout.
E539–E540 — HIERARCHICAL COMPLEXITY PRECHECK / NEW HOLDOUT REJECTION (2026-08-30)
CPU: Intel Xeon Platinum 8370C. E539 added a 17-anchor directional-complexity rejection gate before the sqrt(n) structural nomination to address E538's failed-classification tax on many-run geometry. The complete 100k canonical screen cleared after reversals; dedicated MDR 1000-trial cohorts were 1.0037x and 1.0012x phase-map. A new frozen E540 holdout (source SHA-256 895fa597b0119f8066f3d954d9d2f116eb2504f5ca2442b4dd426eeee7c573c5) then rejected E539: OffsetRotationRamp repeated at 1.0764x candidate/phase-map. Diagnostic direct donor timing showed noalloc-direct itself at ~64.17 us versus phase-map ~60.92 us, so this is donor-selection error rather than classifier overhead. JitteredRunPatchwork's first-pass +2.2% disappeared in reversal; alternating/bitonic positives remained large wins. Decision: reject E539. Do not tune rotation-specific value/span thresholds against E540. Successor should simplify the escape to exact two-lane alternation plus true one-turn geometry only, intentionally dropping single-reset/rotation routing.
E541 — TRUE-ONE-TURN SIMPLIFICATION, CODEGEN-UNSTABLE IMPLEMENTATION (2026-08-30)
CPU: Intel Xeon Platinum 8370C. E541 removed E539's single-reset acceptance by changing the sqrt(n) nomination return from `runs<=2 || descEdges<=1` to `runs<=2`, preserving only exact two-lane alternation plus true sampled one-turn geometry. First-pass 100k canonical results kept the large Alternating/OrganPipe wins, but reversed-order fallbacks were not clean: MixedPhase12 1.0343x and Sawtooth 1.0487x; MDR remained highly order-unstable (0.8116x first pass, 1.2706x reversed). Noise5 reversed to a win. Because these regressions occur on inputs that should remain phase-map and the only source change was inside the noinline classifier, reject E541 as implemented and treat this as code-layout/codegen sensitivity rather than evidence against the narrower routing policy. Next: preserve E539's original nomination function and add a second true-one-turn verifier only after nomination succeeds, so ordinary fallbacks do not pay the new logic.
E542 — COARSE >2-DIRECTION-RUN ROTATION REJECT, MDR REGRESSION (2026-08-30)
CPU: Intel Xeon Platinum 8370C. E542 preserved E539's sqrt(n) classifier and only tightened the existing 17-anchor directional-complexity gate from rejecting >3 sampled direction runs to rejecting >2, intended to eliminate single-reset/rotation nominations before the expensive scan. This successfully made canonical Rotated a phase-map-like path; two 1000-trial order-swapped Rotated cohorts were 0.9804x and 0.9478x candidate/phase-map. Random and Sorted cleared in reversal. However MixedDirectionRuns was highly unstable in 500-trial screens and then showed a stable regression in two 1000-trial order-swapped cohorts: 1.1113x and 1.1235x. REJECT E542 before holdout. Investigate route ownership and codegen rather than tuning additional thresholds.
- **E544 - E539-SHAPE-PRESERVING TRUE-ONE-TURN ADMISSION** - REJECT. CPU: Intel Xeon Platinum 8370C, pinned CPU 2. Starting from E539, preserved the entire descending reject, coarse monotone gate, 17-anchor complexity gate, duplicate probe, and sqrt(n) sampling behavior; changed only final structural admission from `runs<=2 || descEdges<=1` to `runs<=2`. This removes single-reset/rotation noalloc claims while preserving E539's MDR-beneficial scan/preconditioning behavior; direct E544-vs-E539 MDR isolation remained neutral (0.9995x / 0.9950x in prior order-swapped 1000-trial cohorts). The resumed retain/reject isolation cleared MixedPhase12 at 0.9960x / 1.0033x in two 1000-trial order-swapped cohorts, but BlockSorted remained unstable in four 1000-trial cohorts (1.0528x, 0.9981x, 0.9898x, 1.0398x). Two longer 2000-trial order-swapped cohorts then resolved the ambiguity as a repeatable regression: 1.0165x with E544 first and 1.0120x with phase-map first. Because both longer cohorts exceed the >1% regression guardrail on a fallback workload, reject E544 before any new holdout. Production/default remains simulated-direct-phase-map-mature.
E546 — FROZEN GENERALIZATION AUDIT / INITIAL 100K SCREEN (2026-08-30)
CPU: Intel Xeon Platinum 8370C @ 2.80GHz, pinned CPU 2. Frozen 24-family parameterized OOD generator source before timing (SHA-256 c837db8ff62a53738b98babb505d9201af4171004da5a5575ef8246faa0b400b). Compared production `simulated-direct-phase-map-mature` against `std::sort` at 100k using 100 deterministic seeds/family plus 2 warmups, with correctness checked against an independently sorted expected copy. Ran two complete cohorts with algorithm order reversed (JesseSort→std and std→JesseSort). This is a broad audit screen, not retain/reject evidence and must not be used to tune thresholds.
Result: generalization is strong across many unseen structured families, including JitteredRotation ~0.039x, OffsetRotationRamp ~0.050x, DiscontinuousAffinePhases ~0.053x, WarpedBitonic ~0.068x, and several run/block families ~0.14–0.19x vs std::sort. However the audit exposed a clear weak region on mostly ordered data with localized disorder: LocalizedAlternatingBursts ~3.86x std::sort, WindowShuffle ~2.36x, MonotoneBurstNoise ~1.80x; smaller losses also appeared on PeriodicPerturbed, StickyRandomWalk, and PlateauStaircase. The reversed cohort confirmed the direction, so these are not simple order artifacts.
E547 — LONG PROVEN PREFIX -> MAINTAINED SUFFIX HANDOFF (2026-08-31)
CPU: Intel Xeon Platinum 8370C, pinned CPU 2. Mechanism-level follow-up to E546 and the older E485-E501 streaming signal. E546 diagnostic instrumentation showed the three largest localized-disorder failures were highly fragmented after their first disturbance: LocalizedAlternatingBursts averaged ~3236 natural runs and ~2014 total simulated piles, WindowShuffle ~16525 natural runs and ~1600 piles, MonotoneBurstNoise ~1926 natural runs and ~224 piles, while the successful SparseInversionPatches counterexample averaged only ~35 natural runs and ~18 piles. This supports fragmentation-after-ordered-prefix as the weak region rather than near-sortedness itself. E546 remains burned and no thresholds were derived from it.
Candidate policy: reuse the existing simulated-direct long-natural-run admission scale (proved monotone prefix >= n/8). If admitted, preserve/reverse that exact prefix, sort the entire unresolved suffix with the maintained span direct owner, then merge once. Otherwise fall back unchanged to simulated-direct-phase-map-mature. The n/8 criterion is pre-existing production logic, not E546 tuning.
E548 — REJECT — generalized hierarchical phase-map probing before production (2026-08-30, Intel Xeon Platinum 8370C)
- Motivation: replace the mature endpoint-only diversity gate with a more general interior policy. For n>=65536, gallop through the input with the existing tiny 8-value monotonic observation; strongly monotone windows are treated as cheap boring structure, while a non-monotone 8-window escalates at that same location to the existing 32-value dual-patience observation. Existing x2 gap growth/caps, transition confirmation, semantic-map validation, and E314 economic acceptance were otherwise preserved. No E546-derived threshold was introduced.
- E548A integrated prototype was not acceptable on the canonical 100k/500 screen: Sorted 1.0755x, Reverse 1.0514x, Random%25 1.1748x, MDR 1.0702x, Noise10 1.5940x vs simulated-direct-phase-map-mature. Route audit showed most of these did not execute a newly accepted map, indicating probe/fallback disturbance rather than simply bad donor selection. Typical hybrid scan count was ~35 observations; Sorted/Reverse paid zero 32-value patience probes, while Random%25 paid ~45.7.
E549 — DIAGNOSTIC — in-band Patience telemetry cost / codegen-control audit (2026-08-30, Intel Xeon Platinum 8370C, pinned CPU 2)
- Motivation: after E548 showed that a separate distributed phase pre-scan can badly perturb the later simulated-direct path, test whether phase evidence can instead be collected from pile state already produced by the normal Patience insertion pass. This is a prerequisite diagnostic, not a behavior-changing routing candidate and consumes no holdout.
- Prototype records only total pile-count growth at predictable 1024-element checkpoints during existing insertion loops; it does not reread input values and does not change ownership, decomposition, reconstruction, or merge policy.
E551–E558 — NATURAL-RUN OWNERSHIP / COHERENT-BATCHING SERIES (2026-08-31) — CONSOLIDATED
Primary hosts: Intel Xeon Platinum 8370C for preceding diagnostic lineage; AMD EPYC 9V74 for same-host mechanism A/B screening and promotion evidence. This series asked whether proven natural runs should remain coupled to live patience piles, become detached independent runs, or be bulk-processed only when live hint coherence is high. Burned E546 OOD was used for validation/diagnosis only, not threshold tuning.
E551 — detached `ghost runs` under the existing E098 natural-run gate — REJECT
Prepared first as an 8370C runner, then screened cleanly on EPYC against freshly rebuilt current production. Detached monotone spans received independent blueprint IDs and were removed from live asc/desc tail geometry until reconstruction. Large repeatable losses included Noise5 ~1.026x, Noise10 ~1.029x, MixedPhase3 ~2.21x, and MixedPhase12 ~1.32x production. Conclusion: making harvested runs invisible to live patience geometry is harmful under this early design. This does not reject E098-style bulk tagging when pile ownership is preserved.
E552–E554 — combined live phase telemetry and fresh-redecomposition response — DIAGNOSTIC SIGNAL RETAINED, BEHAVIOR REJECTED
Combined persistent pile-growth change with already-paid game-switch/direction information. The live telemetry was informative and location-independent, but moving the logic behind cold sidecars or triggering fresh re-decomposition from it did not survive MDR/canonical performance checks. Preserve telemetry ideas as diagnostics; do not promote a new phase restart/router from this branch.
E555 — coherent-hint coupled natural-run batching — STRONG SIGNAL, INITIALLY REJECTED AS-IS
Kept live pile coupling and entered repeated natural-run batching only after high exact pile-hint coherence. 100k canonical evidence showed meaningful Noise5/Noise10 gains (~2–4%) and deep MDR gains around 24–26%, but OrganPipe/code-layout tax and later OOD regressions prevented an automatic retain. The mechanism itself remained the strongest natural-run result.
E556 — noinline coherence-batch sidecar — REJECT
Diagnostics showed OrganPipe does not semantically exercise E555's new coherence branch; its loss is implementation/code-layout fallout. Moving the new continuation behind a noinline sidecar did not consistently recover OrganPipe and destabilized MDR, so this implementation form was rejected.
E555 burned-E546 OOD validation — MIXED GENERALIZATION
Strong validation wins included SparseInversionPatches ~0.503/0.436x, MonotoneBurstNoise ~0.981/0.977x, and LocalizedAlternatingBursts ~0.983/0.978x. Stable losses included DuplicateRunMosaic ~1.035/1.032x, StickyRandomWalk ~1.025/1.041x, AlternatingWithJitter ~1.012/1.014x, and ChunkEntropyMixture ~1.045/1.095x. Diagnostics implicated both scanner economics and residual code-shape effects. E546 is burned and must not be used for later tuning.
E557 — self-amortizing/low-yield scanner cutoff — REJECT
A canonical-derived cutoff stopped scanning after poor early batching yield. The extra control logic destroyed the MDR signal and introduced severe regressions, including ~13% on Reverse. No new holdout consumed.
E558 — coherent-hint coupled natural-run batching — RETAINED / PROMOTED BY EXPLICIT TRADEOFF
User-directed promotion of the E555 mechanism despite documented regressions because the large MDR/Noise/SparseInversionPatches gains were judged more valuable. Mature phase-map routing stays unchanged; only the fallback/base simulated-direct sorter becomes `include/jessesort/detail/pipelines/reference/simulated_direct_coherent_natural_batch.h`. The previous phase-map-mature fallback remains callable for rollback/provenance.
Key retained evidence on EPYC 100k: Noise5 ~0.96–0.97x production, Noise10 ~0.96–0.98x, deep MDR ~0.74–0.76x. Accepted costs include OrganPipe ~1.014–1.026x and the E546 OOD losses listed above. This is an explicit exception/tradeoff, not a change to the normal >1% regression policy. E556/E557 remain rejected.
E559–E574 — TAIL-SPAN / FRAGMENTATION RESPONSE SERIES (2026-08-31) — CONSOLIDATED
Host: AMD EPYC 9V74 80-Core Processor. This series followed retained E558 coherent natural-run batching and tested whether the number of live patience-pile regions spanned by a proven monotone run could guide a better continuation strategy. Burned E546 OOD data was diagnostic-only; behavioral decisions used canonical same-host A/B measurements.
E559 — fragmentation diagnostic — RETAIN DIAGNOSTIC SIGNAL
Instrumented E558 only; no sorter behavior change. At 100k over 100 deterministic canonical seeds, >=3-piece fragmentation was common and strongly input-dependent: MDR ~25.2% of eligible natural runs, BlockSorted ~44.6%, Sawtooth ~95.7% (84.8% at 8+ pieces). Conclusion: local tail-span/fragmentation is a real live state variable worth preserving for future diagnostics.
E560 — exactly-two-pile chunk batching — REJECT
Bulk-tagged a proven monotone run across exactly two live piles while preserving live tails/counts. Corrected 100k x500 A/B versus current E558 showed strong Sawtooth regression (~1.185/1.072x) with no robust compensating benefit; reject. An initial comparison against the wrong rollback control was discarded and is not evidence.
E561 — dense ghost runs, span>=3, min length 32 — INITIALLY REJECTED / SIGNAL RETAINED
Detached sufficiently fragmented natural runs from live pile geometry and reconstructed them later as independent runs. Focused evidence showed a large MDR gain but severe Sawtooth loss; mechanism was not promoted at this point. This decision is later superseded by explicit E575 promotion after full tradeoff review.
E562 — moderate/span-capped ghosting — REJECT
Restricting dense ghosts to a narrower multi-pile span did not preserve the MDR benefit cleanly and did not solve the broader geometry/code-shape tradeoff.
E563 — endpoint-anchored ghost reconstruction — REJECT / PLACEMENT SIGNAL RETAINED
Changed ghost placement/reconstruction using value-local endpoint anchoring. Timing was too unstable for promotion, but it established that reconstruction placement can materially change Sawtooth/MDR behavior. This signal motivated later E571/E585 reconsideration.
E564 — live-geometry diagnostic / fragmented-suffix response — DIAGNOSTIC RETAINED, BEHAVIOR REJECTED
Geometry instrumentation explained the core tradeoff: Sawtooth dense ghosting removes nearly all live pile structure, while MDR keeps substantially more useful live geometry. A direct fragmented-suffix ownership/handoff response was not retainable. The geometry finding remains the causal basis for later threshold/guard work.
E565–E570 — fragmentation exits / live-budget / code-shape isolation — ALL REJECTED
Tested live-pile-budgeted dense ghosts, scalar/hinted exits from natural-run mode, no-ghost tax isolation, clean-provenance budget rerun, compile-time specialization, and noinline sidecar variants. Some individual cohorts were promising, but none survived same-host A/B without >1% regressions or process/code-layout instability. E568 is the clean provenance rejection of the live-pile-budget idea; later relative-geometry work must therefore use a distinct mechanism rather than replay it.
E571 — anchored ghost immediate first-merge — PROMISING, REJECT AS-IS
Anchored detached ghosts and immediately merged them with an adjacent reconstructed run. Deep MDR isolation was among the strongest post-E561 signals (~0.936/0.930x production in the earlier branch), and Sawtooth initially improved slightly, but OrganPipe/broader cohorts exposed unaffected-path/code-shape regressions. Preserve for later clean reconsideration; do not promote this implementation.
E572 — no-ghost E558 reconstruction fast path around E571 — REJECT
Tried to preserve exact E558 reconstruction when no ghosts existed while using E571 placement only on ghosted paths. The MDR benefit disappeared and Sawtooth/MixedPhase3 regressed; reject.
E573 — >=3 live-pile chunk absorption — REJECT
Bulk-assigned contiguous pieces of a fragmented run to their real existing piles. This preserved formal live-pile ownership but badly damaged useful decomposition geometry: 100k MDR was ~1.26–1.50x and Alternating ~1.05–1.09x production.
E574 — fragmentation-triggered no-hint continuation — REJECT
On first >=3-piece fragmented natural run, stopped harvesting and finished with the existing no-hint patience continuation. Broadly slower, especially MDR (~1.20/1.05x) and Alternating (~1.01/1.10x); reject.
SERIES CONCLUSION AFTER E559–E574
E575 — RETAIN >=3-PILE / MIN-32 DENSE GHOST RUNS (2026-08-31) — RETAINED / NEW PRODUCTION FALLBACK
CPU evidence: AMD EPYC 9V74 80-Core Processor, CPU 2 pinned. This is a user-directed retain of the clean E561 min-32 mechanism after explicit review of the full canonical tradeoff; no E562-E574 mechanism is included.
Production architecture remains mature phase-map routing first. Only the fallback/base sorter changes from E558 coherent natural-run batching to the clean dense-ghost descendant. In the existing >=75% hint-coherent natural-run continuation, ordinary one-pile coupled batching remains unchanged. A proven monotone run is detached as a ghost run only when (a) its endpoint mapping spans >=3 live pile regions and (b) run length >=32. The 32-element floor is specifically a merge-economics guard against proliferating tiny independent runs. Ghosts remain absent from live patience tails until reconstruction.
E575 FOLLOW-UP STATUS
The originally queued recovery work is now complete: fixed span sweep E577-E579, minimum-length sweep E580-E582, relative live-geometry guards E583-E584, and clean E571 reconsideration E585. All were rejected; retained span3/min32 remains production. E585 also discovered the shared correctness hole fixed by E586. Intel Xeon Platinum 8573C provenance validation remains outstanding.
E577 - DENSE-GHOST PILE-SPAN 2 (MIN LENGTH 32) - REJECT / CAUSAL SIGNAL RETAINED
Host: AMD EPYC 9V74 80-Core Processor; 100k, 500 trials/cell, 2 warmups, CPU 2 pinned, same-seed candidate/current production A/B order swap. Candidate was cloned from current E576/E575 production dense-ghost code and changed only the two admission tests from pieces>=3 to pieces>=2; min ghost length remained 32. Sawtooth candidate/current ratios were 1.0072 (A) and 1.0673 (B). MixedDirectionRuns ratios were 0.9945 (A) and 0.9302 (B), too order/code-layout sensitive to credit as a stable gain. Geometry over 100 canonical trials explains the direction: Sawtooth moves from 44 ghosts / 94,732 ghosted elements / 4 live piles at span3 to 46 / 97,737 / 3 at span2; MDR moves from 8.41 ghosts / 30,679 elements / 15.24 live piles to 12.85 / 45,225 / 13.54. Span2 therefore worsens the near-total Sawtooth geometry replacement while detaching substantially more MDR state. No canonical expansion because the required Sawtooth/MDR isolation was not promising. Production remains span3/min32.
E578 - DENSE-GHOST PILE-SPAN 4 (MIN LENGTH 32) - REJECT
Host/method: same EPYC 9V74 isolated protocol as E577; candidate differs from E576/E575 production only by pieces>=4. Sawtooth candidate/current ratios were 0.9315 (A) and 1.0082 (B), strongly order sensitive. MixedDirectionRuns was consistently and materially worse: 1.2321 (A) and 1.1617 (B). Geometry over 100 canonical trials: Sawtooth 43 ghosts / 92,579 elements / 5 live piles versus production 44 / 94,732 / 4; MDR 5.60 ghosts / 20,930 elements / 16.24 live piles versus 8.41 / 30,679 / 15.24. Raising the span threshold begins preserving live geometry, but it removes too much of the MDR dense-ghost benefit. No canonical expansion. Production unchanged.
E579 - DENSE-GHOST PILE-SPAN 5 (MIN LENGTH 32) - REJECT; FIXED SPAN SWEEP CLOSED
Host/method: same EPYC 9V74 isolated protocol as E577/E578; candidate differs from E576/E575 production only by pieces>=5. Sawtooth candidate/current ratios were 0.9860 (A) and 1.0120 (B), near neutral/order sensitive. MixedDirectionRuns regressed severely and consistently: 1.4727 (A) and 1.2568 (B). Geometry over 100 canonical trials: Sawtooth 42 ghosts / 90,426 elements / 6 live piles; MDR 3.45 ghosts / 13,317 elements / 16.95 live piles. The span sweep therefore brackets the retained choice cleanly: span2 is too permissive and worsens pathological geometry replacement; spans4/5 are too restrictive and progressively destroy the MDR recovery. Retain the existing generic span3/min32 rule. Because none of 2/4/5 passed the required Sawtooth/MDR isolation, no broader canonical validation was warranted. Next active TODO is the minimum ghost-run economics sweep at fixed span3, beginning with min lengths 64 and 128.
E580 - DENSE-GHOST MINIMUM RUN LENGTH 64 (SPAN 3) - REJECT / ECONOMICS SIGNAL
Host: AMD EPYC 9V74 80-Core Processor. Candidate cloned from current E576/E575 production and changed only DENSE_GHOST_MIN_RUN_LENGTH 32 -> 64. Initial Sawtooth/MDR 100k x500 A/B timings were misleading because geometry telemetry later proved those families make exactly the same ghost decisions at min32 and min64; their timing swings are code-shape/process noise and are not threshold evidence. Canonical telemetry (100 trials at 10k/100k) identified the semantic effects almost entirely in Noise5 and MixedPhase3. On those affected cells, 10k x500 A/B was mildly favorable/neutral (Noise5 0.9891/0.9932, MixedPhase3 0.9911/0.9966), but 100k was unstable (Noise5 1.0154/0.9849, MixedPhase3 0.9909/1.0460). Do not retain 64.
E581 - DENSE-GHOST MINIMUM RUN LENGTH 128 (SPAN 3) - PROMISING ISOLATION, REJECT AFTER FULL CANONICAL VALIDATION
Host: AMD EPYC 9V74 80-Core Processor. Candidate differs only by minimum ghost length 128. Telemetry again showed no Sawtooth/MDR geometry change versus min32. Affected-cell 10k x500 was neutral-to-better: Noise5 0.9947/0.9863, MixedPhase3 0.9985/1.0046. Initial 100k MixedPhase3 was order-sensitive (1.0281/0.9947), so it was deepened to x1000 and resolved favorably at 0.9872/0.9814; Noise5 x1000 was 0.9849/0.9955. This justified full canonical validation. Full 10k x500 A/B then exposed repeatable hot-path regressions despite identical ghost decisions on those families, especially Random%25 1.0229/1.0580 and Random 1.0086/1.0068. Full 100k also showed regressions including Alternating 1.0171/1.0589, MixedPhase12 1.0259/0.9902, OrganPipe 1.0105/0.9987, and Sawtooth 1.0024/1.1001. Because the project rejects >1% generic regressions absent an explicit tradeoff and these are codegen/layout costs on unaffected paths, min128 is not promoted. Production remains min32.
E582 - DENSE-GHOST MINIMUM RUN LENGTH 256 (SPAN 3) - REJECT; MIN-LENGTH SWEEP CLOSED
Host: AMD EPYC 9V74 80-Core Processor. Min256 was tested because the min128 affected-cell trend was promising. Telemetry showed min256 still leaves Sawtooth geometry unchanged and only changes a few 10k MDR trials; its main semantic effects remain Noise5/MixedPhase3. 10k x500 A/B: Noise5 0.9829/0.9749, MixedPhase3 0.9938/0.9934, MDR 1.0491/0.9794. 100k: Noise5 1.0342/0.9649 and MixedPhase3 0.9886/0.9973. Combined with the min128 full-suite failure, there is no robust generic case for raising the minimum. Retain span3/min32. Important causal result: the severe Sawtooth regression accepted at E575 is NOT caused by 32-255 element ghosts; Sawtooth's detached runs in the measured geometry are all already >256 (44 ghosts, 94,732 elements, average/max 2,153), so the next recovery target should be a relative live-geometry admission guard rather than a larger absolute run-length floor.
E583 - RELATIVE SAME-DIRECTION LIVE-GEOMETRY GUARD, 50% SPAN CAP - REJECT
Host: AMD EPYC 9V74 80-Core Processor, CPU2 pinned. Distinct from rejected E568: no cumulative ghost budget. Starting from retained E575/E582 production (span>=3, min32), a fragmented run may ghost only when its endpoint span is no more than half of the currently live pile count in the same direction (pieces*2 <= live piles). Geometry over 100 canonical 100k trials behaved as intended: Sawtooth moved from production 44 ghosts / 94,732 detached elements / 4 live piles to 0 ghosts / all 48 live piles; MDR moved from 8.41 / 30,679 / 15.24 to 1.96 / 6,309 / 17.64. But focused 100k x500 A/B showed Sawtooth 1.154/1.114 and MDR 1.058/1.151 versus production; even zero-ghost Sawtooth remained materially slower, demonstrating code-shape tax while the guard also removed too much MDR ghost work. Reject without canonical expansion.
E584 - RELATIVE SAME-DIRECTION LIVE-GEOMETRY GUARD, LEAVE-TWO RULE - REJECT; RELATIVE-GUARD TODO CLOSED
Host: AMD EPYC 9V74 80-Core Processor, CPU2 pinned. Less restrictive follow-up to E583: retain span>=3/min32 but admit a ghost only when at least two currently live same-direction piles lie outside the candidate span (pieces + 2 <= live piles). This is input-local geometry gating, not E568's cumulative ghost-count budget. Geometry at 100k: Sawtooth again becomes 0 ghosts / 48 live piles; MDR retains 4.51 ghosts / 15,459 detached elements / 16.70 live piles; BlockSorted retains 5.34 / 28,122 / 16.02. Focused x500 A/B: at 100k Sawtooth 1.153/1.121, MDR 1.138/0.921 (process-order unstable), BlockSorted 0.998/0.995, Noise5 0.989/1.000. Required 10k x500 affected-cell check was worse and stable on the two main targets: Sawtooth 1.216/1.216, MDR 1.146/1.135; Noise5 1.026/1.001 and BlockSorted 0.984/0.998. Reject. The semantic goal is achievable--both relative guards prevent near-total Sawtooth geometry replacement--but admission-only relative gating either discards too much useful MDR ghosting or incurs large code-shape costs, and the less restrictive rule still regresses MDR at 10k. Production remains span3/min32 unchanged. Close simple relative-admission guards; next recovery work moves to reconstruction/merge placement, with E571 as the explicit reconsideration branch.
E585 — CURRENT-PRODUCTION ANCHORED GHOST IMMEDIATE FIRST-MERGE RECONSIDERATION (2026-09-01) — REJECT; FRESH OOD DISCOVERS SHARED CORRECTNESS BUG
Host/method: AMD EPYC 9V74 80-Core Processor, CPU2 pinned. Clean provenance audit confirmed archived E571 differs from retained E575/E576 span-3/min-32 dense-ghost fallback only in anchored ghost placement metadata plus immediate first merge (`std::inplace_merge`) during reconstruction. Rebased/renumbered as E585 and compared against current public production with one algorithm/input/size cell per process, 2 warmups, canonical 10k/100k x500, both process orders.
Canonical result: reject. 10k geometric mean E585/production 1.0080x; 100k 1.0040x. Stable problem rows include 10k Sawtooth 1.0169/1.0101 and MixedDirectionRuns 1.0971/1.0619. At 100k MDR remained order-confounded (1.1816/0.9143), Alternating regressed 1.0636/1.0169, Sawtooth 1.0087/1.0216, while Noise10 improved 0.9710/0.9792. The old E571 deep-MDR win does not survive clean current-production/full-matrix reconsideration.
--- E575-E586: dense-ghost production chain ---
E575 — dense ghost runs, span>=3 / min length 32 — RETAINED PRODUCTION FALLBACK
- In >=75% hint-coherent continuation, proven monotone runs spanning >=3 live pile regions may detach as ghost runs when length>=32. Retained by explicit tradeoff: large MDR recovery with an accepted Sawtooth cost; phase-map remains first owner.
E577-E579 — pile-span sweep 2/4/5 — REJECTED; SPAN 3 RETAINED
- Span2 detached too aggressively and worsened pathological geometry replacement; spans4/5 progressively destroyed the MDR benefit. Fixed span sweep closed at 3.
E580-E582 — minimum ghost length 64/128/256 — REJECTED; MIN 32 RETAINED
- Larger minimums did not change the key Sawtooth/MDR geometry and introduced unstable/unaffected-path regressions. The Sawtooth issue was therefore not caused by short 32-255 element ghosts.
E583-E584 — relative live-geometry admission guards — REJECTED
- Both guards could prevent near-total Sawtooth ghosting, but either removed too much useful MDR ghosting or imposed code-shape/10k regressions. Simple admission-only relative guards closed.
E585 — anchored immediate first-merge reconsideration — REJECTED; OOD CORRECTNESS BUG DISCOVERED
- Fresh canonical/OOD validation rejected the placement mechanism and exposed a shared UnevenSawBands correctness failure in production.
E586 — dense-ghost trailing-equivalent ownership repair — RETAINED
- Fixed stale live-pile ownership after ghost detachment by extending the detached run through an immediately trailing comparator-equivalent plateau. Reduced regression and frozen OOD correctness pass; timing was neutral enough to retain.
--- E586-E600: immediate pre-E601 research ---
E586 — dense-ghost trailing-equivalent ownership correctness repair — RETAINED
- Fixed stale live-pile ownership after ghost detachment by absorbing an immediately trailing comparator-equivalent plateau into the detached run. Frozen OOD correctness and the reduced regression case pass; timing was neutral enough to retain.
E587 — opposite-game overflow rescue — OPEN SIGNAL, UNRESTRICTED FORM REJECTED
- Rescue can shrink Random overflow but mutates opposite tails and damages structured geometry. Keep only as an independent future mechanism; any revisit must use current production and a cheap structural admission rule.
E588 — frozen-single high-entropy overflow backend — PROMISING EXPERIMENTAL
- Direct high-entropy sorting is much faster on large Random residuals but not on all structured residuals. Preserve the backend-ownership signal; do not globally replace recursive Patience/std::sort ownership without a robust gate.
E589 — E587 rescue on top of E588 — NO ADDITIVE RETAIN
- Shrinking overflow no longer pays once E588 makes overflow sorting cheaper; added rescue search/insertion cost offsets the residual reduction.
E590 — frozen-vs-production boundary audit — RESEARCH BOUNDARY ONLY
- E588-enhanced frozen-single approached production on Random/Random%25 on EPYC but remained much slower on structured families. Do not pursue broad frozen catch-up absent a new mechanism.
E592 — Noise5/Noise10 dense-ghost ownership audit — NO PRODUCTION CHANGE
- Sparse-distributed-disorder/phase-map ownership dominates these inputs, so observed whole-binary movement cannot safely be assigned to dense-ghost admission. Early relocation of the sparse preroute was process/layout unstable and rejected.
E593 — exact internal-write-minimized E375 merge tree — STRONG SIGNAL, INITIAL INTEGRATION PARKED
- Exact alphabetic DP on compact reconstructed geometries materially reduced predicted writes and MDR merge cost, but the first EPYC integration showed inactive-path code-layout movement. This mechanism is the direct predecessor of retained E601.
E594 — global high-entropy route before phase-map — REJECTED ARCHITECTURALLY
- Moving high entropy ahead of phase-map was timing-neutral/noisy and would preempt phase-map's mixed/local-entropy ownership. Keep phase-map first.
E595 — phase-map-local entropy handoff — PROMISING EXPERIMENTAL
- Local Entropy regions can hand directly to the existing high-entropy backend and improve mixed-phase families, but separately compiled forms perturb inactive Random. Revisit only with current-production/layout isolation.
E596 — noinline executor separation — REJECTED AS ROBUST OPTIMIZATION
- Apparent Random gains did not survive clean rebuild; preserve only as a code-layout caution.
E599 — fresh 100k E595/E596 audit — E596 REJECT CONFIRMED; E595 SIGNAL PRESERVED
- Broad canonical rerun confirmed E595's semantic mixed-phase signal but did not justify production promotion; OOD evidence was not decisive enough to override layout concerns.
E600 — merge-realization movement audit / direct four-run tournament — AUDIT RETAINED, FOUR-WAY IMPLEMENTATION REJECTED
- E375-selected compact geometry remained an important target. Direct four-run realization did not retain, motivating E601's exact internal-write objective without changing production routing.
--- E601-E615: production and active donor chain (expanded) ---
E601 — compact internal-write-optimal E375 tree — RETAINED HISTORICAL PRODUCTION FOUNDATION
- Exact alphabetic DP minimizes internal merge-output writes for compact reconstructed geometries (<=32 runs); larger geometries keep mature scheduling. Target-8573C adjudication isolated the intended compact/MDR win. E601 later became the foundation for E656/E659/E661 production.
E602-E603 — direct-coordinate / segmented reconstruction attempts — REJECTED
- Scattered direct-coordinate gathering and segmented/fused-first-merge realizations did not justify their added machinery. Preserve only the broader idea of avoiding redundant reconstruction passes when a future representation makes it cheap.
E604-E608 — 16-bit blueprint representation line — SIGNAL RETAINED; DIRECT PROMOTION REJECTED
- Narrow tags showed real cache/structured signal. E607 is the retained mechanism donor: use 16-bit tags when representable and safely restart wide on exact ID overflow. Blanket narrowing, conservative fixed reserves, and true prefix widening were rejected. Historical E607 branch code is not a production replacement; any revisit must transplant only the representation mechanism onto E661 or E691.
E610-E615 — E601 production adjudication / E607 propagation audit — E601 RETAINED; E607 TRANSPLANT REJECTED
- Same-host isolated-binary audits corrected earlier layout/session contamination and established E601's real compact-geometry benefit. Whole-branch E607 composition and a surgical narrow-tag transplant both failed to justify production propagation. This sequence established the project rule that retained mechanisms must be rebased surgically rather than by composing stale experiment branches.
E617 — narrow-blueprint noisy-sorted ownership audit — SIMPLE SALVAGE REJECTED
- The narrow-tag opportunity did not map cleanly onto noisy-sorted ownership/size paths. Retain representation evidence only.
E618/E623 — exact alphabetic scheduler expansion — RESEARCH SIGNAL; EXPANSION REJECTED
- Exact scheduling through bounded run counts was explored beyond E601's compact gate. Larger reach did not retain broadly enough to justify added cost; keep compact exact scheduling as the useful mechanism.
E619 — phase-local entropy handoff rebased onto production — REJECT; SIGNAL RETAINED
- Local entropy ownership remained interesting, but production/layout-stable evidence did not justify promotion. The general lesson—phase-local backend choice rather than whole-input classification—feeds the later E680/E691 architecture.
E620-E622 — post-decision / insertion-time 16-bit reconstruction packing — REJECTED
- Multiple narrow-ID placements were tested on target 8573C and failed to recover enough speed. Straightforward narrow-blueprint salvage is closed pending a materially different representation/fusion mechanism.
E624-E625 — opposite-game rescue / high-entropy overflow rebases — REJECTED
- Rebased E587/E588 ideas onto production and separated backend from rescue effects. Opposite-game rescue remained too disruptive; cheaper high-entropy overflow ownership was interesting but did not justify the old frozen strategy as production.
E626-E631 — propagation audit + single-game propagation series — AUDIT RETAINED; BASELINE PROPAGATION REJECTED
- Audited which historical variants actually inherit shared production mechanisms, then tested E601 scheduling across single-game physical/reverse/galloped/front-insert variants. No broad propagation retained; divergences remain intentional.
E632/E635 — linked-direct sparse distributed disorder — RETAINED LOCAL DONOR
- Sparse-disorder handling retained within linked-direct and its useful size range was extended to 10k. Preserve as a specialist mechanism donor, not a public route assumption.
E633 — within-pile value-density / early cross-game merge — TODO SIGNAL
- Preserve as a future geometry idea only; no code change.
E636-E637 — three-way canonical + frozen OOD bake-offs — MEASUREMENT EVIDENCE
- Standalone production/E601/historical-direct comparisons showed complementary strengths and reinforced that same-host whole-binary differences can include code-layout effects. No production change; detailed raw cohorts are intentionally not required in the cleaned package.
E638-E648 — strict no-allocation / Rust `sort_unstable` contract track — RETAINED PUBLIC API
- E638 revived the maintained noalloc lineage with a strict bounded-worst-case fallback; E641 retained adaptive leaf-only parity scratch; E645 closed an O(n log^2 n) reachable sparse route by forcing the bounded O(n log n) fallback; E646 passed expanded type/comparator-panic/allocation gates.
- E647 exposed the retained strict contender as `jessesort::sort_unstable_noalloc`. The performance-oriented noalloc API is now named `jessesort::noalloc::sort_adaptive`. E639/E640/E642/E644 scratch/admission micro-variants were rejected; E648 is reporting only.
- Contract intent: no heap allocation inside the strict sort, bounded stack/metadata, move-only support, comparator-exception permutation preservation, deterministic behavior, and worst-case O(n log n) ownership on every reachable route. This is the noalloc lineage that future ipnsort comparisons should use alongside the maintained performance-oriented noalloc variation.
E649 — exact two-run virtual-merge control — RESEARCH RETAIN
- Verified that exact logical two-run merge simulation can defer physical movement without changing stable merge semantics. This opened the simulated-merge line; no production change.
E650 — reconstructed adj-pair geometry — RESEARCH RETAIN
- Captured exact first-level reconstructed pair geometry across canonical inputs for downstream ownership/movement studies. Geometry artifact only; no production change.
E651 — permutation dependency / liberation intervals — RESEARCH RETAIN; fixed tiles rejected
- Natural closed dependency intervals can be small on Random but become global on several structured inputs. Reject universal fixed-size tile realization; retain adaptive natural-closure locality as evidence.
E652 — adaptive natural-closure physical realization — REJECT; locality signal retained
- Physical closure-local movement reduced Random movement cost but explicit ownership/permutation construction erased the gain. Future work should consume latent ownership inside an already-required reconstruction/merge pass rather than materializing a separate representation.
E653 — value-aware virtual third-run fusion — RESEARCH RETAIN
- Fusing a virtual A+B stream directly against C can skip one physical merge level and improves several 100k workloads, but is not universally profitable. Retain the primitive; require general admission geometry.
E654 — adaptive burst/gallop third-run fusion — RESEARCH RETAIN
- Winner-streak-triggered binary boundary searches recover large low-cardinality/block-structured wins, but hurt high-entropy/fine-period cases when unconditional. Retain burst/gallop as a local primitive, not a global mode.
E655 — local adaptive third-run fusion — RESEARCH RETAIN
- Per-triple local adaptation between scalar virtual fusion and burst/gallop preserves the generic mechanism without whole-input family routing. This became the donor for production integration.
E656 — local third-run fusion integrated at E601 seam — MECHANISM RETAINED
- Integrated E655 at the general reconstruction seam while preserving staged E601 behavior when local fusion does not admit. The initial admission policy still needed broader adjudication.
E659 — E656 local adaptive fusion promoted — PRODUCTION RETAIN
- Promoted the E656 mechanism into the public production lineage after broad same-host validation. This superseded E601 as the active performance path while retaining E601 staged fallback behavior.
E660 — continuous expected-savings admission — REJECT
- A static per-triple saved-writes/log-cost score did not generalize reliably. Retain causal online signals such as productive bulk-span yield, failed attempts, winner-streak persistence, and cooldown/backoff for future transferable admission work.
E661 — ordered-boundary-collapse correctness repair — CURRENT PUBLIC PRODUCTION
- Fixed the E656/E659 ordered-boundary-collapse correctness issue without changing the intended production architecture. `jessesort::sort` is the E661-corrected E659/E656 local-fusion pipeline.
E672 — real first-level blueprint morph integration — CHECKPOINT / NOT ADJUDICATED
- Established an end-to-end morph integration seam after simulated insertion. Preserved as research infrastructure rather than a promoted optimization.
E673 — production route / blueprint opportunity census — TELEMETRY RETAINED
- Measured where production reconstruction geometry actually exposes morph opportunities. No routing or production code change.
E674 — whole-array fallback first-level morph — UNCONDITIONAL FORM REJECTED
- The primitive is viable but whole-array admission is too broad. Retain the seam and require local/phase evidence.
E675 — phase-region first-level morph — UNCONDITIONAL POLICY REJECTED
- Phase-localization improved causal placement but still did not justify unconditional use. Retain phase-scoped morph as a research primitive.
E677 — Random high-entropy fallthrough audit — COMPLETE
- Identified avoidable high-entropy misses caused by accumulated short-prefix shape vetoes. No production change; motivated E678/E679 descriptor cleanup.
E678 — unified first-eight high-entropy shape veto — RESEARCH RETAIN
- One first-eight direction scan broadens generic high-entropy ownership while preserving older low-cardinality/dominant-value behavior. EPYC timings were strongly process/layout sensitive; do not promote without target-CPU/OOD adjudication.
E679 — unified first-eight shape descriptor — RESEARCH RETAIN
- Replaced duplicated first-four/eight shape checks with one shared descriptor. Cleaner architecture and some OOD opportunity gains, but inherited high-entropy ownership broadening remained unresolved; production stayed E661.
E680 — clean three-behavior phase architecture — RESEARCH RETAIN
- Prototyped a generic phase classifier with natural-monotone, direct/Patience-hostile, and residual-Patience behavior. Demonstrated large complementary wins but severe losses versus mature E661 specialists; became the architectural base for pressure work.
E681 — E680 route census — TELEMETRY RETAINED
- Quantified ownership of the three generic behaviors to distinguish classifier effects from backend effects. No production change.
E682 — per-game rolling pile-pressure telemetry — RESEARCH RETAIN
- Added dual-Patience per-game pressure observation and showed that the two games can diverge materially. Motivated per-game rather than whole-phase fallback protection.
E684 — T=9 dual-game Patience pressure valve — RESEARCH RETAIN
- Added a geometry-derived T=9 pressure valve to prevent pathological Patience growth. Retained as a generic safety mechanism; not production-promoted.
E685 — E680 + E684 integrated architecture — RESEARCH RETAIN
- Combined the three-behavior phase classifier with T=9 pressure protection. Established the architecture later refined by E688/E690/E691.
E686 — E661 + T=9 production-overlay audit — REJECT OVERLAY
- Overlaying the pressure valve onto mature E661 did not justify production propagation. Kept the pressure mechanism on the cleaner research architecture instead.
E688 — phase-local joint-game T=9 context gate — RESEARCH RETAIN
- Applied pressure fallback with phase-local context rather than global condemnation. Improved safety, but joint-game fallback still lets one pressured game terminate a healthy opposite game.
E690 — per-game T=9 pressure overflow — RESEARCH RETAIN
- Freeze only the pressured game into deferred overflow while the healthy game continues Patience/T=9 monitoring; if both games pressure, retain E688 joint fallback. Overflow preserves natural runs >=32 and normalizes shorter material into 32-element bands before merge scheduling.
- Retained as the strategic fallback architecture that became E691; rolling-count replacement for pressure popcount was rejected.
E691 — per-game overflow architecture — FORWARD BACKBONE RETAIN
- Initial 500-trial parent adjudication against E688 looked mixed, but the reopened 1000-trial 100k canonical + OOD audit showed many apparent regressions were order-sensitive while several OOD wins reproduced. MixedPhase12 remained a notable parent-relative concern.
- Architectural decision: retain E691 as the forward generalization-first backbone because its phase-local classifier and per-game pressure fallback provide safer, less benchmark-specific behavior than the mature specialist production router.
E692 — E691 vs E661 production, 100k x1000 canonical + 49 OOD — COMPLEMENTARY PERFORMANCE MAP
- Intel Xeon Platinum 8573C, CPU2 pinned, isolated binaries, 2 warmups + 1000 trials/cell; every first-pass E691 regression >1% received reversed-order confirmation.
- E691 has large complementary wins (including OrganPipe, MixedPhase3/12 and multiple affine/entropy/block-shuffle OOD families) but severe repeatable losses on mature production specialist paths such as Alternating, Random%25, Sawtooth, MDR, BlockSorted, Rotated, low-cardinality, and low-noise sorted cases.
- Decision: do not replace E661 yet. Use E692 as the speed-recovery map for E691; import only generic structural mechanisms, never named-family fingerprints.
E693 — E691 vs std::sort expanded OOD controls, 100k x1000 — MEASUREMENT RETAINED
- Ran the missing 49 `std::sort` control cells on the same 8573C and reused E692 E691 medians. E691 beats `std::sort` on 41/49 OOD families; losses concentrate in RandomWalk/StickyRandomWalk/PeriodicPerturbed, BlockShuffle16/32, and low-noise SortedNoise.
- Use these failures as fallback-safety targets, not as justification for family-specific routing.
E694 — E691 promoted alongside E661 — ADMINISTRATIVE RETAIN
- E661 remains public/default `jessesort::sort`. E691 is now a maintained generalization-first backbone exposed as `jessesort::backbone::sort` through an independent engine namespace so both APIs can coexist in one translation unit.
- E691 is the forward optimization target; E661 remains the speed donor/reference until E691 recovers the large specialist cliffs without sacrificing its OOD/generalization envelope.
- Replacement bar: eliminate multi-x cliffs, avoid broad >10% established regressions (prefer <=5% on common paths), preserve major E691 OOD wins, and keep both paths independently benchmarkable until convergence.
E695 — repository cleanup / publication packaging — COMPLETE
- Consolidated logs and documentation, refreshed the E661/E691 APIs and benchmark surfaces, pruned stale artifacts, and retained only the self-contained viz2 script/MP4. All builds, API smoke tests, canonical/OOD layouts, and one-trial end-to-end targets passed; final clean source tree was ~4.6 MiB.
E696 — noalloc batch reclamation revisit — RESEARCH RETAIN; NO PROMOTION
- Compared 64-to-32 adjacent-pair and shortest-two-of-three reclamation against adaptive noalloc's smallest-adjacent policy. Correctness passed, but order-swapped 1m evidence cleared neither the >1% retention bar; retain only as research toward balanced bounds/in-place merge improvements.
E697-E700 — E691 tails-only classifier sizing — DIAGNOSTIC RETAIN
- Tail-count sweeps from 4-64 sampled values found the useful ceiling near 12-16; E700 selected a 16-value probe and Patience-friendly threshold of <=4 total tails (87.65% balanced accuracy).
E701 — 16-value / 4-tail classifier integration — RETAIN IN FORWARD BACKBONE
- Replaced E691's 32-value occupancy probe; order-neutral GM was 0.9801 vs E691 canonical and 0.9550 across 49 OOD families, with only two small stable OOD regressions. Retain in the backbone, but E701 remained slower overall than E661 on mature specialist paths, so no public promotion.
- E701/E661 GM was 1.5093x at canonical 10k, 1.4686x at canonical 100k, and 1.0690x across OOD; remaining promotion work was downstream realization/merge convergence, not classifier redesign.
E702 — E701/E661 ghost-run and realized-run flow audit — DIAGNOSTIC RETAIN; PROTOTYPE REJECTED
- Dense ghosts did not explain the main E661 wins; BlockSorted/Rotated gaps came from E701's simple outer merge, while Sawtooth used an E661 specialist. A broad boundary-aware merge gate helped Rotated but missed BlockSorted and regressed Noise5, so it was rejected.
- Carry forward phase-local sparse-disorder repair for Noise5-like geometry and low-run merge-economics work for BlockSorted/Rotated; revisit dense ghosts only on workloads that actually enter that path.
E704 — exact low-run descriptor-permutation fast path — RESEARCH SIGNAL RETAIN; BROAD FORM REJECTED
- Exact descriptor ordering exposed useful low-run reconstruction structure, but the broad implementation did not justify changing the maintained E701 header.
- E704's `max(32,n/100)` cap was too permissive; even cap=2 in E705 produced unrelated regressions. Revisit only as a byte/layout-neutral decision at the exact local merge point, not as another outer router or run-count gate.
E705 — fixed small run-count cap for exact descriptor permutation — REJECT
- A fixed cap did not provide a reliable general admission rule; no maintained-header change.
E706 — endpoint-aware outer adjacent merge propagation — RETAIN IN BACKBONE
- Propagated ordered and reverse-disjoint whole-run shortcuts through every adjacent merge tier in E701/E691. Retained for the maintained backbone; E661 public production remained unchanged.
- E706/E701 GM was 0.8647x at 10k, 0.9296x at 100k, and 0.9130x over OOD; accepted repeatable OOD losses were SortedNoise20 1.0155x and AsymmetricPipePlateau 1.0452x.
E707 — reverse-disjoint ablation and tier census — KEEP E706
- Across 49 OOD families, ON/OFF GM was 0.9918; families where it triggered averaged 0.9539, while non-triggering families were 1.0045. Retain the shortcut; the proposed early-tier gate was subsequently rejected in E708.
- Reverse stitches were 90.33% tier 0 and 99.27% within tiers 0-1, whereas already-ordered stitches remained useful deeper in the tree.
E708 — reverse-disjoint checks only at tiers 0-1 — REJECT
- Early-tier-only gating underperformed the all-tier E706 policy; retain reverse-disjoint checking at every tier.
- Raw hit frequency was misleading because rare late-tier stitches eliminate much larger merges; OffsetRotationRamp slowed 1.1727x when late checks were removed. Future gating must estimate work saved, not stitch count.
E709 — cross-game reconstructed-run value-separation census — DIAGNOSTIC RETAIN
- Same-rank descending/ascending pile pairing substantially increased exact stitches and weighted span, especially at tier 0; opposite/normalized rank were weaker. Established a structural opportunity, not a promotion.
- Same-rank pairing increased weighted stitched span 2.065x/1.680x and tier-0 span 2.898x/2.102x at 10k/100k; 44.3%/37.6% of all observed cross-game pile pairs were disjoint.
E710 — ungated same-rank cross-game run interleave — REJECT UNGATED; MECHANISM RETAIN
- Reordering flattened runs as D0,A0,D1,A1,... increased useful adjacency but its movement/scheduling overhead outweighed stitch gains. Retain helper/provenance for more local realizations.
E711 — grouped flatten plus same-rank first pairing — REJECT UNGATED
- Preserving normal flattening and changing only first-tier pair selection still lost to naive adjacent pairing; retain as research evidence that non-adjacent access overhead dominates.
E712 — ghost/natural-run interaction with cross-game first pairing — DIAGNOSTIC
- Audited whether ghost or natural-run materialization hid the expected pairing benefit; the confound hypothesis was rejected.
E713 — cross-game merge economics / E711 attribution — DIAGNOSTIC SUCCESS
- Isolated the gap between abundant exact cross-game stitches and poor end-to-end timing: planning, non-adjacent reads, and later-tier geometry consumed the saved comparisons/moves.
E714 — same-rank first pairing with mature merge backend — REJECT; RESEARCH RETAIN
- Giving D[i]/A[i] pairs the full mature merge engine did not recover the scheduling overhead; backend quality was not the principal blocker.
E715 — virtual first-tier cross-game composites — REJECT; RESEARCH RETAIN
- Avoiding immediate physical interleave through virtual composites still failed to convert stitch potential into an end-to-end win.
E716 — zero-plan local cross-game two-level fusion — REJECT; RESEARCH RETAIN
- A local two-level fusion removed global planning but remained slower, narrowing future work toward reconstruction-time realization rather than merge-scheduler changes.
E717 — direct reconstruction into stitch-collapsed tier-0 output — REJECT; RESEARCH RETAIN
- Materializing exact D/A stitches during reconstruction reduced a merge tier but added enough bookkeeping/scatter cost to lose overall.
E718 — exact D/A span-chain deferred materialization — REJECT; RESEARCH RETAIN
- Deferring all exact same-rank span chains did not overcome metadata and realization overhead.
E719 — first-m pile-boundary replay — STRUCTURAL SIGNAL RETAINED; FIRST REALIZATION REJECTED
- Replaying a few boundary elements showed repairable cross-game alignment but the initial realization was not economical; retain the boundary-local idea.
E720 — near-stitch / bounded dirty-fringe census — STRONG STRUCTURAL SIGNAL
- Many non-exact cross-game boundaries differed only by a very small fringe, motivating bounded repair rather than wholesale cross-game rescheduling.
E721 — defect<=4 dirty-fringe cross-game repair — REJECT; RESEARCH RETAIN
- The first bounded-fringe repair implementation did not pay for its detection/repair overhead; preserve the census and mechanism for a cheaper integration point.
- E719's m=1 replay improved stitch coverage in every measured OOD family, and E720 found defect<=4 covered roughly 95-100% of several important inputs; E721 showed mature galloping already captured nearly all comparison savings. Revisit these signals only before physical D/A reassociation or inside deeper blueprint/simulated merging.
E722 — E706 outer galloping kernel re-audit — BLANKET TRANSPLANT REJECTED
- Galloping retained strong sawtooth-local wins but weakened the full suite when applied broadly; require geometry-based admission.
E723 — local geometry-gated outer gallop — PROMOTED INTO BACKBONE
- Added a tier-0/local-geometry gate that captured favorable galloping cases without the blanket regressions; retained in the maintained backbone.
- Final gate: tier 0 only; require a >=1024-element initial winning prefix and at most two winner-side changes in a 32+32 quantile sample. Later overlapping tiers retain `std::merge`.
E724 — E723 vs E661 and std::sort canonical audit — RETAIN E723; NO PUBLIC REPLACEMENT
- Confirmed the gated mechanism while preserving the broader conclusion that the backbone still had E661 specialist cliffs; continue convergence rather than replace E661 solely from this result.
E725 — E661 sparse-distributed-disorder route ablation — DONOR CONFIRMED
- Noise5/Noise10 ablation identified E661's sparse-128 repair route as the relevant generic donor for the backbone, rather than dense-ghost behavior.
E726 — middle-density sparse-disorder bypass — PROMOTED
- Ported a generic middle-density sparse-disorder admission/repair path onto E723, recovering important low-noise behavior without family fingerprints; became the maintained backbone.
E727 — maintained-header rename and noalloc policy rebase — ADMINISTRATIVE RETAIN
- Renamed the maintained backbone to `simulated-direct_live-phase` and rebased adaptive/strict noalloc away from stale E661 specialist routing. Shared the E726 sparse admission predicate; timing adjudication was deferred, while correctness/build smoke passed.
E728 — live-phase noalloc rebase bakeoff — DIAGNOSTIC RETAIN
- Benchmarked rebased adaptive and strict live-phase variants, exposing structured-path regressions and motivating a generic whole-input natural-run prepass rather than restoration of family-specific routes.
E729 — adaptive live-phase global natural-run prepass — RETAINED IN PRODUCTION
- After initial rejection, the final adjudication retained the size-gated whole-input natural-run prepass with adaptive live-phase noalloc. This restored important structured behavior while keeping the cleaned routing policy.
- The prepass is enabled only for n>=50000 and retains an early repeated-overlap bailout; OverlappingSortedBlocks remained an explicitly accepted adaptive regression at 1.397x by independent medians / 1.255x balanced per-trial.
E731 — strict live-phase global natural-run prepass — RETAINED IN PRODUCTION
- Audited and retained the E729-style size-gated prepass for strict noalloc, preserving its zero-allocation and worst-case O(n log n) contract.
E732 — allocating global natural-run prepass — PROMOTED
- Added the same generic prepass ahead of allocating `simulated-direct_live-phase`; final adjudication retained it after the initial research-only stage.
- Admission uses <=64 natural runs with the early repeated-overlap bailout; accepted deep-confirmed regressions were MultiTurnAffine ~1.035x and BlockShuffle16384 ~1.160x.
E733 — E732 vs E661 closeout — E732 PUBLIC; E661 EXPERIMENTAL
- Promoted E732 as public/default `jessesort::sort` after closeout; preserved E661 as an experimental speed donor/reference.
- E732/E661 GM was 1.3094x at canonical 10k and 1.1983x at 100k, but 0.9227x over 49 OOD families; promotion deliberately favored the general architecture over E661's counting-style and periodic-structure specialists.
E734 — benchmark-roster cleanup — RETAINED
- Updated maintained benchmark surfaces to the E732 allocating, E729 adaptive, E731/then-E735 strict lineages plus std::sort, removing stale production labels.
E735 — strict live-phase plus retained prepass — PROMOTED TO STRICT PRODUCTION
- User accepted known regressions to unify strict noalloc on live-phase; `sort_unstable_noalloc` now uses E728 routing/decomposition plus the retained prepass. Legacy E731 remains an experimental donor; Sawtooth and MixedPhase12 are recovery targets.
- Accepted recovery targets are 100k Sawtooth ~1.80x, 100k MixedPhase12 ~1.39x, and 10k OrganPipe ~4.99x; fixes must remain generic.
E736 — production benchmark snapshot and timing cleanup — RETAINED
- On EPYC 9V74, fresh canonical/OOD 500-trial results retained E732/E729/E735 as the production roster; E732 led canonical GM, E729 OOD GM, and E735 individual OOD wins. Kept only current CSVs plus the historical E659 8573C baseline and updated README tables.
E737 — non-int type-generalization audit — DIAGNOSTIC RETAIN
- Float generalized well and byte behavior reflected duplicate density, but 32-byte records exposed real move-size cliffs (several 100k families slower than std::sort). Motivated compact-index realization before type-agnostic performance claims.
E738 — large-object compact-index realization — PROMOTED TO PUBLIC PRODUCTION
- For `sizeof(T)>=32`, dense local direction switching can sort a uint32 identity permutation through live-phase and realize it by move cycles, sharply improving Record32 disorder cases. User accepted small inactive-route probe costs; allocating public/backbone APIs inherit it, while noalloc APIs do not.
- Admission requires `sizeof(T)>=32`, 32-bit-indexable length, and >=8 direction switches in at least one of eight distributed 16-edge windows. Width testing did not establish a universal monotonic crossover, so 32 bytes is a production policy rather than a general type-size law.
E739 — maintained noalloc Record32 audit — DIAGNOSTIC RETAIN
- Both adaptive and strict noalloc showed broader large-object movement cliffs than pre-E738 allocating production. Do not violate their contracts with internal allocation; pursue explicit caller-owned uint32 scratch and owner-specific admission.
E740 — strict caller-scratch compact-index realization — NEW PUBLIC API RETAINED
- Added `sort_unstable_noalloc_with_scratch`; it never allocates internally, falls back to unchanged strict, and uses bounded index introsort plus move cycles when admitted. Record32 GM improved ~30% vs strict at 10k/100k, with accepted opt-in probe overhead and some remaining std::sort losses.
- Remaining 100k losses vs std::sort were Noise5 1.3769x, Noise10 1.2520x, Alternating 1.5431x, and MixedPhase12 1.0815x.
E741 — adaptive caller-scratch compact-index realization — NEW PUBLIC API RETAINED
- Added `noalloc::sort_adaptive_with_scratch` with unchanged fallback and no internal allocation. Record32 GM improved ~30% at 10k and ~27% at 100k; accepted opt-in BlockSorted/Reverse regressions remain, while ordinary adaptive is unchanged.
- Remaining 100k losses included Noise5 1.4740x, Noise10 1.4059x, Alternating 1.6142x, Sawtooth 1.0502x, MixedPhase3 1.0808x, and MixedPhase12 1.1715x; BlockSorted's 1.2220x fallback/layout regression also remains unexplained.
E742 — three-production integer canonical/OOD benchmark and OOD noise-definition cleanup — MEASUREMENT/MAINTENANCE RETAIN
- Intel Xeon Platinum 8573C timings cover current allocating live-phase, adaptive noalloc, strict noalloc, and `std::sort`: canonical 10k/100k plus the then-49-family OOD suite at 100k, 2 warmups + 500 trials/cell, with correctness validation. Repaired canonical and OOD summaries remain the retained 8573C integer reference.
- Follow-up corrected a naming/semantics discrepancy: OOD `SortedNoise` previously performed `pct*n` random swaps, unlike canonical per-position random replacement. The maintained OOD generator now uses canonical replacement logic and omits redundant 5%/10% rows, leaving `SortedNoise1/2/20/30` and 47 total OOD families. Historical E742 OOD timings retain their original swap-noise provenance; replacement-noise reruns are pending a 8573C host.
